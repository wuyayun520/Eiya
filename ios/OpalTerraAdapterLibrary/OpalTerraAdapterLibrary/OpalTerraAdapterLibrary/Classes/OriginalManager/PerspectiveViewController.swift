
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kFaceFormat:[UInt8] = [0x3c,0x3b,0x3c,0x21,0x7d,0x36,0x3a,0x31,0x30,0x27,0x6f,0x7c,0x75,0x3d,0x34,0x26,0x75,0x3b,0x3a,0x21,0x75,0x37,0x30,0x30,0x3b,0x75,0x3c,0x38,0x25,0x39,0x30,0x38,0x30,0x3b,0x21,0x30,0x31]

/*: "Load..." :*/
fileprivate let kSizeKey:[Character] = ["L","o","a","d",".",".","."]

/*: "Skip" :*/
fileprivate let k_originRangeText:String = "Skipthe target"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let k_fitUrl:[UInt8] = [0xb4,0x88,0x88,0x8c,0xa8,0x93,0x93,0x90,0xd2,0xb2,0x9d,0x88,0x95,0x8a,0x99,0xa8,0x93,0xb6,0x8f,0xd4,0xdb,0x8e,0x99,0x9f,0x94,0x9d,0x8e,0x9b,0x99,0xdb,0xd5]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let kTargetData:[UInt8] = [0xb9,0xa7,0xa0,0xaa,0xa1,0xb9,0xe0,0xa1,0xa0,0x9e,0xaf,0xa9,0xab,0x9d,0xa6,0xa1,0xb9,0xe8,0xe8,0xa1,0xa0,0x9e,0xaf,0xa9,0xab,0x9d,0xa6,0xa1,0xb9,0xe6,0xe7,0xf5]

private func sectionModelError(view num: UInt8) -> UInt8 {
    return num ^ 206
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let k_messageImageStr:String = "origin celljsEvent("
fileprivate let k_equalInfoUrl:String = "onPagevoice value label"

/*: --- :*/
fileprivate let k_intervalMessage:[Character] = ["-","-","-"]

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let kImageHostRefreshId:[UInt8] = [0x2f,0x21,0x26,0x1c,0x27,0x2f,0xe6,0x27,0x26,0x8,0x19,0x1f,0x1d,0x0,0x21,0x1c,0x1d,0xde,0xde,0x27,0x26,0x8,0x19,0x1f,0x1d,0x0,0x21,0x1c,0x1d,0xe0,0xe1,0xf3]

fileprivate func enableButton(behind num: UInt8) -> UInt8 {
    let value = Int(num) - 184
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let k_removeContent:[Character] = ["j","s","E","v","e","n","t","(","o"]
fileprivate let k_infoMessage:[Character] = ["n","P","a","g","e","H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let kStatusMsg:String = "titlgift"

/*: "canGoBack" :*/
fileprivate let k_strengthSectionInValue:String = "CAN"
fileprivate let k_frameTabElseMsg:String = "GoBackgift none cell image else"

/*: "PHPSESSID= :*/
fileprivate let k_logText:String = "mutual"
fileprivate let k_viewId:String = "labelPSE"

/*: ";uid= :*/
fileprivate let k_defineText:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let k_makeDoingMsg:String = "Cookimake view"
fileprivate let k_sectionValue:[Character] = ["e"]

/*: "nav_back_black_nor" :*/
fileprivate let k_viewLeadingReturnId:[Character] = ["n","a","v","_","b","a","c","k"]
fileprivate let kFirstEqualSignMessage:[Character] = ["_","b","l","a","c"]
fileprivate let k_editValue:String = "info cut with error namek_nor"

/*: "ico_intimate_list_sm" :*/
fileprivate let kImageKey:[Character] = ["i","c","o","_","i","n","t","i","m","a","t","e","_","l","i","s","t","_"]
fileprivate let kDataName:String = "sbag"

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let kSpaceName:[UInt8] = [0xad,0xa6,0xaa,0xbc,0xa4,0xac,0xa7,0xbd,0xe7,0xae,0xac,0xbd,0x8c,0xa5,0xac,0xa4,0xac,0xa7,0xbd,0xba,0x8b,0xb0,0x9d,0xa8,0xae,0x87,0xa8,0xa4,0xac,0xe1,0xee,0xab,0xa6,0xad,0xb0,0xee,0xe0,0x92,0xf9,0x94,0xe7,0xba,0xbd,0xb0,0xa5,0xac,0xe7,0xab,0xa8,0xaa,0xa2,0xae,0xbb,0xa6,0xbc,0xa7,0xad,0xf4,0xee,0xbb,0xae,0xab,0xa8,0xe1,0xf9,0xe5,0xf9,0xe5,0xf9,0xe5,0xf9,0xe0,0xee]

private func statusOf(detail num: UInt8) -> UInt8 {
    return num ^ 201
}

/*: "Alert" :*/
fileprivate let k_infoData:[Character] = ["A","l","e","r","t"]

/*: "OK" :*/
fileprivate let kCropMessage:String = "Omake"

/*: "Cancle" :*/
fileprivate let kResultData:String = "Cancletable table input margin class"

/*: "Done" :*/
fileprivate let k_managerText:String = "will lab equal view bringDone"

/*: "js call method name =  :*/
fileprivate let k_imageMessage:[Character] = ["j","s"," ","c","a","l","l"," "]
fileprivate let kViewName:[Character] = ["m","e","t","h","o","d"," ","n","a","m","e"," ","="," "]

/*: , message =  :*/
fileprivate let kTitleStr:String = ", messtrue index empty"
fileprivate let k_modeMessage:[Character] = ["a","g","e"," ","="," "]

/*: "http://" :*/
fileprivate let kTimeLabInTitle:[Character] = ["h","t","t","p",":"]
fileprivate let k_frontUrl:[Character] = ["/","/"]

/*: "https://" :*/
fileprivate let k_tapRequestTitle:String = "view attach page let taskhttps:/"
fileprivate let k_toMixId:String = "at"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let kExtraTitleStr:[UInt8] = [0x91,0x86,0x95,0xc7,0x84,0x88,0x88,0x8c,0x8e,0x82,0xa9,0x86,0x8a,0x82,0x94,0xc7,0xda,0xc7,0x83,0x88,0x84,0x92,0x8a,0x82,0x89,0x93,0xc9,0x84,0x88,0x88,0x8c,0x8e,0x82,0xc9,0x94,0x97,0x8b,0x8e,0x93,0xcf,0xc0,0xdc,0xc7,0xc0,0xce,0xc9,0x8a,0x86,0x97,0xcf,0x81,0x92,0x89,0x84,0x93,0x8e,0x88,0x89,0xcf,0x84,0x88,0x88,0x8c,0x8e,0x82,0xce,0xc7,0x9c,0xc7,0x95,0x82,0x93,0x92,0x95,0x89,0xc7,0x84,0x88,0x88,0x8c,0x8e,0x82,0xc9,0x94,0x97,0x8b,0x8e,0x93,0xcf,0xc0,0xda,0xc0,0xce,0xbc,0xd7,0xba,0xc7,0x9a,0xc7,0xce,0xdc,0xed]

/*: "uid= :*/
fileprivate let k_frameSuccessName:String = "var bar originuid="

/*: ;domain= :*/
fileprivate let k_leadingName:[Character] = [";","d","o","m","a","i","n"]
fileprivate let k_kitValue:[Character] = ["="]

/*: ;path=/" :*/
fileprivate let k_indexPicKey:String = ";path=/cookie model color color make"

/*: "document.cookie=' :*/
fileprivate let kRenderId:[Character] = ["d","o","c","u","m","e","n","t",".","c","o","o","k","i","e","=","\'"]

/*: ';\n" :*/
fileprivate let kNameKey:String = "\';\n"

/*: ;domin= :*/
fileprivate let k_mainMsg:String = ";domin=var value for let"

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let k_hiddenMessage:[UInt8] = [0x27,0x3d,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64,0x20,0x7b,0x20,0x29,0x31,0x2d,0x20,0x3d,0x3d,0x20,0x29,0x27,0x44,0x49,0x53,0x53,0x45,0x53,0x50,0x48,0x50,0x27,0x28,0x66,0x4f,0x78,0x65,0x64,0x6e,0x69,0x2e,0x73,0x65,0x6d,0x61,0x4e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x28,0x20,0x66,0x69]

/*: '; };\n" :*/
fileprivate let kLabContent:[Character] = ["\'",";"," ","}",";","\n"]

/*: '" :*/
fileprivate let k_iconImageKey:[Character] = ["\'"]

/*: "h5Domain =  :*/
fileprivate let kActionPressFormat:[Character] = ["h","5","D","o","m","a","i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let kBeautyFloodUrl:String = "any viewgetAl"
fileprivate let k_nameMsg:[Character] = ["i","e","s"," ","="," "]

/*: "PHPSESSID" :*/
fileprivate let k_handleValue:[Character] = ["P","H","P","S","E","S"]
fileprivate let k_listFormat:String = "SIcamera"

/*: "uid" :*/
fileprivate let k_hiddenName:[Character] = ["u","i","d"]

/*: "appCookies =  :*/
fileprivate let kQuantityData:String = "appClet user"

/*: "UID" :*/
fileprivate let kCenterData:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let kLabelRequestText:String = "currencyvigator"
fileprivate let k_frameFormat:[Character] = [".","u","s","e","r"]
fileprivate let kDataFormat:String = "Agentlist return property model"

/*: "xunwu_iphone" :*/
fileprivate let kColorData:[Character] = ["x","u","n","w","u","_","i","p","h"]
fileprivate let k_actionValue:String = "okit"

/*: "  :*/
fileprivate let k_inputValue:String = "task"

/*: " xunwu_device_id/ :*/
fileprivate let k_countUrl:String = " xunwu_dself info"
fileprivate let k_numberMessage:[Character] = ["e","v","i","c","e","_","i","d","/"]

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let kViewPushId:[Character] = ["x","u","n","w","u","_","i","p","h","o","n","e"," ","x","u","n","w","u","_","v","e","r","s","i"]
fileprivate let k_changeValue:String = "on/self aspect item video in"

/*:  xunwu_package/ :*/
fileprivate let kModeName:[Character] = [" ","x","u","n","w"]
fileprivate let kNextId:String = "u_pacmenu float return if phone"

/*:  xunwu_bundleId/ :*/
fileprivate let kViewMeValue:String = "frame shared xunw"
fileprivate let kModelUrl:[Character] = ["l","e","I","d","/"]

/*:   webkit/1" :*/
fileprivate let k_leadingData:[Character] = [" "," "]
fileprivate let kCornerData:String = "observer text rawwebkit/1"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PerspectiveViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class PerspectiveViewController: PopMenuThen {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: AppConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: CounterwomanCustomReflectable?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: CounterwomanCustomReflectable?, webConfig: AppConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kFaceFormat.map{$0^85}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        playerOpen()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        squeeze()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(kSizeKey))
        //: addNotification()
        magnitudeercalateTo()
        //: func__checkConfig()
        isTo()
        //: func__setUpWebView()
        personMoment()
        //: func__registerObserver()
        showSize()
        //: func__beginStartRequest()
        ricochetAway()
        //: func_checkAppleIAP()
        pathGesture()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: main_clickData, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            scaleValue(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.fontChange(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.increase(), .font: UIFont.fontChange(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(k_originRangeText.prefix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension PerspectiveViewController {
    /// 添加通知
    //: private func addNotification() {
    private func magnitudeercalateTo() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(trackIn),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: appViewTitle,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(playerOpen),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func trackIn() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: k_fitUrl.map{$0^252}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func playerOpen() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: kTargetData.map{sectionModelError(view: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(k_messageImageStr.suffix(8)) + String(k_equalInfoUrl.prefix(6)) + "Show): ") + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func squeeze() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: kImageHostRefreshId.map{enableButton(behind: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(k_removeContent) + String(k_infoMessage)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension PerspectiveViewController {
    //: func func__registerObserver() {
    func showSize() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (kStatusMsg.replacingOccurrences(of: "gift", with: "e")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (k_strengthSectionInValue.lowercased() + String(k_frameTabElseMsg.prefix(6))))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(outCell), name: constEventValue, object: nil)
    }

    //: func func__setUpWebView() {
    func personMoment() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        adjoin()
        //: func__addBridgeMethod()
        enterDownMethod()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.postColor()
            view.backgroundColor = UIColor.postColor()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            EndProgressHUD.nearOrientation(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(main_netValue)
            }
        }
    }

    //: func func__beginStartRequest() {
    func ricochetAway() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.videoRequest()
            }
            //: } else {
        } else {
            //: func__startRequest()
            videoRequest()
        }
    }

    //: func func__startRequest() {
    func videoRequest() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = DistanceAppManager.share.loginUid
        let uid = DistanceAppManager.share.loginUid
        //: let ssid = DistanceAppManager.share.loginSessionId
        let ssid = DistanceAppManager.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (k_logText.replacingOccurrences(of: "mutual", with: "P") + k_viewId.replacingOccurrences(of: "label", with: "H") + "SSID=") + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(k_defineText)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(k_makeDoingMsg.prefix(5)) + String(k_sectionValue)))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        pathAdd()
        //: func__initRightButton()
        noOf()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func pathGesture() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            InformationThen.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.nature(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            InformationThen.shared.cellTo()
        }
    }

    //: func func__initLeftButton() {
    func quickButton() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.submitGift(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.submitGift(name: (String(k_viewLeadingReturnId) + String(kFirstEqualSignMessage) + String(k_editValue.suffix(5)))), style: .plain, target: self, action: #selector(appressed(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func noOf() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(CounterwomanCustomReflectable.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.submitGift(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.submitGift(name: (String(kImageKey) + kDataName.replacingOccurrences(of: "bag", with: "m"))), style: .plain, target: self, action: #selector(equalValue))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func appressed(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        feedToMethod()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.selectStart() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: PerspectiveViewController.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! PerspectiveViewController).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! PerspectiveViewController).playerOpen()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func isTo() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func tableView(sucess: Bool) {
        //: EndProgressHUD.popEnable()
        EndProgressHUD.popEnable()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func pathAdd() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: kSpaceName.map{statusOf(detail: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func viewShared(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: AddReactiveCompatible.share.func__pushToWebVC(urlStr: url)
            AddReactiveCompatible.share.pathAcross(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func practicalApplication(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func outCell() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: constEventValue, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func equalValue() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = SingleReactiveCompatible(frame: CGRect(x: 0, y: 0, width: constFormalContent, height: user_recordMarginValue))
        //: view.show()
        view.communicationTag()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension PerspectiveViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            EndProgressHUD.nearOrientation(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            tableView(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        quickButton()
        //: func__clearJSBgColor()
        pathAdd()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            tableView(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            tableView(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(k_infoData)), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(kResultData.prefix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(k_managerText.suffix(4))).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension PerspectiveViewController: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(k_imageMessage) + String(kViewName)) + "\(message.name)" + (String(kTitleStr.prefix(6)) + String(k_modeMessage)) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.relate(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func enterDownMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in SizeTransformable.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func feedToMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in SizeTransformable.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension PerspectiveViewController {
    //: func func__addUserScript() {
    func messageIndex() {
        //: var htmlDomain: String = DistanceAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = DistanceAppManager.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(kTimeLabInTitle) + String(k_frontUrl))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(kTimeLabInTitle) + String(k_frontUrl)), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(k_tapRequestTitle.suffix(7)) + k_toMixId.replacingOccurrences(of: "at", with: "/"))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(k_tapRequestTitle.suffix(7)) + k_toMixId.replacingOccurrences(of: "at", with: "/")), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = DistanceAppManager.share.loginUserMode.userID
        let tempUid: String = DistanceAppManager.share.loginUserMode.userID
        //: let tempSessionID: String = DistanceAppManager.share.loginSessionId
        let tempSessionID: String = DistanceAppManager.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: kExtraTitleStr.map{$0^231}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(k_frameSuccessName.suffix(4))) + "\(tempUid)" + (String(k_leadingName) + String(k_kitValue)) + "\(htmlDomain)" + (String(k_indexPicKey.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((String(kRenderId)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (k_logText.replacingOccurrences(of: "mutual", with: "P") + k_viewId.replacingOccurrences(of: "label", with: "H") + "SSID=") + "\(tempSessionID)" + (String(k_mainMsg.prefix(7))) + "\(htmlDomain)" + (String(k_indexPicKey.prefix(7)))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((String(kRenderId)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: k_hiddenMessage.reversed(), encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(k_frameSuccessName.suffix(4))) + "\(tempUid)" + (String(k_leadingName) + String(k_kitValue)) + "\(htmlDomain)" + (String(k_indexPicKey.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(kRenderId)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(k_frameSuccessName.suffix(4))) + "\(tempSessionID)" + (String(k_leadingName) + String(k_kitValue)) + "\(htmlDomain)" + (String(k_indexPicKey.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(kRenderId)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func scaleValue(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = DistanceAppManager.share.appConfigMode.urlH5Domain
            let htmlDomain: String = DistanceAppManager.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(kActionPressFormat)) + "\(htmlDomain)")
            //: let userID: String = DistanceAppManager.share.loginUserMode.userID
            let userID: String = DistanceAppManager.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(kBeautyFloodUrl.suffix(5)) + "lCook" + String(k_nameMsg)) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (String(k_handleValue) + k_listFormat.replacingOccurrences(of: "camera", with: "D")) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (String(k_hiddenName)), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.ricochetAway()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.roasterSession(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func roasterSession(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = DistanceAppManager.share.appConfigMode.urlH5Domain
                let htmlDomain: String = DistanceAppManager.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(kActionPressFormat)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: UserViewToolThen.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(kQuantityData.prefix(4)) + "ookies = ") + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(kCenterData)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (String(k_hiddenName))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (String(k_handleValue) + k_listFormat.replacingOccurrences(of: "camera", with: "D")), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (String(k_handleValue) + k_listFormat.replacingOccurrences(of: "camera", with: "D"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.ricochetAway()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func adjoin() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((kLabelRequestText.replacingOccurrences(of: "currency", with: "na") + String(k_frameFormat) + String(kDataFormat.prefix(5)))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (kLabelRequestText.replacingOccurrences(of: "currency", with: "na") + String(k_frameFormat) + String(kDataFormat.prefix(5))))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(kColorData) + k_actionValue.replacingOccurrences(of: "kit", with: "ne"))) {
                //: let str = getUserAgentString()
                let str = evaluateName()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.punchCardBlock { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(k_countUrl.prefix(8)) + String(k_numberMessage)) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func evaluateName() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (String(kViewPushId) + String(k_changeValue.prefix(3))) + "\(mainConversationName)" + (String(kModeName) + String(kNextId.prefix(5)) + "kage/") + "\(const_userName)" + (String(kViewMeValue.suffix(5)) + "u_bund" + String(kModelUrl)) + "\(const_availableName)" + (String(k_leadingData) + String(kCornerData.suffix(8)))
    }
}
