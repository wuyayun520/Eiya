
//: Declare String Begin

/*: "http://" :*/
fileprivate let kErrorKey:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let k_videoPath:String = "hvaluevaluep"

/*: "http:// :*/
fileprivate let kStartMessage:[Character] = ["h","t","t","p",":","/","/"]

/*: "serverSelctIndex =  :*/
fileprivate let kTextData:[Character] = ["s","e","r","v","e","r","S","e","l","c","t"]
fileprivate let kInfoStr:[Character] = ["I","n","d","e","x"," ","="," "]

/*: "serverUrlStr =  :*/
fileprivate let k_jumpText:String = "border app valueserverU"
fileprivate let kContentName:String = "rlStsize to shared tag"

/*: "app. :*/
fileprivate let k_pageFormat:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let k_textTargetModelName:[Character] = [".","c","o","m"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let k_requestValue:String = "mf/chatshow string play share return"
fileprivate let kAppId:[Character] = ["R","o","o","m","/","s","e"]
fileprivate let k_cellText:String = "beauty make size visiblendGift"

/*: "recharge/applePayNotify" :*/
fileprivate let k_modePath:String = "layerech"
fileprivate let k_sharedTitle:String = "model sizeePa"
fileprivate let k_modelFormat:String = "yNotifycount icon"

/*: "user/appleLogin" :*/
fileprivate let kPinId:[Character] = ["u","s","e","r","/","a","p"]
fileprivate let k_valueKeyTitle:[Character] = ["p","l","e","L","o","g","i","n"]

/*: "IM/getMyBlackList" :*/
fileprivate let k_modelValue:[Character] = ["I","M","/","g","e"]
fileprivate let k_headTextStr:[Character] = ["t","M","y","B","l","a"]
fileprivate let k_pageData:[Character] = ["c","k","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let k_textModelValue:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/"]
fileprivate let k_sharedStr:[Character] = ["s","e","n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let k_popTopName:String = "mf/buselse of"
fileprivate let kTitleText:String = "gift let document openndGift"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let kCustomText:String = "mf/chcollection kind text define"
fileprivate let kColorUrl:String = "atRoompath self icon let"

/*: "en" :*/
fileprivate let k_normalName:[Character] = ["e","n"]

/*: "ar" :*/
fileprivate let kStrengthId:[Character] = ["a","r"]

/*: "es" :*/
fileprivate let k_viewMessage:[Character] = ["e","s"]

/*: "pt" :*/
fileprivate let kDeviceMakeMessage:String = "intimate"

/*: "GJ21FB" :*/
fileprivate let k_viewKey:[Character] = ["G","J","2","1","F"]
fileprivate let k_addMsg:String = "frame"

/*: "完成了检测,请求状态正常" :*/
fileprivate let k_enableStr:String = "load"
fileprivate let k_appValue:[Character] = ["成","了","检","测",",","请","求","状","\u{6001}","正","常"]

/*: "接口请求失败" :*/
fileprivate let k_leadingUserStr:[Character] = ["接","\u{53e3}","请","求","失","败"]

/*: "完成了检测,需要更换server url" :*/
fileprivate let k_imageMakeId:String = "\u{5b8c}成\u{4e86}检"
fileprivate let k_infoFemaleValue:String = "view element object更换ser"
fileprivate let kEqualUrl:[Character] = ["v","e","r"," ","u","r","l"]

/*: "HTTPProxy" :*/
fileprivate let k_mainStr:String = "express nameHTTPPr"
fileprivate let k_genderTitle:String = "oxwill"

/*: "__SCOPED__" :*/
fileprivate let k_viewInfoPath:String = "__SCvalue formal icon"
fileprivate let k_extraRecordMessage:[Character] = ["O","P","E","D","_","_"]

/*: "tap" :*/
fileprivate let kAddFormat:String = "tbutton"

/*: "tun" :*/
fileprivate let k_documentValue:String = "viewn"

/*: "ipsec" :*/
fileprivate let k_colorName:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let k_iconStr:[Character] = ["p","p","p"]

/*: "func__updateTalkingCookies =  :*/
fileprivate let k_videoName:String = "height let player stringfunc__"
fileprivate let k_byId:String = "in manager managerTalkin"
fileprivate let k_fatalPath:String = "es = right view table"

/*: "https:// :*/
fileprivate let k_toFormat:[Character] = ["h","t","t"]
fileprivate let kGroupAddValue:[Character] = ["p","s",":","/","/"]

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let kAtRawStr:[UInt8] = [0x46,0x55,0x4e,0x43,0x3f,0x3f,0x44,0x45,0x4c,0x45,0x54,0x45,0x33,0x41,0x4d,0x45,0x23,0x4f,0x4f,0x4b,0x49,0x45,0x53,0x37,0x49,0x54,0x48,0x35,0x52,0x4c,0x0,0x1d,0x0]

fileprivate func lightColoredPlay(gift num: UInt8) -> UInt8 {
    let value = Int(num) - 224
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "UID" :*/
fileprivate let k_contentFormat:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let kModeMessage:String = "app"
fileprivate let kFoundationBeautyValue:String = "HPmEmmID"

/*: "func__saveAllCookies =  :*/
fileprivate let k_labelFormat:String = "funcalendar"
fileprivate let kIndexViewStr:String = "view self change voiceAllC"
fileprivate let k_touchStr:String = "send top ifs = "

/*: "func__saveAllCookies  error" :*/
fileprivate let k_blackPath:String = "in attribute sharefunc_"
fileprivate let k_playerKey:String = "pack mode caseAllC"
fileprivate let k_kitInputName:String = "kindkie"
fileprivate let kActualName:String = "succeedsucceedosucceed"

/*: "🍪 删除 :*/
fileprivate let k_appPath:[Character] = ["🍪"," ","删","除"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  UserViewToolThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum PlayerSubqueryConvertible: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class UserViewToolThen: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = UserViewToolThen()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        possibilityOn()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(kErrorKey))) || newValue.hasPrefix((k_videoPath.replacingOccurrences(of: "value", with: "t") + "s://")) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if main_failData && !appScreenTitle {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(kStartMessage)) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(kStartMessage)) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    doingRequest(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func possibilityOn() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = show_blockText.integer(forKey: dataVideoTimeValue)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(kTextData) + String(kInfoStr)) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = liveGet()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(kErrorKey))
        //: if Environment_Formal && !Encryption_Request {
        if main_failData && !appScreenTitle {
            //: serverPrefix = "http://"
            serverPrefix = (String(kErrorKey))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(k_jumpText.suffix(7)) + String(kContentName.prefix(4)) + "r = ") + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func liveGet() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func waitress(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(k_pageFormat)) + "\(main_keyValue)" + (String(k_textTargetModelName)), (String(k_pageFormat)) + "\(main_keyValue)" + (String(k_textTargetModelName))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(k_requestValue.prefix(7)) + String(kAppId) + String(k_cellText.suffix(6))),
         //: "recharge/applePayNotify",
         (k_modePath.replacingOccurrences(of: "layer", with: "r") + "arge/appl" + String(k_sharedTitle.suffix(3)) + String(k_modelFormat.prefix(7))),
         //: "user/appleLogin"] + slowPathArr1
         (String(kPinId) + String(k_valueKeyTitle))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(k_modelValue) + String(k_headTextStr) + String(k_pageData)),
         //: "mf/business/sendMsg",
         (String(k_textModelValue) + String(k_sharedStr)),
         //: "mf/business/sendGift",
         (String(k_popTopName.prefix(6)) + "iness/se" + String(kTitleText.suffix(6))),
         //: "mf/chatRoom/sendMsg"]
         (String(kCustomText.prefix(5)) + String(kColorUrl.prefix(6)) + "/sendMsg")]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = UserTextMacroDefine.getSystemLangCode()
        let lang = UserTextMacroDefine.societalLoad()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .sessionDown(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(k_viewKey) + k_addMsg.replacingOccurrences(of: "frame", with: "B")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension UserViewToolThen {
    //: func checkServerUrl() {
    func examineUrl() {
        //: if DistanceAppManager.share.networkStatus != .Unavailable {
        if DistanceAppManager.share.networkStatus != .Unavailable {
            //: func_requestHost()
            yearSize()
            //: } else {
        } else {
            //: func_starCodeTime()
            direction()
        }
    }

    //: func func_starCodeTime() {
    func direction() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.yearSize()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func clipReportReply() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func yearSize() {
        //: if DistanceAppManager.share.networkStatus == .Unavailable {
        if DistanceAppManager.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        clipReportReply()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        ModelThen.compass { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (k_enableStr.replacingOccurrences(of: "load", with: "完") + String(k_appValue)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (String(k_leadingUserStr)))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.direction()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (k_imageMakeId + "测,需要" + String(k_infoFemaleValue.suffix(5)) + String(kEqualUrl)))
                    //: self.func__changeHostUrl()
                    self.funcUrl()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func funcUrl() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        clipReportReply()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        show_blockText.set(serverSelctIndex, forKey: dataVideoTimeValue)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(kErrorKey))
        //: if Environment_Formal && !Encryption_Request {
        if main_failData, !appScreenTitle {
            //: serverPrefix = "http://"
            serverPrefix = (String(kErrorKey))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension UserViewToolThen {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func empower() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(String(k_mainStr.suffix(6)) + k_genderTitle.replacingOccurrences(of: "will", with: "y"))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func anyRemove() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(k_viewInfoPath.prefix(4)) + String(k_extraRecordMessage))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((kAddFormat.replacingOccurrences(of: "button", with: "ap"))) || keyStr.contains((k_documentValue.replacingOccurrences(of: "view", with: "tu"))) || keyStr.contains((String(k_colorName))) || keyStr.contains((String(k_iconStr))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension UserViewToolThen {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func doingRequest(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(k_videoName.suffix(6)) + "update" + String(k_byId.suffix(6)) + "gCooki" + String(k_fatalPath.prefix(5))) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = hiddenName()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = DistanceAppManager.share.appConfigMode.urlH5Domain
        var urlDomain: String = DistanceAppManager.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(kErrorKey)), with: (k_videoPath.replacingOccurrences(of: "value", with: "t") + "s://"))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((k_videoPath.replacingOccurrences(of: "value", with: "t") + "s://")) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(k_toFormat) + String(kGroupAddValue)) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            instanceCookiesSameStreetwiseShadow(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        crepeComment()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func instanceCookiesSameStreetwiseShadow(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: kAtRawStr.map{lightColoredPlay(gift: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func hiddenName() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(kErrorKey))
        //: if Environment_Formal && !Encryption_Request {
        if main_failData, !appScreenTitle {
            //: serverPrefix = "https://"
            serverPrefix = (k_videoPath.replacingOccurrences(of: "value", with: "t") + "s://")
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(k_pageFormat)) + "\(main_keyValue)" + (String(k_textTargetModelName)))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func almondCookie(note _: Notification) {
        //: if DistanceAppManager.share.loginUid.isEmptyString {
        if DistanceAppManager.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            searchUser()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            pastData()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func searchUser() {
        //: var htmlDomain: String = DistanceAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = DistanceAppManager.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(k_contentFormat)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(k_contentFormat))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (kModeMessage.replacingOccurrences(of: "app", with: "P") + kFoundationBeautyValue.replacingOccurrences(of: "m", with: "S")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(kModeMessage.replacingOccurrences(of: "app", with: "P") + kFoundationBeautyValue.replacingOccurrences(of: "m", with: "S"))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(k_contentFormat)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(k_contentFormat))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (kModeMessage.replacingOccurrences(of: "app", with: "P") + kFoundationBeautyValue.replacingOccurrences(of: "m", with: "S")) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(kModeMessage.replacingOccurrences(of: "app", with: "P") + kFoundationBeautyValue.replacingOccurrences(of: "m", with: "S"))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(k_contentFormat))), h5Cookie.keys.contains((String(k_contentFormat))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(k_contentFormat))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(k_contentFormat))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((kModeMessage.replacingOccurrences(of: "app", with: "P") + kFoundationBeautyValue.replacingOccurrences(of: "m", with: "S"))), h5Cookie.keys.contains((kModeMessage.replacingOccurrences(of: "app", with: "P") + kFoundationBeautyValue.replacingOccurrences(of: "m", with: "S"))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(kModeMessage.replacingOccurrences(of: "app", with: "P") + kFoundationBeautyValue.replacingOccurrences(of: "m", with: "S"))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(kModeMessage.replacingOccurrences(of: "app", with: "P") + kFoundationBeautyValue.replacingOccurrences(of: "m", with: "S"))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func pastData() {
        //: let htmlDomain: String = DistanceAppManager.share.appConfigMode.urlH5Domain
        let htmlDomain: String = DistanceAppManager.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == DistanceAppManager.share.loginUserMode.userID {
            if cookie.name == (String(k_contentFormat)), cookie.value == DistanceAppManager.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (kModeMessage.replacingOccurrences(of: "app", with: "P") + kFoundationBeautyValue.replacingOccurrences(of: "m", with: "S")) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(k_contentFormat)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(k_contentFormat))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (kModeMessage.replacingOccurrences(of: "app", with: "P") + kFoundationBeautyValue.replacingOccurrences(of: "m", with: "S")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(kModeMessage.replacingOccurrences(of: "app", with: "P") + kFoundationBeautyValue.replacingOccurrences(of: "m", with: "S"))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(k_contentFormat))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(k_contentFormat))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((kModeMessage.replacingOccurrences(of: "app", with: "P") + kFoundationBeautyValue.replacingOccurrences(of: "m", with: "S"))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(kModeMessage.replacingOccurrences(of: "app", with: "P") + kFoundationBeautyValue.replacingOccurrences(of: "m", with: "S"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func crepeComment() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (k_labelFormat.replacingOccurrences(of: "calendar", with: "c") + "__save" + String(kIndexViewStr.suffix(4)) + "ookie" + String(k_touchStr.suffix(4))) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            show_blockText.set(cookieData, forKey: showIconName)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(k_blackPath.suffix(5)) + "_save" + String(k_playerKey.suffix(4)) + k_kitInputName.replacingOccurrences(of: "kind", with: "oo") + "s  e" + kActualName.replacingOccurrences(of: "succeed", with: "r")))
        }
    }

    //: func func__setAppCookies() {
    func cookies() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = show_blockText.object(forKey: showIconName)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func disregard() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        show_blockText.removeObject(forKey: showIconName)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
