
//: Declare String Begin

/*: / :*/
fileprivate let kCoverMessage:[Character] = ["/"]

/*:  ( :*/
fileprivate let kAvailableMsg:String = " ("

/*: ; build: :*/
fileprivate let kWhiteTitle:String = "; build:index last a remove direct"

/*: ; iOS  :*/
fileprivate let kAfterPath:String = "; iOS index view case"

/*: ;  :*/
fileprivate let kQuantityTitle:[Character] = [";"," "]

/*: )" :*/
fileprivate let k_dataMsg:[Character] = [")"]

/*: "audio" :*/
fileprivate let k_userPageUrl:[Character] = ["a","u","d","i","o"]

/*: + :*/
fileprivate let kProcessorValue:String = "shrug"

/*: .wav" :*/
fileprivate let kActionId:String = "guard tool now resource add.wav"

/*: "audio/wav" :*/
fileprivate let kArrayKey:[Character] = ["a","u"]
fileprivate let k_partyValue:[Character] = ["d","i","o","/","w","a","v"]

/*: .jpg" :*/
fileprivate let k_dataFormat:String = "gift clear text.jpg"

/*: "image/jpeg" :*/
fileprivate let kTitleMessage:String = "leadingmag"

/*: "://app." :*/
fileprivate let kTitleData:[Character] = [":","/","/","a","p","p","."]

/*: "data" :*/
fileprivate let k_pushStackUserStr:[Character] = ["d","a","t","a"]

/*: ":null" :*/
fileprivate let kAddressText:String = ":nullvar list result"

/*: ":{}" :*/
fileprivate let k_submitUrl:String = ":{equal"

/*: "Request failed, Try again later" :*/
fileprivate let kBackgroundKey:[UInt8] = [0xbf,0x88,0x9c,0x98,0x88,0x9e,0x99,0xcd,0x8b,0x8c,0x84,0x81,0x88,0x89,0xc1,0xcd,0xb9,0x9f,0x94,0xcd,0x8c,0x8a,0x8c,0x84,0x83,0xcd,0x81,0x8c,0x99,0x88,0x9f]

/*: "json error" :*/
fileprivate let kModelContent:[Character] = ["j","s","o","n"," ","e","r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let k_workFormat:String = "PLAT"
fileprivate let k_sharePath:String = "false cell length receive crop=ipho"
fileprivate let kRequestId:[Character] = ["s","i","o","n","="]

/*: &packageId= :*/
fileprivate let k_listText:[Character] = ["&"]
fileprivate let kFileName:String = "pacdetailg"

/*: &bundleId= :*/
fileprivate let k_toContent:String = "not record&bun"
fileprivate let k_titleKey:[Character] = ["d","l","e","I","d","="]

/*: &lang= :*/
fileprivate let k_levelMessage:[Character] = ["&","l","a","n","g","="]

/*: "?!@#$^&%*+,:;='" :*/
fileprivate let k_skipHiddenId:[Character] = ["?","!","@","#","$","^","&","%","*"]
fileprivate let k_changeId:String = "+,:;=\'"

/*: "`<>()[]{}/\\| " :*/
fileprivate let k_frameStr:[Character] = ["`","<",">","(",")","[","]"]
fileprivate let k_makeStr:String = "host"
fileprivate let k_imageTitle:String = "}/\\| "

/*: "/route/slowindex" :*/
fileprivate let k_equalKey:[Character] = ["/","r","o","u","t","e","/","s"]
fileprivate let kEndFormat:[Character] = ["l","o","w","i","n","d","e","x"]

/*: "/route/index" :*/
fileprivate let k_tabPath:String = "to view view self zone/rou"

/*: "?path= :*/
fileprivate let kScaleData:String = "random"
fileprivate let kPostId:[Character] = ["p","a","t","h","="]

/*: "&GJNonceStr= :*/
fileprivate let k_deviceKey:[Character] = ["&","G","J","N","o"]
fileprivate let k_tempAddText:String = "view self adnceStr="

/*: "& :*/
fileprivate let kTopUrl:String = "&"

/*: = :*/
fileprivate let kStyleValue:[Character] = ["="]

/*: "&params= :*/
fileprivate let kTimeNameMessage:[Character] = ["&","p","a"]
fileprivate let kLiveName:String = "self view to gift pathrams="

/*: "/ :*/
fileprivate let k_userMsg:[Character] = ["/"]

/*: "? :*/
fileprivate let k_nameKey:[Character] = ["?"]

/*: "token" :*/
fileprivate let kViewEqualName:String = "toinfoen"

/*: "请求成功 :*/
fileprivate let k_thirdFormat:[Character] = ["请","求","成","\u{529f}"]

/*: "AAAAAAAA" :*/
fileprivate let k_beautyData:[Character] = ["A","A","A","A","A","A"]
fileprivate let kValueFormat:String = "aa"

/*: "AA" :*/
fileprivate let k_marginViewUrl:String = "method"

/*: "BB" :*/
fileprivate let k_wrapId:String = "bb"

/*: "下载完成 -  :*/
fileprivate let kOfId:String = "下载完成 "
fileprivate let kEnableName:[Character] = ["-"," "]

/*: "/Documents/" :*/
fileprivate let kEqualTitle:String = "/Documenshare register super cell"
fileprivate let k_inputStartStr:[Character] = ["t","s","/"]

/*: ".mp4" :*/
fileprivate let k_timeIconStr:[Character] = [".","m","p","4"]

/*: "移动地址: :*/
fileprivate let k_keyText:[Character] = ["移","动","地","址",":"]

/*: "后台任务下载回来" :*/
fileprivate let kViewPath:[Character] = ["后","台","任","务","下","载","回","来"]

/*: "下载成功 : :*/
fileprivate let kDuringMsg:String = "下\u{8f7d}成功 "
fileprivate let kModelName:String = ":"

/*: "Net Error" :*/
fileprivate let k_dismissPath:[Character] = ["N","e","t"," ","E"]
fileprivate let k_objectStr:[Character] = ["r","r","o","r"]

/*: "下载失败 : :*/
fileprivate let kUserMakePath:[Character] = ["下","载","\u{5931}","败"," ",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MannerSessionDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let showHalfTitle = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let appNeedTitle = "\(kAddData)/\(userFileContent) (\(const_availableName)" + (String(kWhiteTitle.prefix(8))) + "\(showStreamValue)" + (String(kAfterPath.prefix(6))) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: PullErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let showEndName = MannerSessionDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class MannerSessionDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func buttonCompletion(model: SweepRequestModel, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.smartObject() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.errorMessageStar(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.noneStatus(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func errorMessageStar(model: SweepRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.schemeBackground(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.clickModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    handleBecome(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PullErrorResponse(errorCode: PositionEffectInsetTarget.NetError.rawValue, errorMsg: appViewPingValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        arcInfo(showMsg: appViewPingValue)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (String(k_userPageUrl)) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.upToDate())+\(const_userName)" + (String(kActionId.suffix(4))), mimeType: (String(kArrayKey) + String(k_partyValue)))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(k_dataFormat.suffix(4))), mimeType: (kTitleMessage.replacingOccurrences(of: "leading", with: "i") + "e/jpeg"))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    handleBecome(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PullErrorResponse(errorCode: PositionEffectInsetTarget.NetError.rawValue, errorMsg: appViewPingValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        arcInfo(showMsg: appViewPingValue)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func noneStatus(model: SweepRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.schemeBackground(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.clickModel(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: appScreenTitle ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = showHalfTitle }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    handleBecome(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PullErrorResponse(errorCode: PositionEffectInsetTarget.NetError.rawValue, errorMsg: appViewPingValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        arcInfo(showMsg: appViewPingValue)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = showHalfTitle }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    handleBecome(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, PullErrorResponse(errorCode: PositionEffectInsetTarget.NetError.rawValue, errorMsg: appViewPingValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        arcInfo(showMsg: appViewPingValue)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func handleBecome(model: SweepRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            UserViewToolThen.share.doingRequest(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if appScreenTitle, model.requestServer.contains((String(kTitleData))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.belowImageDate(key: UserViewToolThen.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (String(k_pushStackUserStr)) + "\"" + (String(kAddressText.prefix(5))), with: "" + "\"" + (String(k_pushStackUserStr)) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !appScreenTitle {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(kAddressText.prefix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<SimulationModelType>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == PositionEffectInsetTarget.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, PullErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    arcInfo(showMsg: responseModel.msg ?? String(bytes: kBackgroundKey.map{$0^237}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case PositionEffectInsetTarget.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: notiButtonData, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, PullErrorResponse(errorCode: PositionEffectInsetTarget.NetError.rawValue, errorMsg: (String(kModelContent)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func schemeBackground(model: SweepRequestModel) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (k_workFormat.lowercased() + "form" + String(k_sharePath.suffix(5)) + "ne&ver" + String(kRequestId)) + "\(mainConversationName)" + (String(k_listText) + kFileName.replacingOccurrences(of: "detail", with: "ka") + "eId=") + "\(const_userName)" + (String(k_toContent.suffix(4)) + String(k_titleKey)) + "\(const_availableName)" + (String(k_levelMessage)) + "\(UserViewToolThen.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if appScreenTitle, serverUrl.contains((String(kTitleData))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if UserViewToolThen.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(k_equalKey) + String(kEndFormat)))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(k_tabPath.suffix(4)) + "te/index")) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((kScaleData.replacingOccurrences(of: "random", with: "?") + String(kPostId)) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(k_deviceKey) + String(k_tempAddText.suffix(7))) + "\(UserViewToolThen.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.adjustInUp(key: UserViewToolThen.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(kTimeNameMessage) + String(kLiveName.suffix(5))) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func clickModel(model: SweepRequestModel) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(appNeedTitle)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (kViewEqualName.replacingOccurrences(of: "info", with: "k")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func practice(model: SweepRequestModel) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.schemeBackground(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (String(k_thirdFormat)) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (String(k_beautyData) + kValueFormat.uppercased())) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (kOfId.capitalized + String(kEnableName)) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(kEqualTitle.prefix(8)) + String(k_inputStartStr)) + NSDate.upToDate() + (String(k_timeIconStr))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (String(k_keyText)) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (String(kViewPath)))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = RegisterDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func holdDoingInwards(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: data_statusText.doingApp(), with: main_keyValue)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (kDuringMsg + kModelName.capitalized) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, PullErrorResponse(errorCode: PositionEffectInsetTarget.Normal.rawValue, errorMsg: (String(k_dismissPath) + String(k_objectStr)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, PullErrorResponse(errorCode: PositionEffectInsetTarget.NetError.rawValue, errorMsg: (String(k_dismissPath) + String(k_objectStr)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (String(kUserMakePath)) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func textCompletion(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: data_statusText.doingApp(), with: main_keyValue)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (kDuringMsg + kModelName.capitalized) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, PullErrorResponse(errorCode: PositionEffectInsetTarget.Normal.rawValue, errorMsg: (String(k_dismissPath) + String(k_objectStr)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, PullErrorResponse(errorCode: PositionEffectInsetTarget.NetError.rawValue, errorMsg: (String(k_dismissPath) + String(k_objectStr)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (String(kUserMakePath)) + "\(errMsg)")
            }
        }
    }
}
