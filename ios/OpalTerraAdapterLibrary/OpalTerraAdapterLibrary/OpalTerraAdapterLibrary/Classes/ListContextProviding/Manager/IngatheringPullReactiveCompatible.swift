
//: Declare String Begin

/*: "setPassword" :*/
fileprivate let kLeadingMessage:String = "seshared"
fileprivate let kLetterValue:String = "me userPass"

/*: "user/appleLogin" :*/
fileprivate let kButtonMessage:String = "presenter"
fileprivate let kMakeToPath:String = "eLogintext else"

/*: "user/googleLogin" :*/
fileprivate let kKitStr:[Character] = ["u","s","e","r","/","g","o","o","g","l","e","L","o","g","i","n"]

/*: "user/smsLogin" :*/
fileprivate let k_modelStr:[Character] = ["u","s","e","r","/","s","m","s","L","o"]
fileprivate let k_cookieStr:String = "gerror"

/*: "user/emailVcodeLogin" :*/
fileprivate let k_aidStr:String = "size renderuser/e"
fileprivate let kGiftFormat:String = "any make sharedcodeLo"
fileprivate let kListMsg:String = "framen"

/*: "user/pwdLogin" :*/
fileprivate let k_numberVideoMessage:String = "type top back atuser/pw"
fileprivate let kMakeMessage:String = "dLoginin for let in background"

/*: "user/emailPwdLogin" :*/
fileprivate let kValueKey:String = "frame path guard trueuser/e"
fileprivate let k_sendName:String = "Logincurve normal"

/*: "+ :*/
fileprivate let k_giftFormat:[Character] = ["+"]

/*: "mobile" :*/
fileprivate let kBlockMatchMessage:[UInt8] = [0x1b,0x19,0x14,0x1f,0x1a,0x13]

private func arrayMake(current num: UInt8) -> UInt8 {
    return num ^ 118
}

/*: "areaCode" :*/
fileprivate let k_styleStr:[UInt8] = [0x6c,0x7f,0x68,0x6c,0x4e,0x62,0x69,0x68]

private func playerModel(bottom num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "user/sendLoginSms" :*/
fileprivate let kAddCellFormat:[Character] = ["u","s","e","r","/","s","e","n","d","L","o","g"]
fileprivate let k_firstPath:String = "net toinSms"

/*: "RSA加密失败" :*/
fileprivate let k_darkValue:[Character] = ["R","S","A","加","密","失","败"]

/*: "password" :*/
fileprivate let kModelContent:[UInt8] = [0x69,0x5a,0x6c,0x6c,0x70,0x68,0x6b,0x5d]

fileprivate func borderPageView(view num: UInt8) -> UInt8 {
    let value = Int(num) - 249
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "vcode" :*/
fileprivate let k_fileListKey:[UInt8] = [0x6e,0x5b,0x67,0x5c,0x5d]

fileprivate func tableShow(format num: UInt8) -> UInt8 {
    let value = Int(num) + 8
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "user/modifyPassword" :*/
fileprivate let k_uponMessage:[Character] = ["u","s","e","r","/","m","o","d","i","f","y","P","a"]
fileprivate let kResignName:String = "sizeword"

/*: "user/sendModifyPwdSms" :*/
fileprivate let kBlockValue:[Character] = ["u","s","e","r","/","s","e","n","d","M","o","d","i","f","y","P","w"]
fileprivate let kDetailKey:[Character] = ["d","S","m","s"]

/*: "user/sendModifyPwdEmail" :*/
fileprivate let k_statusCloseName:String = "user/size if"
fileprivate let kColorFromUrl:String = "difyPwdmake record"
fileprivate let kAgainNameValue:[Character] = ["E","m","a","i","l"]

/*: "mf/user/getMyInfo" :*/
fileprivate let kMessageStr:[Character] = ["m","f","/"]
fileprivate let kWrapUrl:[Character] = ["u","s","e","r","/","g","e","t","M","y","I","n","f","o"]

/*: "invite/bindInviter" :*/
fileprivate let k_managerKey:[Character] = ["i","n","v","i","t","e","/","b","i","n","d"]
fileprivate let kDataPath:String = "Invitertable false user extension number"

/*: "mf/user/CheckInviteCode" :*/
fileprivate let kStatuteMaxName:String = "mf/uslabel any send else make"
fileprivate let k_makeData:String = "hecback"
fileprivate let kTitleKey:[Character] = ["I","n","v","i","t","e","C","o","d","e"]

/*: "mf/user/updateInfo" :*/
fileprivate let kViewPath:String = "asset image view page formf/user/u"
fileprivate let kItemValue:String = "pto"
fileprivate let k_modelPath:String = "section range view text themeateInfo"

/*: "p0" :*/
fileprivate let kTargetText:[Character] = ["p","0"]

/*: "email" :*/
fileprivate let k_partyMsg:[UInt8] = [0x6c,0x69,0x61,0x6d,0x65]

/*: "user/getEmailBindVCode" :*/
fileprivate let k_modelData:String = "utor"
fileprivate let kSearchedName:String = "mailBindpending self print to"
fileprivate let kLabelId:[Character] = ["V","C","o","d","e"]

/*: "user/sendLoginEmail" :*/
fileprivate let kTitleData:String = "cell center text layeruser/se"
fileprivate let k_cornerId:[Character] = ["n","d","L","o","g","i","n","E","m","a","i","l"]

/*: "user/emailBind" :*/
fileprivate let k_componentStr:String = "capture"
fileprivate let k_toFormat:[Character] = ["s","e","r","/","e","m","a","i","l","B","i","n","d"]

/*: "user/logout" :*/
fileprivate let k_serviceFormat:[Character] = ["u","s","e","r","/"]
fileprivate let k_lineId:[Character] = ["l","o","g","o","u","t"]

/*: "app/countryList" :*/
fileprivate let k_byData:String = "app/coucolor name"
fileprivate let k_layerMessage:String = "ntcelly"

/*: "user/getMobileBindVCode" :*/
fileprivate let k_makeMsg:String = "private useruser/"
fileprivate let k_viewFormat:[Character] = ["g","e","t","M","o","b","i","l","e","B","i","n","d","V","C","o","d","e"]

/*: "user/mobileBind" :*/
fileprivate let k_removeName:String = "usesuccess"
fileprivate let k_labModelTouchPath:String = "send partyileBind"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  IngatheringPullReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/29.
//

//: import UIKit
import UIKit

//: class TalkingLoginRequestTool: NSObject {
class IngatheringPullReactiveCompatible: NSObject {
    /// 第三方登录请求
    /// - Parameters:
    ///   - type: 登录类型
    ///   - content: 未加密手机号/邮箱
    ///   - params: 参数
    ///   - completion: 回调

    //: class func req_thirdLogin(type: LoginType, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
    class func inner(type: PalmClusterLiteral, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = self.req_thirdLoginModel(type: type, params: params)
        let reqModel = self.appeal(type: type, params: params)
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: return
                return
            }
            // 跳转设置密码页
            //: let json = JSON(result ?? [:])
            let json = JSON(result ?? [:])
            //: if json["setPassword"].boolValue == true {
            if json[(kLeadingMessage.replacingOccurrences(of: "shared", with: "t") + String(kLetterValue.suffix(4)) + "word")].boolValue == true {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: AddReactiveCompatible.share.func__pushToSetNewPasswordVC(type: type, content: content, isNewPwd: true)
                AddReactiveCompatible.share.confusion(type: type, content: content, isNewPwd: true)
                //: return
                return
            }

            // 获取用户信息
            //: TalkingLoginRequestTool.req_LoginUserInfoData(type, completion: completion)
            IngatheringPullReactiveCompatible.more(type, completion: completion)
        }
    }

    /// 第三方登录请求模型
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    /// - Returns: 请求模型
    //: private class func req_thirdLoginModel(type: LoginType, params: [String: String]) -> TalkingRequestModel {
    private class func appeal(type: PalmClusterLiteral, params: [String: String]) -> SweepRequestModel {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SweepRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: switch type {
        switch type {
        //: case .AppleLogin:
        case .AppleLogin:
            //: reqModel.requestPath = "user/appleLogin"
            reqModel.requestPath = (kButtonMessage.replacingOccurrences(of: "present", with: "us") + "/appl" + String(kMakeToPath.prefix(6)))
        //: case .GoogleLogin:
        case .GoogleLogin:
            //: reqModel.requestPath = "user/googleLogin"
            reqModel.requestPath = (String(kKitStr))
        //: case .PhoneLogin:
        case .PhoneLogin:
            //: reqModel.requestPath = "user/smsLogin"
            reqModel.requestPath = (String(k_modelStr) + k_cookieStr.replacingOccurrences(of: "error", with: "in"))
        //: case .EmailLogin:
        case .EmailLogin:
            //: reqModel.requestPath = "user/emailVcodeLogin"
            reqModel.requestPath = (String(k_aidStr.suffix(6)) + "mailV" + String(kGiftFormat.suffix(6)) + kListMsg.replacingOccurrences(of: "frame", with: "gi"))
        //: case .PwdPhoneLogin:
        case .PwdPhoneLogin:
            //: reqModel.requestPath = "user/pwdLogin"
            reqModel.requestPath = (String(k_numberVideoMessage.suffix(7)) + String(kMakeMessage.prefix(6)))
        //: case .PwdEmailLogin:
        case .PwdEmailLogin:
            //: reqModel.requestPath = "user/emailPwdLogin"
            reqModel.requestPath = (String(kValueKey.suffix(6)) + "mailPwd" + String(k_sendName.prefix(5)))
        }
        //: reqModel.params = params
        reqModel.params = params

        //: return reqModel
        return reqModel
    }

    /// 获取验证码
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_getPhoneCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func phone(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: k_busyName)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: kBlockMatchMessage.map{arrayMake(current: $0)}, encoding: .utf8)!: reslutStr, String(bytes: k_styleStr.map{playerModel(bottom: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = SweepRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/sendLoginSms"
            reqModel.requestPath = (String(kAddCellFormat) + String(k_firstPath.suffix(5)))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(k_darkValue)))
        }
    }

    /// 密码登录
    /// - Parameters:
    ///   - phoneStr: 手机号登录
    ///   - areaStr: 区号
    ///   - pwd: 密码
    ///   - completion: 回调
    //: class func req_passwordLogin(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func toMark(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultPhoneStr = ""
        var resultPhoneStr = ""
        //: var resultCodeStr = ""
        var resultCodeStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: k_busyName)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": resultPhoneStr, "areaCode": resultCodeStr, "password": resultPwdStr]
            let params = [String(bytes: kBlockMatchMessage.map{arrayMake(current: $0)}, encoding: .utf8)!: resultPhoneStr, String(bytes: k_styleStr.map{playerModel(bottom: $0)}, encoding: .utf8)!: resultCodeStr, String(bytes: kModelContent.map{borderPageView(view: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdPhoneLogin, params: params, completion: completion)
            IngatheringPullReactiveCompatible.inner(type: .PwdPhoneLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(k_darkValue)))
        }
    }

    /// 修改密码
    /// - Parameters:
    ///   - pwd: 密码
    ///   - vcode: 验证码
    ///   - completion: 回调
    //: class func req_modifyPassword(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
    class func gift(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: k_busyName)
            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: var params = ["password": resultPwdStr]
            var params = [String(bytes: kModelContent.map{borderPageView(view: $0)}, encoding: .utf8)!: resultPwdStr]

            //: if vcode?.count ?? 0 > 0 {
            if vcode?.count ?? 0 > 0 {
                //: params["vcode"] = vcode
                params[String(bytes: k_fileListKey.map{tableShow(format: $0)}, encoding: .utf8)!] = vcode
            }

            //: let reqModel = TalkingRequestModel()
            let reqModel = SweepRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/modifyPassword"
            reqModel.requestPath = (String(k_uponMessage) + kResignName.replacingOccurrences(of: "size", with: "ss"))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(k_darkValue)))
        }
    }

    /// 获取修改【手机号】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdSms(completion: @escaping FinishBlock) {
    class func along(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdSms"
        reqModel.requestPath = (String(kBlockValue) + String(kDetailKey))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取修改【邮箱】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdEmail(completion: @escaping FinishBlock) {
    class func tip(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdEmail"
        reqModel.requestPath = (String(k_statusCloseName.prefix(5)) + "sendMo" + String(kColorFromUrl.prefix(7)) + String(kAgainNameValue))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户信息接口
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_LoginUserInfoData(_ type: LoginType, completion: FinishBlock? = nil) {
    class func more(_ type: PalmClusterLiteral, completion: FinishBlock? = nil) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(kMessageStr) + String(kWrapUrl))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: return
                return
            }
            //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
            show_blockText.set(result, forKey: notiClickStatusTitle)
            //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<CoverReactiveCompatible>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: DistanceAppManager.share.loginUserMode = userModel
                DistanceAppManager.share.loginUserMode = userModel

                // 记录上次登录类型
                //: Defaults.set(type.rawValue, forKey: TalkingLastLoginTypeCacheKey)
                show_blockText.set(type.rawValue, forKey: noti_buttonData)
                //: if DistanceAppManager.share.loginUserMode.updateInfo == false {
                if DistanceAppManager.share.loginUserMode.updateInfo == false { // 需要完善资料
                    //: completion?(false, nil, errorModel)
                    completion?(false, nil, errorModel)
                    //: AddReactiveCompatible.share.func__pushToFillUserInfoVC()
                    AddReactiveCompatible.share.notFillAgainText()
                    //: return
                    return
                }

                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                NotificationCenter.default.post(name: user_liveVersionValue, object: nil, userInfo: result as! [String: Any])

                //: if DistanceAppManager.share.loginUserMode.remindBindEmail == true {
                if DistanceAppManager.share.loginUserMode.remindBindEmail == true { // 需要绑定邮箱
                    //: DispatchQueue.main.asyncAfter(deadline: .now()+0.5) {
                    DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
                        //: AddReactiveCompatible.share.func__pushToLockUserEmailVC(isShowBack: false)
                        AddReactiveCompatible.share.makeColorView(isShowBack: false)
                    }
                }
            }
        }
    }

    /// 绑定邀请邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_inviteBindInviter(params: [String: String], completion: @escaping FinishBlock) {
    class func fileCompletion(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "invite/bindInviter"
        reqModel.requestPath = (String(k_managerKey) + String(kDataPath.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测邀请码
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_CheckCodeData(params: [String: String], completion: @escaping FinishBlock) {
    class func panoramicView(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/user/CheckInviteCode"
        reqModel.requestPath = (String(kStatuteMaxName.prefix(5)) + "er/C" + k_makeData.replacingOccurrences(of: "back", with: "k") + String(kTitleKey))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新用户信息
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_updateUserInfo(params: [String: Any], completion: @escaping FinishBlock) {
    class func show(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/user/updateInfo"
        reqModel.requestPath = (String(kViewPath.suffix(9)) + kItemValue.replacingOccurrences(of: "to", with: "d") + String(k_modelPath.suffix(7)))
        //: var temparams = params
        var temparams = params
        //: temparams += UIDevice.deviceInfoDic
        temparams += UIDevice.deviceInfoDic
        //: if let poStr = temparams["p0"] {
        if let poStr = temparams["p0"] {
            //: let token = NSDate.getCurrentTimeStamp()
            let token = NSDate.upToDate()
            //: reqModel.addHeaderToken = token
            reqModel.addHeaderToken = token
            //: let key = token.appending(TokenSaltStr)
            let key = token.appending(main_successTitle)
            //: temparams["p0"] = (poStr as? String)?.encrypt(withKey: key)
            temparams["p0"] = (poStr as? String)?.encrypt(withKey: key)
        }
        //: reqModel.params = temparams
        reqModel.params = temparams
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// Email Code
    /// - Parameters:
    ///   - emailStr: Email
    ///   - emailType: Login or Bind
    //: class func req_getBindEmailCode( emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
    class func pick(emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: k_busyName)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: let params = ["email": reslutStr]
            let params = [String(bytes: k_partyMsg.reversed(), encoding: .utf8)!: reslutStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = SweepRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: if isBind {
            if isBind {
                //: reqModel.requestPath = "user/getEmailBindVCode"
                reqModel.requestPath = (k_modelData.replacingOccurrences(of: "to", with: "se") + "/getE" + String(kSearchedName.prefix(8)) + String(kLabelId))
                //: } else {
            } else {
                //: reqModel.requestPath = "user/sendLoginEmail"
                reqModel.requestPath = (String(kTitleData.suffix(7)) + String(k_cornerId))
            }
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(k_darkValue)))
        }
    }

    /// 绑定邮箱验证码
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_bindEmailCode( emailCode: String, completion: @escaping FinishBlock) {
    class func stick(emailCode: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SweepRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "user/emailBind"
        reqModel.requestPath = (k_componentStr.replacingOccurrences(of: "capture", with: "u") + String(k_toFormat))
        //: reqModel.params = ["vcode": emailCode]
        reqModel.params = [String(bytes: k_fileListKey.map{tableShow(format: $0)}, encoding: .utf8)!: emailCode]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: class func req_passwordEmailLogin(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func queryClick(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultEmailStr = ""
        var resultEmailStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: k_busyName)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["email": resultEmailStr, "password": resultPwdStr]
            let params = [String(bytes: k_partyMsg.reversed(), encoding: .utf8)!: resultEmailStr, String(bytes: kModelContent.map{borderPageView(view: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdEmailLogin, params: params, completion: completion)
            IngatheringPullReactiveCompatible.inner(type: .PwdEmailLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(k_darkValue)))
        }
    }

    //: typealias RequestLoginOutBlock = (_ t: Bool) -> Void
    typealias RequestLoginOutBlock = (_ t: Bool) -> Void

    //: @objc class func req_loginOut(completion: @escaping RequestLoginOutBlock) {
    @objc class func harvestMoon(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "user/logout"
        reqModel.requestPath = (String(k_serviceFormat) + String(k_lineId))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    //: @objc class func func__requestReloginUserInfo(completion: @escaping RequestLoginOutBlock) {
    @objc class func listRecordSort(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(kMessageStr) + String(kWrapUrl))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
                show_blockText.set(result, forKey: notiClickStatusTitle)
                //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
                if let userModel = JSONDeserializer<CoverReactiveCompatible>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                    //: DistanceAppManager.share.loginUserMode = userModel
                    DistanceAppManager.share.loginUserMode = userModel
                }
            }
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 获取允许国家列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_countryList( completion: @escaping FinishBlock) {
    class func developingTotalry(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "app/countryList"
        reqModel.requestPath = (String(k_byData.prefix(7)) + k_layerMessage.replacingOccurrences(of: "cell", with: "r") + "List")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 绑定手机号获取验证码
    //: class func req_bindPhoneGetCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func textDropCompletion(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: k_busyName)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: kBlockMatchMessage.map{arrayMake(current: $0)}, encoding: .utf8)!: reslutStr, String(bytes: k_styleStr.map{playerModel(bottom: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = SweepRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/getMobileBindVCode"
            reqModel.requestPath = (String(k_makeMsg.suffix(5)) + String(k_viewFormat))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(k_darkValue)))
        }
    }

    /// 绑定手机号
    //: class func req_MobileBind(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
    class func editWith(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: k_busyName)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr, "vcode": vcode]
            let params = [String(bytes: kBlockMatchMessage.map{arrayMake(current: $0)}, encoding: .utf8)!: reslutStr, String(bytes: k_styleStr.map{playerModel(bottom: $0)}, encoding: .utf8)!: reslutCodeStr, String(bytes: k_fileListKey.map{tableShow(format: $0)}, encoding: .utf8)!: vcode]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = SweepRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/mobileBind"
            reqModel.requestPath = (k_removeName.replacingOccurrences(of: "success", with: "r") + "/mob" + String(k_labModelTouchPath.suffix(7)))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(k_darkValue)))
        }
    }
}
