
//: Declare String Begin

/*: "PARTY_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let kFrameStartStr:[UInt8] = [0x53,0x42,0x51,0x57,0x5a,0x5c,0x50,0x57,0x42,0x57,0x56,0x50,0x5c,0x40,0x4b,0x42,0x4d,0x44,0x46,0x5c,0x4d,0x4c,0x57,0x4a,0x45,0x4a,0x40,0x42,0x57,0x4a,0x4c,0x4d]

private func socialUnit(quit num: UInt8) -> UInt8 {
    return num ^ 3
}

/*: "p- :*/
fileprivate let kBottomUrl:String = "p-map path"

/*: - :*/
fileprivate let kWrapValue:String = "-"

/*: "redirectUrl" :*/
fileprivate let kPriorityText:[Character] = ["r","e","d","i","r","e","c","t","U"]
fileprivate let kRequestModelEditFormat:[Character] = ["r","l"]

/*: "VOICEROOM: 进房模型解析失败。" :*/
fileprivate let kColorMessage:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","进","房"]
fileprivate let kMessageName:String = "模\u{578b}解析失败\u{3002}"

/*: "Party room exception" :*/
fileprivate let k_gestureUrl:String = "message touch layer share forPart"
fileprivate let k_actionId:String = "table available anyom exc"
fileprivate let k_cellWithUrl:String = "search"

/*: _ :*/
fileprivate let kDenyKey:String = "normal"

/*: "male" :*/
fileprivate let kLeadingStr:[UInt8] = [0xc2,0xce,0xc3,0xca]

/*: "female" :*/
fileprivate let kTailPath:[Character] = ["f","e","m","a","l","e"]

/*: "VOICEROOM: 群聊进房失败。groupId:  :*/
fileprivate let k_equalData:String = "VreceiveICER"
fileprivate let k_makeBySectionData:[Character] = [" ","\u{7fa4}","\u{804a}","进","\u{623f}","失","\u{8d25}","。","g","r"]
fileprivate let k_frameKey:String = "import dataoupId: "

/*: ." :*/
fileprivate let k_succeedBlockUrl:[Character] = ["."]

/*: "VOICEROOM: 切房模型解析失败。" :*/
fileprivate let kSexFailureId:String = "VOICerrorROOM"
fileprivate let kLeadingName:String = ": equal房模型"

/*: "VOICEROOM: 群聊切房失败。groupId:  :*/
fileprivate let kAtPath:String = "VOmakeCER"
fileprivate let k_frameData:String = " 群\u{804a}切房失败"
fileprivate let k_wantMessage:[Character] = ["。","g","r","o","u","p","I","d",":"," "]

/*: "errCode" :*/
fileprivate let kFromMakeMsg:[UInt8] = [0xcf,0xdc,0xdc,0xad,0xd9,0xce,0xcf]

fileprivate func centerTouch(collect num: UInt8) -> UInt8 {
    let value = Int(num) - 106
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "custom_kickout_message" :*/
fileprivate let k_toMPath:String = "custosend"
fileprivate let kViewMsg:String = "cell image make classout_mes"
fileprivate let k_infoMakeFormat:String = "sagseat"

/*: "closeRoom" :*/
fileprivate let kFromFormat:String = "CLOSE"
fileprivate let k_viewMsg:[Character] = ["R","o","o","m"]

/*: "adminCloseRoom" :*/
fileprivate let kSharedUrl:[Character] = ["a","d","m","i","n","C","l"]
fileprivate let k_backgroundFormat:[Character] = ["o","s","e","R","o","o","m"]

/*: "The room is closed, please go to another room" :*/
fileprivate let kCoverKey:[UInt8] = [0x7e,0x42,0x4f,0xa,0x58,0x45,0x45,0x47,0xa,0x43,0x59,0xa,0x49,0x46,0x45,0x59,0x4f,0x4e,0x6,0xa,0x5a,0x46,0x4f,0x4b,0x59,0x4f,0xa,0x4d,0x45,0xa,0x5e,0x45,0xa,0x4b,0x44,0x45,0x5e,0x42,0x4f,0x58,0xa,0x58,0x45,0x45,0x47]

private func mpData(main num: UInt8) -> UInt8 {
    return num ^ 42
}

/*: "OK" :*/
fileprivate let k_showData:String = "Osystem"

/*: "You were kicked out of the room" :*/
fileprivate let k_whiteMessage:[UInt8] = [0x83,0x99,0x9f,0x4a,0xa1,0x8f,0x9c,0x8f,0x4a,0x95,0x93,0x8d,0x95,0x8f,0x8e,0x4a,0x99,0x9f,0x9e,0x4a,0x99,0x90,0x4a,0x9e,0x92,0x8f,0x4a,0x9c,0x99,0x99,0x97]

fileprivate func addView(layer num: UInt8) -> UInt8 {
    let value = Int(num) - 42
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "VOICEROOM：进房失败，roomID:  :*/
fileprivate let k_cellId:String = "VOICshow"
fileprivate let k_labUrl:String = "OOM：进height self to"
fileprivate let kTeamMsg:[Character] = ["r","o","o","m","I","D",":"," "]

/*: , error:  :*/
fileprivate let kTopLayerText:String = "equal "
fileprivate let k_byPath:String = "extension conversation as cornererror: "

/*: , reason:  :*/
fileprivate let kIncreaseMsg:String = "check margin progress back var, reaso"
fileprivate let k_levelRangeModeStr:String = "current submit inn: "

/*: , extendedData:  :*/
fileprivate let k_cookieMsg:String = "male angle text restore touch, exten"
fileprivate let k_arcViewName:[Character] = ["d","e","d","D","a","t","a",":"," "]

/*: "VOICEROOM: 当前用户推流失败，roomID:  :*/
fileprivate let kTextMsg:String = "VOICEmanager"
fileprivate let k_atTextListPath:String = "当\u{524d}用户"
fileprivate let k_colorMsg:String = "oomIDeffect center if if"
fileprivate let kMakeName:[Character] = [":"," "]

/*: , errorCode:  :*/
fileprivate let k_buttonDomainPath:String = ", ermanager app"

/*: , streamID:  :*/
fileprivate let k_userActualKey:[Character] = [","," ","s","t","r","e","a","m"]
fileprivate let k_labelRowStr:[Character] = ["I","D",":"," "]

/*: "An error occurred, please try again" :*/
fileprivate let k_playName:[UInt8] = [0xb3,0xe0,0x92,0xd7,0xe4,0xe4,0xe1,0xe4,0x92,0xe1,0xd5,0xd5,0xe7,0xe4,0xe4,0xd7,0xd6,0x9e,0x92,0xe2,0xde,0xd7,0xd3,0xe5,0xd7,0x92,0xe6,0xe4,0xeb,0x92,0xd3,0xd9,0xd3,0xdb,0xe0]

fileprivate func resumeTouch(to num: UInt8) -> UInt8 {
    let value = Int(num) - 114
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "VOICEROOM: 拉流失败，roomID:  :*/
fileprivate let k_directionKey:String = "VOICshow"
fileprivate let k_localData:[Character] = ["R","O","O","M",":"," ","拉","\u{6d41}"]
fileprivate let kImageToData:String = "view guard self path失败，ro"

/*: "VOICEROOM: 信令解析失败，command:  :*/
fileprivate let kBubbleKey:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","信","\u{4ee4}","\u{89e3}","析"]
fileprivate let k_pathValue:[Character] = ["失","败","，","c","o","m","m","a","n","d",":"," "]

/*: , roomID:  :*/
fileprivate let k_statusPath:[Character] = [","," ","r","o","o","m","I","D",":"," "]

/*: "data" :*/
fileprivate let k_modelUrl:[Character] = ["d","a","t","a"]

/*: "position" :*/
fileprivate let k_groupName:String = "positionumber"

/*: "xxxx--  :*/
fileprivate let kToArrayName:String = "xxxx--content the info gift"
fileprivate let k_languageUrl:String = " "

/*: "opType" :*/
fileprivate let k_pagePath:String = "opTypeindex make add"

/*: "party:changeMike" :*/
fileprivate let kInfoNameValue:[Character] = ["p","a","r","t","y",":","c","h","a","n","g","e","M","i","k","e"]

/*: "type" :*/
fileprivate let k_appTitle:String = "tbringe"

/*: "toPosition" :*/
fileprivate let k_logId:[Character] = ["t","o","P","o","s","i"]
fileprivate let k_dataGiftId:String = "tiored"

/*: "party:adminChangeMike" :*/
fileprivate let k_strengthValue:[Character] = ["p","a","r","t","y",":","a","d","m","i","n","C","h","a"]
fileprivate let kDestroyData:[Character] = ["n","g","e","M","i","k","e"]

/*: "You were kicked off the seat" :*/
fileprivate let k_atMsg:String = "You wegift clear string"
fileprivate let kEveryData:String = "cked frame cell make"
fileprivate let kAppMessage:String = "off thvalue tool value"

/*: "party:changeCoin" :*/
fileprivate let k_makeFormat:[Character] = ["p","a","r","t","y",":","c","h","a","n","g","e","C","o","i","n"]

/*: "party:closeStream" :*/
fileprivate let kColorMsg:String = "PART"
fileprivate let k_userMsg:String = "size click info instanceseStre"
fileprivate let kLayerMsg:String = "aframe"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CoverManager.swift
//  Adjust
//
//  Created by DouXiu on 2024/9/12.
//

//: import ZegoExpressEngine
import ZegoExpressEngine

/// 语聊房状态变更通知
//: public let PARTY_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "PARTY_STATUS_CHANGE_NOTIFICATION")
public let data_contentName = NSNotification.Name(rawValue: String(bytes: kFrameStartStr.map{socialUnit(quit: $0)}, encoding: .utf8)!)
/// 麦位数
//: public let MIKE_SEAT_COUNT = 8
public let data_ofAddHeightValue = 8

/// 语聊房角色
//: enum VoiceRoomRole: Int {
enum LocationMagnitude: Int {
    //: case host = 1
    case host = 1 // 房主
    //: case audience = 2
    case audience = 2 // 观众
    //: case manager = 4
    case manager = 4 // 管理员
}

//: class TalkingVoiceRoomManager: NSObject {
class CoverManager: NSObject {
    /// 语聊房模型
    //: private(set) var partyModel = TalkingVoiceRoomModel()
    private(set) var partyModel = ResumeCancelMeasurable()
    /// 是否正在语聊房中（状态变更时发送通知）
    //: private(set) var isParty: Bool = false {
    private(set) var isParty: Bool = false {
        //: didSet {
        didSet {
            //: NotificationCenter.default.post(name: PARTY_STATUS_CHANGE_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: data_contentName, object: self)
        }
    }

    /// 是否小窗口模式
    //: private(set) var isSmallMode: Bool = false
    private(set) var isSmallMode: Bool = false
    /// singleton
    //: private static var _instance: TalkingVoiceRoomManager?
    private static var _instance: CoverManager?
    // 语聊房控制器
    //: private var voiceRoomVC: TalkingVoiceRoomViewController?
    private var voiceRoomVC: PanoramicViewController?
    /// 麦位信息字典 [麦位 0~7：麦位模型]
    //: private var mikePositionDict = [Int: TalkingMikeListItemModel]()
    private var mikePositionDict = [Int: ItemMeasurable]()
    //: private let mplock = NSLock()
    private let mplock = NSLock()
    /// 正在进房请求中（防止多次调用）
    //: private var isEnterRoomReq = false
    private var isEnterRoomReq = false
    /// 麦克风操作请求中（防止多次调用）
    //: private var isMicReq = false
    private var isMicReq = false

    //: override init() {
    override init() {
        //: super.init()
        super.init()
        //: self.mikePositionDict = initMikePosition()
        self.mikePositionDict = locationAction()
    }

    //: class func shared() -> TalkingVoiceRoomManager {
    class func byRest() -> CoverManager {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingVoiceRoomManager()
            _instance = CoverManager()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func dataAddImage() {
        //: if TalkingVoiceRoomManager._instance != nil {
        if CoverManager._instance != nil {
            //: TalkingVoiceRoomManager._instance = nil
            CoverManager._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 语聊房小窗口悬浮窗
    //: private lazy var miniView: TalkingVoiceRoomMiniView = {
    private lazy var miniView: TitleMakeMiniView = {
        //: let mini = TalkingVoiceRoomMiniView.buildLiveMiniView()
        let mini = TitleMakeMiniView.valueModel()
        //: mini.isHidden = true
        mini.isHidden = true
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.voiceRoom_goback()
            self.ofColorGoback()
        }
        //: return mini
        return mini
        //: }()
    }()
}

// MARK: - 操作麦位请求

//: extension TalkingVoiceRoomManager {
extension CoverManager {
    /// 房主/观众 麦位操作请求
    /// - Parameters:
    ///   - type: 1 上麦，2 下麦，3 切麦，4 开麦，5 闭麦
    ///   - position: 麦位
    ///   - toPosition: 目标麦位（切麦传值）
    //: func voiceRoom_reqChangeMike(type: Int, position: Int, toPosition: Int? = nil) {
    func optionMatch(type: Int, position: Int, toPosition: Int? = nil) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_changeMike(roomId: partyModel.roomId,
        EnableReqTool.sourceCellPoint(roomId: partyModel.roomId,
                                                //: uid: Int(DistanceAppManager.share.loginUid) ?? 0,
                                                uid: Int(DistanceAppManager.share.loginUid) ?? 0,
                                                //: type: type,
                                                type: type,
                                                //: position: position,
                                                position: position,
                                                //: toPos: toPosition)
                                                toPos: toPosition)
        //: { succeed, result, errorModel in
        { succeed, result, errorModel in
            //: self.isMicReq = false
            self.isMicReq = false
            //: guard succeed else {
            guard succeed else { // 失败提示
                //: if errorModel?.errorCode == 20001 {
                if errorModel?.errorCode == 20001 { // 已在麦位，开始推流，刷新UI
                    //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    //: let streamID = self.voiceRoom_getStreamID()
                    let streamID = self.reconcile()
                    //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                    ZegoExpressEngine.shared().startPublishingStream(streamID)
                    //: guard let dict = result as? [String: Any] else { return }
                    guard let dict = result as? [String: Any] else { return }
                    //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: dict) else { return }
                    guard let mikeModel = ItemMeasurable.deserialize(from: dict) else { return }
                    //: self.updateMikePosition(position: position, model: mikeModel)
                    self.visualImageStyle(position: position, model: mikeModel)
                }
                //: return
                return
            }

            // 成功后只做逻辑处理，在接收信令处统一刷新UI
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                //: let streamID = self.voiceRoom_getStreamID()
                let streamID = self.reconcile()
                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                ZegoExpressEngine.shared().startPublishingStream(streamID)

            //: case 2:
            case 2: // 下麦成功
                //: ZegoExpressEngine.shared().stopPublishingStream()
                ZegoExpressEngine.shared().stopPublishingStream()

            //: case 3:
            case 3: // 切麦成功
                //: break
                break

            //: case 4:
            case 4: // 开麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }
        }
    }

    /// 房主对他人操作请求
    /// - Parameters:
    ///   - uid: 用户Id
    ///   - type: 1 锁麦，2 解锁麦，3 踢下麦，4 开麦，5 闭麦
    ///   - position: 麦位
    //: func voiceRoom_reqAdminChangeMike(uid: Int, type: Int, position: Int) {
    func drawRein(uid: Int, type: Int, position: Int) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_adminChangeMike(roomId: partyModel.roomId,
        EnableReqTool.titlePost(roomId: partyModel.roomId,
                                          //: uid: uid,
                                          uid: uid,
                                          //: type: type,
                                          type: type,
                                          //: position: position)
                                          position: position)
        //: { _, _, _ in
        { _, _, _ in
            //: self.isMicReq = false
            self.isMicReq = false
        }
    }

    /// 获取推流Id
    //: private func voiceRoom_getStreamID() -> String {
    private func reconcile() -> String {
        //: return "p-\(DistanceAppManager.share.loginUid)-\(partyModel.roomId)"
        return "p-\(DistanceAppManager.share.loginUid)-\(partyModel.roomId)"
    }
}

// MARK: - 权限检测，开启/进入/切换 语聊房

//: extension TalkingVoiceRoomManager {
extension CoverManager {
    /// 检测状态并进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func voiceRoom_checkAndTurnOn(roomId: String? = nil) {
    func equalSmart(roomId: String? = nil) {
        //: guard isParty == false else { return }
        guard isParty == false else { return }
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true

        // 1. 权限检测
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        ToolThen.cellTact(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.arcInfo(showMsg: main_intimateName)
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: return
                return
            }

            // 2. 接口是否禁播检测
            //: TalkingVoiceRoomReqTool.req_voiceRoomCheck(roomId: roomId) { succeed, result, errorModel in
            EnableReqTool.showContent(roomId: roomId) { succeed, result, errorModel in
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: guard succeed else {
                guard succeed else {
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? [String: Any] {
                        if let result = result as? [String: Any] {
                            //: AddReactiveCompatible.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            AddReactiveCompatible.share.pathAcross(urlStr: result[(String(kPriorityText) + String(kRequestModelEditFormat))] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.arcInfo(showMsg: errorModel!.errorMsg)
                    }
                    //: return
                    return
                }

                // 3. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isSpace == false else {
                guard ListSocketManager.shared.isSpace == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.arcInfo(showMsg: kTopReplaceHeightData)
                    //: return
                    return
                }

                // 4. 校验成功，解析模型
                //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
                guard let model = ResumeCancelMeasurable.deserialize(from: result as? Dictionary) else {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 进房模型解析失败。")
                    UploadDisplayThen.makeSelected(msg: (String(kColorMessage) + kMessageName))
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.arcInfo(showMsg: (String(k_gestureUrl.suffix(4)) + "y ro" + String(k_actionId.suffix(6)) + "eptio" + k_cellWithUrl.replacingOccurrences(of: "search", with: "n")).localized)
                    //: return
                    return
                }
                //: self.partyModel = model
                self.partyModel = model

                // 5. 创建语聊房UI，同时调用sdk创建房间
                //: self.voiceRoomVC = TalkingVoiceRoomViewController()
                self.voiceRoomVC = PanoramicViewController()
                //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
                self.put()?.pushViewController(self.voiceRoomVC!, animated: true)
                //: self.miniView.refreshMiniView()
                self.miniView.sinceShared()
                //: self.miniView.isHidden = true
                self.miniView.isHidden = true
                //: self.voiceRoom_create()
                self.modeUser()
                //: UIApplication.shared.isIdleTimerDisabled = true
                UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
                //: self.isParty = true
                self.isParty = true

                // 6. 埋点
                //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(DistanceAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
                kSoundName.messUp(eventID: "\(mainListValue)_\(DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.male.rawValue ? String(bytes: kLeadingStr.map{$0^175}, encoding: .utf8)! : (String(kTailPath)))")

                // 7. 初始化弹幕群聊
                //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                WithReactiveCompatible.saveByMentalAct(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                    //: if succeed == false {
                    if succeed == false {
                        //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊进房失败。groupId: \(self.partyModel.chatGroupId).")
                        UploadDisplayThen.makeSelected(msg: (k_equalData.replacingOccurrences(of: "receive", with: "O") + "OOM:" + String(k_makeBySectionData) + String(k_frameKey.suffix(7))) + "\(self.partyModel.chatGroupId).")
                        //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                        self.arcInfo(showMsg: (String(k_gestureUrl.suffix(4)) + "y ro" + String(k_actionId.suffix(6)) + "eptio" + k_cellWithUrl.replacingOccurrences(of: "search", with: "n")).localized)
                        //: self.voiceRoom_releaseAllResource()
                        self.voiceOverShared()
                    }
                }
            }
        }
    }

    /// 切换语聊房
    /// - Parameters:
    ///   - roomId: 当前进入房间Id
    ///   - beforeRoomId: 上一房间Id
    //: func voiceRoom_switch(roomId: String, beforeRoomId: String) {
    func compartmentTop(roomId: String, beforeRoomId: String) {
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true
        // 1. 调用切房接口
        //: TalkingVoiceRoomReqTool.req_voiceRoomSwitch(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
        EnableReqTool.panoramicViewWrite(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
            //: self.isEnterRoomReq = false
            self.isEnterRoomReq = false
            //: guard succeed else { return }
            guard succeed else { return }
            // 2. 释放上个房间资源 (使用partyModel旧值)
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.groupEnablePan()

            // 3. 主播切房，需要关闭底部tabbar动效 (使用partyModel旧值)
            //: if String(self.partyModel.streamerInfo.uid) == DistanceAppManager.share.loginUid {
            if String(self.partyModel.streamerInfo.uid) == DistanceAppManager.share.loginUid {
                //: self.isParty = false
                self.isParty = false
            }

            // 4. 校验成功，解析模型（给partyModel重新赋值）
            //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
            guard let model = ResumeCancelMeasurable.deserialize(from: result as? Dictionary) else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM: 切房模型解析失败。")
                UploadDisplayThen.makeSelected(msg: (kSexFailureId.replacingOccurrences(of: "error", with: "E") + kLeadingName.replacingOccurrences(of: "equal", with: "切") + "解析失败。"))
                //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                self.arcInfo(showMsg: (String(k_gestureUrl.suffix(4)) + "y ro" + String(k_actionId.suffix(6)) + "eptio" + k_cellWithUrl.replacingOccurrences(of: "search", with: "n")).localized)
                //: return
                return
            }
            //: self.partyModel = model
            self.partyModel = model

            // 5. 创建语聊房UI，同时调用sdk切房
            //: self.voiceRoomVC = TalkingVoiceRoomViewController()
            self.voiceRoomVC = PanoramicViewController()
            //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
            self.put()?.pushViewController(self.voiceRoomVC!, animated: true)
            //: self.miniView.refreshMiniView()
            self.miniView.sinceShared()
            //: self.miniView.isHidden = true
            self.miniView.isHidden = true
            //: self.voiceRoom_switch(beforeRoomId: beforeRoomId)
            self.replyPicture(beforeRoomId: beforeRoomId)
            //: self.isParty = true
            self.isParty = true
            //: self.isSmallMode = false
            self.isSmallMode = false

            // 6. 埋点
            //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(DistanceAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
            kSoundName.messUp(eventID: "\(mainListValue)_\(DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.male.rawValue ? String(bytes: kLeadingStr.map{$0^175}, encoding: .utf8)! : (String(kTailPath)))")

            // 7. 初始化弹幕群聊
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
            WithReactiveCompatible.saveByMentalAct(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                //: if succeed == false {
                if succeed == false {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊切房失败。groupId: \(self.partyModel.chatGroupId).")
                    UploadDisplayThen.makeSelected(msg: (kAtPath.replacingOccurrences(of: "make", with: "I") + "OOM:" + k_frameData + String(k_wantMessage)) + "\(self.partyModel.chatGroupId).")
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.arcInfo(showMsg: (String(k_gestureUrl.suffix(4)) + "y ro" + String(k_actionId.suffix(6)) + "eptio" + k_cellWithUrl.replacingOccurrences(of: "search", with: "n")).localized)
                    //: self.voiceRoom_releaseAllResource()
                    self.voiceOverShared()
                }
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingVoiceRoomManager {
extension CoverManager {
    /// 最小化语聊房
    //: func voiceRoom_mini() {
    func mix() {
        //: isSmallMode = true
        isSmallMode = true
        //: miniView.isHidden = false
        miniView.isHidden = false
        //: voiceRoomVC?.popCurrentViewController()
        voiceRoomVC?.controllerAnimated()
    }

    /// 返回语聊房
    //: func voiceRoom_goback() {
    func ofColorGoback() {
        //: isSmallMode = false
        isSmallMode = false
        //: miniView.isHidden = true
        miniView.isHidden = true
        //: guard voiceRoomVC != nil else { return }
        guard voiceRoomVC != nil else { return }
        //: guard let currentVC = currentViewController() else { return }
        guard let currentVC = selectStart() else { return }
        // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
        //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
        if currentVC.isKind(of: DistantViewController.self) {
            //: if var vcArr = currentVC.navigationController?.viewControllers {
            if var vcArr = currentVC.navigationController?.viewControllers {
                //: vcArr.removeLast()
                vcArr.removeLast()
                //: vcArr.append(voiceRoomVC!)
                vcArr.append(voiceRoomVC!)
                //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                currentVC.navigationController?.setViewControllers(vcArr, animated: true)
            }
            //: } else {
        } else {
            //: currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
            currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
        }
    }

    /// 销毁语聊房所有资源
    //: func voiceRoom_releaseAllResource() {
    func voiceOverShared() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isParty = false
            self.isParty = false // 有通知监听，需置为false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: self.miniView.removeFromSuperview()
            self.miniView.removeFromSuperview() // 移除小窗口
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.groupEnablePan() // 将语聊房视图从栈中移除并释放资源
            //: if self.partyModel.position >= 0 {
            if self.partyModel.position >= 0 { // 退房下麦
                //: self.voiceRoom_reqChangeMike(type: 2, position: self.partyModel.position)
                self.optionMatch(type: 2, position: self.partyModel.position)
            }
            //: ZegoExpressEngine.shared().logoutRoom()
            ZegoExpressEngine.shared().logoutRoom() // 退出房间
            //: ZegoExpressEngine.destroy(nil)
            ZegoExpressEngine.destroy(nil) // 销毁引擎
            //: TalkingVoiceRoomManager.destroy()
            CoverManager.dataAddImage() // 销毁当前单例
        }
    }
}

// MARK: - Private Event

//: extension TalkingVoiceRoomManager {
extension CoverManager {
    /// 创建语聊房
    //: private func voiceRoom_create() {
    private func modeUser() {
        // 创建引擎
        //: let profile = ZegoEngineProfile()
        let profile = ZegoEngineProfile()
        //: profile.appID = UserTextMacroDefine.getVoiceRoomAppId()
        profile.appID = UserTextMacroDefine.voiceObject()
        //: profile.scenario = .standardChatroom
        profile.scenario = .standardChatroom
        //: ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        // 进阶配置，进入房间
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: let user = ZegoUser(userID: DistanceAppManager.share.loginUid)
        let user = ZegoUser(userID: DistanceAppManager.share.loginUid)
        //: ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
        ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
    }

    /// 切换语聊房
    /// - Parameter beforeRoomId: 上一房间Id
    //: private func voiceRoom_switch(beforeRoomId: String) {
    private func replyPicture(beforeRoomId: String) {
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
        ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
    }
}

// MARK: - ZegoEventHandler

//: extension TalkingVoiceRoomManager: ZegoEventHandler {
extension CoverManager: ZegoEventHandler {
    // MARK: - 进房/切房 监听回调

    /// 进房/切房 回调
    //: func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
    func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: if errorCode == 0, reason == .logined {
        if errorCode == 0, reason == .logined { // 进房成功
            // 启动声浪等级监控
            //: ZegoExpressEngine.shared().startSoundLevelMonitor()
            ZegoExpressEngine.shared().startSoundLevelMonitor()
            // 开启音量稳定控制
            //: ZegoExpressEngine.shared().enableAEC(true)
            ZegoExpressEngine.shared().enableAEC(true)
            // 获取嘉宾列表接口
            //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
            EnableReqTool.purview(roomId: partyModel.roomId) { list in
                // 保存麦位列表信息
                //: for (key, value) in list.enumerated() {
                for (key, value) in list.enumerated() {
                    //: self.modifyMikePosition(key: key, model: value)
                    self.res(key: key, model: value)
                }
                // 刷新麦位列表UI
                //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                self.voiceRoomVC?.mikePositionView.afterExamine()
                // 房主首次创建房间，主动上麦
                //: if self.partyModel.userRole == VoiceRoomRole.host.rawValue, self.partyModel.isCreate == true {
                if self.partyModel.userRole == LocationMagnitude.host.rawValue, self.partyModel.isCreate == true {
                    //: self.voiceRoom_reqChangeMike(type: 1, position: 0)
                    self.optionMatch(type: 1, position: 0)
                }
            }

            //: } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
        } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
            //: if errorCode == 0, reason == .reconnected {
            if errorCode == 0, reason == .reconnected { // 重连成功
                //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
                EnableReqTool.purview(roomId: partyModel.roomId) { list in
                    // 更新麦位列表信息
                    //: for (key, value) in list.enumerated() {
                    for (key, value) in list.enumerated() {
                        //: self.modifyMikePosition(key: key, model: value)
                        self.res(key: key, model: value)
                        //: if String(value.uid) == DistanceAppManager.share.loginUid {
                        if String(value.uid) == DistanceAppManager.share.loginUid {
                            //: if value.mikeStatus == 1 {
                            if value.mikeStatus == 1 { // 房主闭麦
                                //: ZegoExpressEngine.shared().stopPublishingStream()
                                ZegoExpressEngine.shared().stopPublishingStream()
                                //: } else {
                            } else {
                                //: let streamID = self.voiceRoom_getStreamID()
                                let streamID = self.reconcile()
                                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                                ZegoExpressEngine.shared().startPublishingStream(streamID)
                                //: let isMute = value.mikeStatus == 0 ? true : false
                                let isMute = value.mikeStatus == 0 ? true : false
                                //: ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                                ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                            }
                        }
                    }
                    // 刷新麦位列表UI
                    //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                    self.voiceRoomVC?.mikePositionView.afterExamine()
                }
                //: } else if errorCode != 0 {
            } else if errorCode != 0 {
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kSoundName.releaseStr(eventID: kKeyMessageValue, parameterStr: [String(bytes: kFromMakeMsg.map{centerTouch(collect: $0)}, encoding: .utf8)!: String(errorCode)])
            }

            //: } else {
        } else { // 进房失败
            //: if errorCode == 1002055 || errorCode == 1002050 {
            if errorCode == 1_002_055 || errorCode == 1_002_050 { // 用户退出 || 被踢出房间
                //: let json = JSON(extendedData)
                let json = JSON(extendedData)
                //: let value = json["custom_kickout_message"].stringValue
                let value = json[(k_toMPath.replacingOccurrences(of: "send", with: "m") + "_kick" + String(kViewMsg.suffix(7)) + k_infoMakeFormat.replacingOccurrences(of: "seat", with: "e"))].stringValue
                //: if value == "closeRoom" || value == "adminCloseRoom" { // 关闭房间
                if value == (kFromFormat.lowercased() + String(k_viewMsg)) || value == (String(kSharedUrl) + String(k_backgroundFormat)) { // 关闭房间
                    //: voiceRoom_releaseAllResource()
                    voiceOverShared()
                    //: let config = ShowAlertConfig()
                    let config = VideoAlertConfig()
                    //: config.alignment = .center
                    config.alignment = .center
                    //: TalkingAlertShow.customAlert(message: "The room is closed, please go to another room".localized, rightBtnTitle: "OK".localized, rightBlock: {
                    FrameHiddenReactiveCompatible.systemToConfig(message: String(bytes: kCoverKey.map{mpData(main: $0)}, encoding: .utf8)!.localized, rightBtnTitle: "OK".localized, rightBlock: {
                        // 跳转到首页tab-party
                        //: NotificationCenter.default.post(name: SWITCH_TABBAR_HOME_PARTY_NOTIFICATION, object: nil, userInfo: nil)
                        NotificationCenter.default.post(name: show_clickMaxValue, object: nil, userInfo: nil)
                        //: }, config: config)
                    }, config: config)

                    //: } else {
                } else { // 被踢出房间
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked out of the room".localized)
                    arcInfo(showMsg: String(bytes: k_whiteMessage.map{addView(layer: $0)}, encoding: .utf8)!.localized)
                }

                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM：进房失败，roomID: \(roomID), error: \(errorCode), reason: \(reason), extendedData: \(extendedData).")
                UploadDisplayThen.makeSelected(msg: (k_cellId.replacingOccurrences(of: "show", with: "ER") + String(k_labUrl.prefix(5)) + "房失败，" + String(kTeamMsg)) + "\(roomID)" + (kTopLayerText.replacingOccurrences(of: "equal", with: ",") + String(k_byPath.suffix(7))) + "\(errorCode)" + (String(kIncreaseMsg.suffix(7)) + String(k_levelRangeModeStr.suffix(3))) + "\(reason)" + (String(k_cookieMsg.suffix(7)) + String(k_arcViewName)) + "\(extendedData).")
                //: func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                arcInfo(showMsg: (String(k_gestureUrl.suffix(4)) + "y ro" + String(k_actionId.suffix(6)) + "eptio" + k_cellWithUrl.replacingOccurrences(of: "search", with: "n")).localized)
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                kSoundName.releaseStr(eventID: kKeyMessageValue, parameterStr: [String(bytes: kFromMakeMsg.map{centerTouch(collect: $0)}, encoding: .utf8)!: String(errorCode)])
            }
            //: voiceRoom_releaseAllResource()
            voiceOverShared()
        }
    }

    // MARK: - 推流回调

    /// 当前用户推流状态回调
    //: func onPublisherStateUpdate(_ state: ZegoPublisherState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPublisherStateUpdate(_: ZegoPublisherState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 || errorCode == 1003020 || errorCode == 1002055 else {
        guard errorCode == 0 || errorCode == 1_003_020 || errorCode == 1_002_055 else { // 推流失败，用户下麦
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 当前用户推流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            UploadDisplayThen.makeSelected(msg: (kTextMsg.replacingOccurrences(of: "manager", with: "R") + "OOM: " + k_atTextListPath + "\u{63a8}\u{6d41}失败，r" + String(k_colorMsg.prefix(5)) + String(kMakeName)) + "\(partyModel.roomId)" + (String(k_buttonDomainPath.prefix(4)) + "rorCode: ") + "\(errorCode)" + (String(k_userActualKey) + String(k_labelRowStr)) + "\(streamID).")
            //: func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
            arcInfo(showMsg: String(bytes: k_playName.map{resumeTouch(to: $0)}, encoding: .utf8)!.localized)
            //: let dict = getAllMikePositions()
            let dict = each()
            //: for (position, model) in dict where String(model.uid) == DistanceAppManager.share.loginUid {
            for (position, model) in dict where String(model.uid) == DistanceAppManager.share.loginUid {
                //: voiceRoom_reqChangeMike(type: 2, position: position)
                optionMatch(type: 2, position: position)
                //: resetMikePosition(position: position, model: model)
                labModel(position: position, model: model)
            }
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                voiceOverShared()
            }
            //: return
            return
        }
    }

    // MARK: - 拉流回调

    /// 当前用户拉流状态回调
    //: func onPlayerStateUpdate(_ state: ZegoPlayerState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPlayerStateUpdate(_: ZegoPlayerState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 else {
        guard errorCode == 0 else { // 拉流失败
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 拉流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            UploadDisplayThen.makeSelected(msg: (k_directionKey.replacingOccurrences(of: "show", with: "E") + String(k_localData) + String(kImageToData.suffix(5)) + "omID: ") + "\(partyModel.roomId)" + (String(k_buttonDomainPath.prefix(4)) + "rorCode: ") + "\(errorCode)" + (String(k_userActualKey) + String(k_labelRowStr)) + "\(streamID).")
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                voiceOverShared()
            }

            //: return
            return
        }
    }

    // MARK: - 流更新回调

    /// 流更新回调
    //: func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData: [AnyHashable: Any]?, roomID: String) {
    func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData _: [AnyHashable: Any]?, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: switch updateType {
        switch updateType {
        //: case .add:
        case .add:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 开始拉流
                //: ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
            }

        //: case .delete:
        case .delete:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 停止拉流
                //: ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
            }

        //: @unknown default:
        @unknown default:
            //: fatalError()
            fatalError()
        }
    }

    // MARK: - 声浪等级监听回调

    /// 本地声浪等级
    //: func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
    func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
        //: guard Int(truncating: soundLevel) > 0 else { return }
        guard Int(truncating: soundLevel) > 0 else { return }
        //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: partyModel.position, level: soundLevel)
        voiceRoomVC?.mikePositionView.upCell(position: partyModel.position, level: soundLevel)
    }

    /// 远端声浪等级
    //: func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
    func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
        //: let dict = getAllMikePositions()
        let dict = each()
        //: for (streamID, level) in soundLevels {
        for (streamID, level) in soundLevels {
            //: let components = streamID.split(separator: "-").map(String.init)
            let components = streamID.split(separator: "-").map(String.init)
            //: guard components.count > 2 else { continue }
            guard components.count > 2 else { continue }
            //: if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
            if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
                //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: position, level: level)
                voiceRoomVC?.mikePositionView.upCell(position: position, level: level)
            }
        }
    }

    // MARK: - 信令

    /// 上麦、下麦、锁麦、解锁麦、切麦操作会收到后端下发信令
    //: func onIMRecvCustomCommand(_ command: String, from fromUser: ZegoUser, roomID: String) {
    func onIMRecvCustomCommand(_ command: String, from _: ZegoUser, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: guard let decodedStr = command.removingPercentEncoding else {
        guard let decodedStr = command.removingPercentEncoding else {
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 信令解析失败，command: \(command), roomID: \(roomID).")
            UploadDisplayThen.makeSelected(msg: (String(kBubbleKey) + String(k_pathValue)) + "\(command)" + (String(k_statusPath)) + "\(roomID).")
            //: return
            return
        }
        // 更新麦位信息，刷新列表UI
        //: let json = JSON(parseJSON: decodedStr)
        let json = JSON(parseJSON: decodedStr)
        //: let position = json["data"]["position"].intValue
        let position = json[(String(k_modelUrl))][(k_groupName.replacingOccurrences(of: "number", with: "n"))].intValue
        //: guard position < MIKE_SEAT_COUNT else { return }
        guard position < data_ofAddHeightValue else { return }
        //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: json["data"].dictionaryObject) else { return }
        guard let mikeModel = ItemMeasurable.deserialize(from: json[(String(k_modelUrl))].dictionaryObject) else { return }
        //: printLog(message: "xxxx-- \(json.rawValue)")
        printLog(message: (String(kToArrayName.prefix(6)) + k_languageUrl.capitalized) + "\(json.rawValue)")
        //: if json["opType"].stringValue == "party:changeMike" { // 房主/观众 麦位操作信令
        if json[(String(k_pagePath.prefix(6)))].stringValue == (String(kInfoNameValue)) { // 房主/观众 麦位操作信令
            //: let type = json["data"]["type"].intValue
            let type = json[(String(k_modelUrl))][(k_appTitle.replacingOccurrences(of: "bring", with: "yp"))].intValue
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: position, model: mikeModel)
                visualImageStyle(position: position, model: mikeModel)

            //: case 2:
            case 2: // 下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = nextLandmark(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    labModel(position: position, model: oldModel)
                }

            //: case 3:
            case 3: // 切麦
                //: resetMikePosition(position: position, model: mikeModel)
                labModel(position: position, model: mikeModel)
                //: let toPosition = json["data"]["toPosition"].intValue
                let toPosition = json[(String(k_modelUrl))][(String(k_logId) + k_dataGiftId.replacingOccurrences(of: "red", with: "n"))].intValue
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: toPosition, model: mikeModel)
                visualImageStyle(position: toPosition, model: mikeModel)

            //: case 4, 5:
            case 4, 5: // 开麦、闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                visualImageStyle(position: position, model: mikeModel)

            //: default:
            default:
                //: break
                break
            }
            // 刷新礼物面板头像
            //: if type == 1 || type == 2 || type == 3 {
            if type == 1 || type == 2 || type == 3 {
                //: voiceRoomVC?.bottomView.refreshGiftIcon()
                voiceRoomVC?.bottomView.allIcon()
            }

            //: } else if json["opType"].stringValue == "party:adminChangeMike" { // 房主对他人操作信令
        } else if json[(String(k_pagePath.prefix(6)))].stringValue == (String(k_strengthValue) + String(kDestroyData)) { // 房主对他人操作信令
            //: switch json["data"]["type"].intValue {
            switch json[(String(k_modelUrl))][(k_appTitle.replacingOccurrences(of: "bring", with: "yp"))].intValue {
            //: case 1, 2:
            case 1, 2: // 锁麦、解锁麦
                //: updateMikePosition(position: position, model: mikeModel)
                visualImageStyle(position: position, model: mikeModel)

            //: case 3:
            case 3: // 踢下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = nextLandmark(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    labModel(position: position, model: oldModel)
                    // 刷新礼物面板头像
                    //: voiceRoomVC?.bottomView.refreshGiftIcon()
                    voiceRoomVC?.bottomView.allIcon()
                    //: guard String(oldModel.uid) == DistanceAppManager.share.loginUid else { return }
                    guard String(oldModel.uid) == DistanceAppManager.share.loginUid else { return }
                    //: ZegoExpressEngine.shared().stopPublishingStream()
                    ZegoExpressEngine.shared().stopPublishingStream()
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked off the seat".localized)
                    arcInfo(showMsg: (String(k_atMsg.prefix(6)) + "re ki" + String(kEveryData.prefix(5)) + String(kAppMessage.prefix(6)) + "e seat").localized)
                }

            //: case 4:
            case 4: // 开麦
                //: updateMikePosition(position: position, model: mikeModel)
                visualImageStyle(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == DistanceAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == DistanceAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                visualImageStyle(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == DistanceAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == DistanceAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }

            //: } else if json["opType"].stringValue == "party:changeCoin" { // 更新用户麦位金币
        } else if json[(String(k_pagePath.prefix(6)))].stringValue == (String(k_makeFormat)) { // 更新用户麦位金币
            //: TalkingVoiceRoomManager.shared().getAllMikePositions().forEach { position, model in
            CoverManager.byRest().each().forEach { position, model in
                //: if model.uid == mikeModel.uid {
                if model.uid == mikeModel.uid {
                    //: model.coin = mikeModel.coin
                    model.coin = mikeModel.coin
                    //: updateMikePosition(position: position, model: model)
                    visualImageStyle(position: position, model: model)
                    //: return
                    return
                }
            }

            //: } else if json["opType"].stringValue == "party:closeStream" { // 推流失败
        } else if json[(String(k_pagePath.prefix(6)))].stringValue == (kColorMsg.lowercased() + "y:clo" + String(k_userMsg.suffix(6)) + kLayerMsg.replacingOccurrences(of: "frame", with: "m")) { // 推流失败
            //: ZegoExpressEngine.shared().stopPublishingStream()
            ZegoExpressEngine.shared().stopPublishingStream()
        }
    }
}

// MARK: - 信令处理

//: extension TalkingVoiceRoomManager {
extension CoverManager {
    /// 更新麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func updateMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func visualImageStyle(position: Int, model: ItemMeasurable) {
        //: modifyMikePosition(key: position, model: model)
        res(key: position, model: model)
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.textClick(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        responseStatusUid(uid: model.uid)
    }

    /// 重置麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func resetMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func labModel(position: Int, model: ItemMeasurable) {
        //: modifyMikePosition(key: position, model: TalkingMikeListItemModel())
        res(key: position, model: ItemMeasurable())
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.textClick(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        responseStatusUid(uid: model.uid)
    }

    /// 更新本人麦位、底部麦克风状态
    /// - Parameter uid: 用户Id
    //: private func updateBottomMicStatusIfCurrentUser(uid: Int) {
    private func responseStatusUid(uid: Int) {
        //: guard String(uid) == DistanceAppManager.share.loginUid else { return }
        guard String(uid) == DistanceAppManager.share.loginUid else { return }
        //: let dict = getAllMikePositions()
        let dict = each()
        //: if let position = dict.first(where: { $0.value.uid == uid })?.key {
        if let position = dict.first(where: { $0.value.uid == uid })?.key {
            //: partyModel.position = position
            partyModel.position = position
            //: } else {
        } else {
            //: partyModel.position = -1
            partyModel.position = -1
        }
        //: voiceRoomVC?.bottomView.refreshBottomMicStatus()
        voiceRoomVC?.bottomView.toOn()
    }
}

// MARK: - 麦位字典安全操作

//: extension TalkingVoiceRoomManager {
extension CoverManager {
    /// 获取初始化模型
    //: private func initMikePosition() -> [Int: TalkingMikeListItemModel] {
    private func locationAction() -> [Int: ItemMeasurable] {
        //: for index in 0 ..< MIKE_SEAT_COUNT {
        for index in 0 ..< data_ofAddHeightValue {
            //: modifyMikePosition(key: index, model: TalkingMikeListItemModel())
            res(key: index, model: ItemMeasurable())
        }
        //: return mikePositionDict
        return mikePositionDict
    }

    /// 添加/修改 麦位
    //: func modifyMikePosition(key: Int, model: TalkingMikeListItemModel) {
    func res(key: Int, model: ItemMeasurable) {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: mikePositionDict[key] = model
        mikePositionDict[key] = model
    }

    /// 获取麦位
    //: func getMikePosition(key: Int) -> TalkingMikeListItemModel? {
    func nextLandmark(key: Int) -> ItemMeasurable? {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict[key]
        return mikePositionDict[key]
    }

    /// 获取所有麦位
    //: func getAllMikePositions() -> [Int: TalkingMikeListItemModel] {
    func each() -> [Int: ItemMeasurable] {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict
        return mikePositionDict
    }
}
