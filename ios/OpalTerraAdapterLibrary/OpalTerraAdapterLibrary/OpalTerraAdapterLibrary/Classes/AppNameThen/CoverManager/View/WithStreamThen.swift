
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kResultFormat:[UInt8] = [0x7c,0x7b,0x7c,0x61,0x3d,0x76,0x7a,0x71,0x70,0x67,0x2f,0x3c,0x35,0x7d,0x74,0x66,0x35,0x7b,0x7a,0x61,0x35,0x77,0x70,0x70,0x7b,0x35,0x7c,0x78,0x65,0x79,0x70,0x78,0x70,0x7b,0x61,0x70,0x71]

private func titleDay(make num: UInt8) -> UInt8 {
    return num ^ 21
}

/*: "Followers" :*/
fileprivate let kSharePresentUrl:String = "Followerstask lab original"

/*: "Following" :*/
fileprivate let k_toTableUrl:[Character] = ["F","o","l","l","o","w"]
fileprivate let k_styleValue:String = "ING"

/*: "#E6E6E6" :*/
fileprivate let k_coverQuantityStr:[Character] = ["#","E","6","E","6","E"]
fileprivate let kValueActualPath:[Character] = ["6"]

/*: "btn_message_more" :*/
fileprivate let kSearchId:String = "female for info giftbtn_messa"
fileprivate let k_videoData:[Character] = ["g","e","_","m","o","r","e"]

/*: "UID:  :*/
fileprivate let kViewSucceedContentKey:[Character] = ["U","I","D",":"," "]

/*: "level_ :*/
fileprivate let k_statusMessage:[Character] = ["l"]
fileprivate let k_scaleId:String = "evel_me model app type"

/*: "Gift" :*/
fileprivate let kRefuseId:[UInt8] = [0x15,0x3b,0x34,0x26]

private func tapModelUser(to num: UInt8) -> UInt8 {
    return num ^ 82
}

/*: "Mike" :*/
fileprivate let k_selectedName:[UInt8] = [0x5d,0x79,0x7b,0x75]

fileprivate func objectStyle(pause num: UInt8) -> UInt8 {
    let value = Int(num) - 16
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Follow" :*/
fileprivate let k_disappearFormat:[UInt8] = [0x48,0x61,0x62,0x62,0x61,0x79]

private func selfImport(color num: UInt8) -> UInt8 {
    return num ^ 14
}

/*: "gift_voice_card" :*/
fileprivate let kCollectValue:String = "gift_success request string text if"
fileprivate let k_topPath:String = "ajumpd"

/*: "btn_party_voice_nor" :*/
fileprivate let k_sizeId:String = "btn_true cell name"
fileprivate let kModelMsg:String = "C"
fileprivate let k_firstValue:[Character] = ["e","_","n","o","r"]

/*: "btn_party_voice_pre" :*/
fileprivate let k_bottomStr:[Character] = ["b","t","n","_","p","a","r","t","y","_","v","o","i"]
fileprivate let kContainerTitle:[Character] = ["c","e","_","p","r","e"]

/*: "attentionUid" :*/
fileprivate let kViewTitle:[Character] = ["a","t","t","e","n","t","i","o","n","U","i","d"]

/*: "source" :*/
fileprivate let kPageMessage:String = "channelource"

/*: "Kick out" :*/
fileprivate let k_contentName:[Character] = ["K","i","c","k"," "]
fileprivate let k_keyFrameTitle:[Character] = ["o","u","t"]

/*: "Unmute" :*/
fileprivate let k_viewExampleText:[Character] = ["U","n","m","u","t","e"]

/*: "Mute" :*/
fileprivate let k_dataMessage:String = "app selfMute"

/*: "Kick off" :*/
fileprivate let kByContent:[Character] = ["K","i","c","k"," ","o","f"]
fileprivate let k_infoKey:[Character] = ["f"]

/*: "Block" :*/
fileprivate let k_equalId:[Character] = ["B","l","o","c","k"]

/*: "Report" :*/
fileprivate let k_inputPath:[Character] = ["R","e","p","o","r","t"]

/*: "#F6F6F6" :*/
fileprivate let kVideoSubmitPath:String = "#F6F6F6touch female let equal a"

/*: "Confirm to kick out the room?" :*/
fileprivate let kNameFormat:String = "Confiself add"
fileprivate let kAtUpgradeBottomTitle:String = " kick oufee value array in extension"
fileprivate let kKitId:[Character] = ["t"," ","t","h","e"," ","r","o","o","m","?"]

/*: "Cancel" :*/
fileprivate let k_selectedData:String = "pop true category button actionCancel"

/*: "Sure" :*/
fileprivate let kSucceedUserTitle:[Character] = ["S","u","r","e"]

/*: "Are you sure you want to cancel the mute?" :*/
fileprivate let kPlayName:[UInt8] = [0x52,0x61,0x76,0x33,0x6a,0x7c,0x66,0x33,0x60,0x66,0x61,0x76,0x33,0x6a,0x7c,0x66,0x33,0x64,0x72,0x7d,0x67,0x33,0x67,0x7c,0x33,0x70,0x72,0x7d,0x70,0x76,0x7f,0x33,0x67,0x7b,0x76,0x33,0x7e,0x66,0x67,0x76,0x2c]

private func managerName(leading num: UInt8) -> UInt8 {
    return num ^ 19
}

/*: "Confirm" :*/
fileprivate let k_acceptTitle:[Character] = ["C","o"]
fileprivate let k_giftText:String = "NFIRM"

/*: "The mute has been lifted~" :*/
fileprivate let k_attributeImageColorPath:[Character] = ["T","h","e"," ","m"]
fileprivate let kImageUrl:[Character] = ["u","t","e"," ","h","a","s"," ","b","e","e","n"," ","l","i","f","t","e","d","~"]

/*: "Confirm to kick off the seat?" :*/
fileprivate let kQueryContent:String = "to returnConf"
fileprivate let k_equalName:[Character] = ["c","k"," "]
fileprivate let kActionText:[Character] = ["o","f","f"," ","t","h","e"," ","s","e","a","t","?"]

/*: "OK" :*/
fileprivate let k_namePushKey:[Character] = ["O","K"]

/*: "Shielding Success" :*/
fileprivate let kMakeMessage:String = "row shared true letShie"
fileprivate let k_sharedPath:String = "Successsize hidden shared class jump"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  WithStreamThen.swift
//  AbroadTalking
//

//: import UIKit
import UIKit

//: @objc protocol TalkingVoiceRoomCardViewDelegate: NSObjectProtocol {
@objc protocol MakeObjectProtocol: NSObjectProtocol {
    // @
    //: func func__atUserClick(uid: String, nickname: String)
    func high(uid: String, nickname: String)

    //: func func__giftUserClick(uid: String)
    func signaling(uid: String)

    //: func func__muteUserMickClick(uid: String, mikeStatus: Int, position: Int)
    func itemTouch(uid: String, mikeStatus: Int, position: Int)

    //: func func__followUserSucess(uid: String)
    func videoGroupUid(uid: String)
}

//: class TalkingVoiceRoomCardView: UIView {
class WithStreamThen: UIView {
    //: var popView: TalkingPopView?
    var popView: DataPopView?
    //: var data =  [String: Any]()
    var data = [String: Any]()
    //: var type: LiveUserCardType?
    var type: MakeDefaultsSerializable?
    //: var uid = ""
    var uid = ""
    //: var liveUserId = String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid)
    var liveUserId = String(CoverManager.byRest().partyModel.streamerInfo.uid) // 主播ID

    //: var userModel = TalkingUserInfoModel.init()
    var userModel = GroupTransformable()

    //: open weak var delegate: TalkingVoiceRoomCardViewDelegate?
    open weak var delegate: MakeObjectProtocol?

    //: init(frame: CGRect, uid: String) {
    init(frame: CGRect, uid: String) {
        //: super.init(frame: frame)
        super.init(frame: frame)

        //: if uid == DistanceAppManager.share.loginUserMode.userID {
        if uid == DistanceAppManager.share.loginUserMode.userID {
            //: self.type = .user_me
            self.type = .user_me
            //: } else {
        } else {
            //: self.type = .user_other
            self.type = .user_other
        }
        //: self.uid = uid
        self.uid = uid
        //: setupSubviews()
        sexAdd()
        //: setupSubViewsConstraint()
        failFrom()
        //: reqUserCardInfo()
        overInfo()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kResultFormat.map{titleDay(make: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {}

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: return view
        return view
        //: }()
    }()

    //: lazy var uidLb: UILabel = {
    lazy var uidLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .fontChange(type: .Regular, fontSize: 12)
        label.font = .fontChange(type: .Regular, fontSize: 12)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.childMenu()
        //: return label
        return label
        //: }()
    }()

    //: lazy var topIcon: UIButton = {
    lazy var topIcon: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(iconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(snapTool), for: .touchUpInside)
        //: btn.layer.cornerRadius = 40
        btn.layer.cornerRadius = 40
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.layer.borderWidth = 2
        btn.layer.borderWidth = 2
        //: btn.layer.borderColor = UIColor.white.cgColor
        btn.layer.borderColor = UIColor.white.cgColor
        //: return btn
        return btn

        //: }()
    }()

    //: lazy var nameLb: UILabel = {
    lazy var nameLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .fontChange(type: .Semibold, fontSize: 15)
        label.font = .fontChange(type: .Semibold, fontSize: 15)
        //: label.textColor = UIColor.colorStatuteNameApp()
        label.textColor = UIColor.colorStatuteNameApp()
        //: return label
        return label
        //: }()
    }()

    //: lazy var levelIcon: UIImageView = {
    lazy var levelIcon: UIImageView = {
        //: let imag = UIImageView.init()
        let imag = UIImageView()
        //: imag.contentMode = .scaleAspectFill
        imag.contentMode = .scaleAspectFill
        //: return imag
        return imag
        //: }()
    }()

    //: lazy var signLb: UILabel = {
    lazy var signLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .fontChange(type: .Regular, fontSize: 13)
        label.font = .fontChange(type: .Regular, fontSize: 13)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.childMenu()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 2
        label.numberOfLines = 2
        //: return label
        return label
        //: }()
    }()

    //: private lazy var followersLab: UILabel = {
    private lazy var followersLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .childMenu()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Followers".localized
        lab.text = (String(kSharePresentUrl.prefix(9))).localized
        //: lab.font = UIFont.fontChange(type: .Semibold, fontSize: 12)
        lab.font = UIFont.fontChange(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followersNum: UILabel = {
    private lazy var followersNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .colorStatuteNameApp()
        lab.textColor = .colorStatuteNameApp()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.fontChange(type: .Semibold, fontSize: 15)
        lab.font = UIFont.fontChange(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingLab: UILabel = {
    private lazy var followingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .childMenu()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Following".localized
        lab.text = (String(k_toTableUrl) + k_styleValue.lowercased()).localized
        //: lab.font = UIFont.fontChange(type: .Semibold, fontSize: 12)
        lab.font = UIFont.fontChange(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingNum: UILabel = {
    private lazy var followingNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .colorStatuteNameApp()
        lab.textColor = .colorStatuteNameApp()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.fontChange(type: .Semibold, fontSize: 15)
        lab.font = UIFont.fontChange(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var lineView: UIView = {
    private lazy var lineView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.init(hex: "#E6E6E6")
        view.backgroundColor = UIColor(hex: (String(k_coverQuantityStr) + String(kValueActualPath)))
        //: return view
        return view
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .fill
        v.alignment = .fill
        //: v.distribution = .fillEqually
        v.distribution = .fillEqually
        //: return v
        return v
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.setImage(UIImage.submitGift(name: "btn_message_more"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(kSearchId.suffix(9)) + String(k_videoData))), for: .normal)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(residence), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var muteView: TalkingLiveRoomMuteView = {
    private lazy var muteView: EqualView = {
        //: let v = TalkingLiveRoomMuteView()
        let v = EqualView()
        //: v.viewType  = .voiceRoom
        v.viewType = .voiceRoom
        //: return v
        return v
        //: }()
    }()
}

//: extension TalkingVoiceRoomCardView {
extension WithStreamThen {
    //: func reqUserCardInfo() {
    func overInfo() {
        //: TalkingLiveManager.req__userCardInfo(uid: self.uid, streamerUid: liveUserId, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
        ValueThen.searchUse(uid: self.uid, streamerUid: liveUserId, roomId: CoverManager.byRest().partyModel.roomId) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }

            //: if let userModel = JSONDeserializer<TalkingUserInfoModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<GroupTransformable>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: self.userModel = userModel
                self.userModel = userModel
                //: self.setUserCardData()
                self.beautyGesture()
            }
        }
    }

    //: func setUserCardData() {
    func beautyGesture() {
        //: uidLb.text = "UID: \(self.userModel.uid)"
        uidLb.text = (String(kViewSucceedContentKey)) + "\(self.userModel.uid)"
        //: topIcon.setUrlImage(urlStr: self.userModel.headPic)
        topIcon.successOn(urlStr: self.userModel.headPic)
        //: nameLb.text = self.userModel.nickname
        nameLb.text = self.userModel.nickname
        //: levelIcon.image = UIImage.submitGift(name: "level_\(self.userModel.level)")
        levelIcon.image = UIImage.submitGift(name: (String(k_statusMessage) + String(k_scaleId.prefix(5))) + "\(self.userModel.level)")
        //: signLb.text = self.userModel.signature
        signLb.text = self.userModel.signature
        //: followersNum.text = self.userModel.attentionNum
        followersNum.text = self.userModel.attentionNum
        //: followingNum.text = self.userModel.fansNum
        followingNum.text = self.userModel.fansNum

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: setBottomView(sex: self.userModel.sex)
            gayness(sex: self.userModel.sex)
        }
    }

    //: func setBottomView(sex: String) {
    func gayness(sex _: String) {
        //: var dataSource = Array<String>()
        var dataSource = [String]()
        //: if self.userModel.isOnMike {
        if self.userModel.isOnMike {
            //: dataSource.append("Gift")
            dataSource.append(String(bytes: kRefuseId.map{tapModelUser(to: $0)}, encoding: .utf8)!)
            //: if liveUserId == DistanceAppManager.share.loginUserMode.userID && self.userModel.mikeStatus != 0  {
            if liveUserId == DistanceAppManager.share.loginUserMode.userID, self.userModel.mikeStatus != 0 { // 当前用户是主播非自己闭麦  闭麦/开麦 按钮
                //: dataSource.append("Mike")
                dataSource.append(String(bytes: k_selectedName.map{objectStyle(pause: $0)}, encoding: .utf8)!)
            }
        }
        //: dataSource.append("@")
        dataSource.append("@")

        //: if self.userModel.isAttention == false {
        if self.userModel.isAttention == false {
            //: dataSource.append("Follow")
            dataSource.append(String(bytes: k_disappearFormat.map{selfImport(color: $0)}, encoding: .utf8)!)
        }

        //: for i in 0 ..< dataSource.count {
        for i in 0 ..< dataSource.count {
            //: let btnStr = dataSource[i]
            let btnStr = dataSource[i]
            //: let btn = UIButton()
            let btn = UIButton()
            //: btn.accessibilityLabel = btnStr
            btn.accessibilityLabel = btnStr
            //: btn.setTitleColor(UIColor.appThemeColor(), for: UIControl.State.normal)
            btn.setTitleColor(UIColor.increase(), for: UIControl.State.normal)
            //: if btnStr == "Gift" {
            if btnStr == String(bytes: kRefuseId.map{tapModelUser(to: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.submitGift(name: "gift_voice_card"), for: .normal)
                btn.setImage(UIImage.submitGift(name: (String(kCollectValue.prefix(5)) + "voice_c" + k_topPath.replacingOccurrences(of: "jump", with: "r"))), for: .normal)
                //: }else if btnStr == "Mike"{
            } else if btnStr == String(bytes: k_selectedName.map{objectStyle(pause: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.submitGift(name: "btn_party_voice_nor"), for: .normal)
                btn.setImage(UIImage.submitGift(name: (String(k_sizeId.prefix(4)) + "party_voi" + kModelMsg.lowercased() + String(k_firstValue))), for: .normal)
                //: btn.setImage(UIImage.submitGift(name: "btn_party_voice_pre"), for: .selected)
                btn.setImage(UIImage.submitGift(name: (String(k_bottomStr) + String(kContainerTitle))), for: .selected)
                //: if self.userModel.mikeStatus != 2 {
                if self.userModel.mikeStatus != 2 {
                    //: btn.isSelected = true
                    btn.isSelected = true
                }
                //: }else{
            } else {
                //: btn.titleLabel?.font = UIFont.pingfangMediumFont(fontSize: 15)
                btn.titleLabel?.font = UIFont.tapShared(fontSize: 15)
                //: btn.setTitle(btnStr.localized, for: UIControl.State.normal)
                btn.setTitle(btnStr.localized, for: UIControl.State.normal)
            }
            //: btn.addTarget(self, action: #selector(itemTouch(sender:)), for: UIControl.Event.touchUpInside)
            btn.addTarget(self, action: #selector(doingBusinessAsItem(sender:)), for: UIControl.Event.touchUpInside)
            //: self.stackView.addArrangedSubview(btn)
            self.stackView.addArrangedSubview(btn)

            //: if i != 0 {
            if i != 0 {
                //: let lineView = UIView.init()
                let lineView = UIView()
                //: lineView.backgroundColor = UIColor.init(hex: "#E6E6E6")
                lineView.backgroundColor = UIColor(hex: (String(k_coverQuantityStr) + String(kValueActualPath)))
                //: btn.addSubview(lineView)
                btn.addSubview(lineView)
                //: lineView.snp.makeConstraints { make in
                lineView.snp.makeConstraints { make in
                    //: make.top.equalTo(10)
                    make.top.equalTo(10)
                    //: make.leading.equalToSuperview()
                    make.leading.equalToSuperview()
                    //: make.width.equalTo(1)
                    make.width.equalTo(1)
                    //: make.height.equalTo(24)
                    make.height.equalTo(24)
                }
            }
        }
    }

    //: @objc func itemTouch(sender: UIButton) {
    @objc func doingBusinessAsItem(sender: UIButton) {
        //: let accessLabel = sender.accessibilityLabel ?? ""
        let accessLabel = sender.accessibilityLabel ?? ""
        //: switch accessLabel {
        switch accessLabel {
        //: case "Gift":
        case String(bytes: kRefuseId.map{tapModelUser(to: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__giftUserClick(uid: self.userModel.uid)
            self.delegate?.signaling(uid: self.userModel.uid)
            //: self.dismiss()
            self.barTable()
        //: case "Mike":
        case String(bytes: k_selectedName.map{objectStyle(pause: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__muteUserMickClick(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            self.delegate?.itemTouch(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            //: self.dismiss()
            self.barTable()
        //: case "@":
        case "@":
            //: self.delegate?.func__atUserClick(uid: self.userModel.uid, nickname: self.userModel.nickname)
            self.delegate?.high(uid: self.userModel.uid, nickname: self.userModel.nickname)
            //: self.dismiss()
            self.barTable()
        //: case "Follow":
        case String(bytes: k_disappearFormat.map{selfImport(color: $0)}, encoding: .utf8)!:
            //: freshfollowUser(sender: sender)
            freshfollowSender(sender: sender)
        //: default: break
        default: break
        }
    }

    //: func freshfollowUser(sender: UIButton) {
    func freshfollowSender(sender: UIButton) {
        //: req_followUser(uid: self.userModel.uid) { succeed in
        buildCompletion(uid: self.userModel.uid) { succeed in
            //: if succeed {
            if succeed {
                //: self.followingNum.text = "\((Int(self.userModel.fansNum ) ?? 0) + 1)"
                self.followingNum.text = "\((Int(self.userModel.fansNum) ?? 0) + 1)"
                //: self.stackView.removeArrangedSubview(sender)
                self.stackView.removeArrangedSubview(sender)
                //: sender.isHidden = true
                sender.isHidden = true
                //: self.delegate?.func__followUserSucess(uid: self.userModel.uid)
                self.delegate?.videoGroupUid(uid: self.userModel.uid)
            }
        }
    }

    //: func req_followUser(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    func buildCompletion(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["attentionUid"] = uid
        dict[(String(kViewTitle))] = uid
        //: dict["source"] = "2"
        dict[(kPageMessage.replacingOccurrences(of: "channel", with: "s"))] = "2"
        //: PlayerRequestTool.req_atationTool(isAttention: true, params: dict) { succeed, result, errorModel in
        PlayerRequestTool.appPath(isAttention: true, params: dict) { succeed, _, _ in
            //: completion?(succeed)
            completion?(succeed)
        }
    }

    //: @objc private func iconBtnClick() {
    @objc private func snapTool() {
        //: AddReactiveCompatible.share.func__pushToUserDetailVC(uid: self.userModel.uid)
        AddReactiveCompatible.share.afterTo(uid: self.userModel.uid)
        //: self.dismiss()
        self.barTable()
    }
}

//: extension TalkingVoiceRoomCardView {
extension WithStreamThen {
    //: @objc func moreBtnClick() {
    @objc func residence() {
        //: var titleArray: [String] = []
        var titleArray: [String] = []
        /// 主播 & 用户在房间内
        //: if liveUserId == DistanceAppManager.share.loginUserMode.userID && self.userModel.isInRoom {
        if liveUserId == DistanceAppManager.share.loginUserMode.userID, self.userModel.isInRoom {
            //: titleArray.append("Kick out".localized)
            titleArray.append((String(k_contentName) + String(k_keyFrameTitle)).localized)
            //: if self.userModel.isPartyMute {
            if self.userModel.isPartyMute {
                //: titleArray.append("Unmute".localized)
                titleArray.append((String(k_viewExampleText)).localized)
                //: }else {
            } else {
                //: titleArray.append("Mute".localized)
                titleArray.append((String(k_dataMessage.suffix(4))).localized)
            }
            //: if self.userModel.isOnMike {
            if self.userModel.isOnMike {
                //: titleArray.append("Kick off".localized)
                titleArray.append((String(kByContent) + String(k_infoKey)).localized)
            }
            //: if !self.userModel.inMyBlackList {
            if !self.userModel.inMyBlackList {
                //: titleArray.append("Block".localized)
                titleArray.append((String(k_equalId)).localized)
            }
        }
        //: titleArray.append("Report".localized)
        titleArray.append((String(k_inputPath)).localized)

        //: Config.MenuCellConfig.menuCellHeight = 26
        Config.ValueContent.menuCellHeight = 26
        //: let dropMenu = DropDownMenuView.pullDropDrownMenu(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        let dropMenu = AddDataSource.following(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        //: dropMenu.menuBgColor = UIColor.init(hex: "#F6F6F6")!
        dropMenu.menuBgColor = UIColor(hex: (String(kVideoSubmitPath.prefix(7))))!
        //: dropMenu.menuDelegate = self
        dropMenu.menuDelegate = self
        //: dropMenu.menuStyle = .MenuCustStyle
        dropMenu.menuStyle = .MenuCustStyle
    }

    //: func show() {
    func placeWith() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = DataPopView(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: constFormalContent, height: user_recordMarginValue)
        //: popView?.initWithView(view: self)
        popView?.smart(view: self)
        //: popView?.showInView(view: self.currentViewController()?.view ?? UserTextMacroDefine.getWindow())
        popView?.academePosition(view: self.selectStart()?.view ?? UserTextMacroDefine.clearstoryNo())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func barTable() {
        //: popView?.dismissView()
        popView?.upView()
        //: popView = nil
        popView = nil
    }
}

//: extension TalkingVoiceRoomCardView: DropDownMenuViewDelegate {
extension WithStreamThen: WillSizeViewDelegate {
    //: func didClickSelectedRow(index: Int, title: String) {
    func nonePlayer(index _: Int, title: String) {
        //: switch title {
        switch title {
        //: case "Kick out".localized:  // 踢出房间
        case (String(k_contentName) + String(k_keyFrameTitle)).localized: // 踢出房间
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick out the room?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            FrameHiddenReactiveCompatible.theory(title: nil, message: (String(kNameFormat.prefix(5)) + "rm to" + String(kAtUpgradeBottomTitle.prefix(8)) + String(kKitId)).localized, leftBtnTitle: (String(k_selectedData.suffix(6))).localized, rightBtnTitle: (String(kSucceedUserTitle)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                EndProgressHUD.listHide()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomKickout(targetUid: self.userModel.uid, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
                EnableReqTool.tight(targetUid: self.userModel.uid, roomId: CoverManager.byRest().partyModel.roomId) { _, _, _ in
                    //: EndProgressHUD.popEnable()
                    EndProgressHUD.popEnable()
                    //: self.dismiss()
                    self.barTable()
                }
            }
        //: case "Unmute".localized: // 解除禁言
        case (String(k_viewExampleText)).localized: // 解除禁言
            //: TalkingAlertShow.alert(title: nil, message: "Are you sure you want to cancel the mute?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Confirm".localized) {
            FrameHiddenReactiveCompatible.theory(title: nil, message: String(bytes: kPlayName.map{managerName(leading: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(k_selectedData.suffix(6))).localized, rightBtnTitle: (String(k_acceptTitle) + k_giftText.lowercased()).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                EndProgressHUD.listHide()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomUnMute(targetUid: self.userModel.uid, roomID:TalkingVoiceRoomManager.shared().partyModel.roomId, completion: { succeed, result, errorModel in
                EnableReqTool.nameCompletion(targetUid: self.userModel.uid, roomID: CoverManager.byRest().partyModel.roomId, completion: { succeed, _, _ in
                    //: EndProgressHUD.popEnable()
                    EndProgressHUD.popEnable()
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "The mute has been lifted~".localized)
                        self.rear(showMsg: (String(k_attributeImageColorPath) + String(kImageUrl)).localized)
                    }
                    //: self.dismiss()
                    self.barTable()
                    //: })
                })
            }
        //: case "Mute".localized: // 设置禁言
        case (String(k_dataMessage.suffix(4))).localized: // 设置禁言
            //: muteView.showView()
            muteView.pushAside()
            //: muteView.targetUid = self.userModel.uid
            muteView.targetUid = self.userModel.uid
            //: self.dismiss()
            self.barTable()

        //: case "Kick off".localized: /// 踢下麦
        case (String(kByContent) + String(k_infoKey)).localized: /// 踢下麦
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick off the seat?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            FrameHiddenReactiveCompatible.theory(title: nil, message: (String(kQueryContent.suffix(4)) + "irm to ki" + String(k_equalName) + String(kActionText)).localized, leftBtnTitle: (String(k_selectedData.suffix(6))).localized, rightBtnTitle: (String(kSucceedUserTitle)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingVoiceRoomManager.shared().voiceRoom_reqAdminChangeMike(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                CoverManager.byRest().drawRein(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                //: self.dismiss()
                self.barTable()
            }

        //: case "Report".localized:
        case (String(k_inputPath)).localized:
            //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            let reportView = InformDataSource(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            //: reportView.showReportViewIn(view: nil)
            reportView.magnitudeeraction(view: nil)
        //: case "Block".localized:
        case (String(k_equalId)).localized:
            //: TalkingAlertShow.alert(title: nil, message: kMessage_blocking, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized) {
            FrameHiddenReactiveCompatible.theory(title: nil, message: user_barQuantityervalTitle, leftBtnTitle: (String(k_selectedData.suffix(6))).localized, rightBtnTitle: "OK".localized) {
                //: TalkingAlertShow.hideAlert()
                FrameHiddenReactiveCompatible.middleText()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAlertShow.hideAlert()
                FrameHiddenReactiveCompatible.middleText()
                //: TalkingUserRequestManager.func__pullBlackWithUserId(uid: self.userModel.uid, isBlack: true) { succeed, result, errorModel in
                FrameThen.cagey(uid: self.userModel.uid, isBlack: true) { succeed, _, _ in
                    //: if succeed == true {
                    if succeed == true {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Shielding Success".localized)
                        self.rear(showMsg: (String(kMakeMessage.suffix(4)) + "lding " + String(k_sharedPath.prefix(7))).localized)
                        //: self.dismiss()
                        self.barTable()
                    }
                }
            }
        //: default: break
        default: break
        }
    }
}

//: extension TalkingVoiceRoomCardView {
extension WithStreamThen {
    // 添加视图
    //: private func setupSubviews() {
    private func sexAdd() {
        //: self.addSubview(contentView)
        self.addSubview(contentView)
        //: self.addSubview(topIcon)
        self.addSubview(topIcon)
        //: contentView.addSubview(uidLb)
        contentView.addSubview(uidLb)
        //: contentView.addSubview(nameLb)
        contentView.addSubview(nameLb)
        //: contentView.addSubview(levelIcon)
        contentView.addSubview(levelIcon)
        //: contentView.addSubview(signLb)
        contentView.addSubview(signLb)
        //: contentView.addSubview(followersLab)
        contentView.addSubview(followersLab)
        //: contentView.addSubview(followersNum)
        contentView.addSubview(followersNum)
        //: contentView.addSubview(followingLab)
        contentView.addSubview(followingLab)
        //: contentView.addSubview(followingNum)
        contentView.addSubview(followingNum)

        //: contentView.layoutIfNeeded()
        contentView.layoutIfNeeded()

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: contentView.addSubview(lineView)
            contentView.addSubview(lineView)
            //: contentView.addSubview(stackView)
            contentView.addSubview(stackView)
            //: contentView.addSubview(moreBtn)
            contentView.addSubview(moreBtn)
        }
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func failFrom() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-44)
            make.bottom.equalToSuperview().offset(-44)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: if self.type == .user_me {
            if self.type == .user_me {
                //: make.height.equalTo(192)
                make.height.equalTo(192)
                //: }else {
            } else {
                //: make.height.equalTo(233)
                make.height.equalTo(233)
            }
        }
        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(contentView).offset(-33)
            make.top.equalTo(contentView).offset(-33)
            //: make.width.height.equalTo(80)
            make.width.height.equalTo(80)
        }
        //: uidLb.snp.makeConstraints { make in
        uidLb.snp.makeConstraints { make in
            //: make.leading.top.equalTo(12)
            make.leading.top.equalTo(12)
            //: make.trailing.equalTo(topIcon.snp.leading).offset(-10)
            make.trailing.equalTo(topIcon.snp.leading).offset(-10)
        }
        //: nameLb.snp.makeConstraints { make in
        nameLb.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom).offset(12)
            make.top.equalTo(topIcon.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview().offset(-12)
            make.centerX.equalToSuperview().offset(-12)
            //: make.width.lessThanOrEqualTo(ScreenWidth/2)
            make.width.lessThanOrEqualTo(constFormalContent / 2)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: levelIcon.snp.makeConstraints { make in
        levelIcon.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLb.snp.trailing).offset(5)
            make.leading.equalTo(nameLb.snp.trailing).offset(5)
            //: make.centerY.equalTo(nameLb)
            make.centerY.equalTo(nameLb)
            //: make.size.equalTo(CGSize(width: 32, height: 14))
            make.size.equalTo(CGSize(width: 32, height: 14))
        }
        //: signLb.snp.makeConstraints { make in
        signLb.snp.makeConstraints { make in
            //: make.top.equalTo(nameLb.snp.bottom).offset(12)
            make.top.equalTo(nameLb.snp.bottom).offset(12)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(38)
            make.height.equalTo(38)
        }
        //: followersNum.snp.makeConstraints { make in
        followersNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(-ScreenWidth/4+10)
            make.centerX.equalTo(contentView).offset(-constFormalContent / 4 + 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followingNum.snp.makeConstraints { make in
        followingNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(ScreenWidth/4-10)
            make.centerX.equalTo(contentView).offset(constFormalContent / 4 - 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followersLab.snp.makeConstraints { make in
        followersLab.snp.makeConstraints { make in
            //: make.top.equalTo(followersNum.snp.bottom).offset(5)
            make.top.equalTo(followersNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followersNum)
            make.centerX.equalTo(followersNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: followingLab.snp.makeConstraints { make in
        followingLab.snp.makeConstraints { make in
            //: make.top.equalTo(followingNum.snp.bottom).offset(5)
            make.top.equalTo(followingNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followingNum)
            make.centerX.equalTo(followingNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: lineView.snp.makeConstraints { make in
            lineView.snp.makeConstraints { make in
                //: make.top.equalTo(followersLab.snp.bottom).offset(12)
                make.top.equalTo(followersLab.snp.bottom).offset(12)
                //: make.leading.equalTo(7)
                make.leading.equalTo(7)
                //: make.trailing.equalTo(-7)
                make.trailing.equalTo(-7)
                //: make.height.equalTo(1)
                make.height.equalTo(1)
            }
            //: stackView.snp.makeConstraints { make in
            stackView.snp.makeConstraints { make in
                //: make.top.equalTo(lineView)
                make.top.equalTo(lineView)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(44)
                make.height.equalTo(44)
            }
            //: moreBtn.snp.makeConstraints { make in
            moreBtn.snp.makeConstraints { make in
                //: make.trailing.equalToSuperview()
                make.trailing.equalToSuperview()
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.size.equalTo(CGSize(width: 44, height: 20))
                make.size.equalTo(CGSize(width: 44, height: 20))
            }
        }
    }

    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with _: UIEvent?) {
        //: let touch = touches.first!
        let touch = touches.first!
        //: var point = touch.location(in: self.currentViewController()?.view)
        var point = touch.location(in: self.selectStart()?.view)
        //: point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        point = contentView.layer.convert(point, from: self.selectStart()?.view.layer)
        //: if !contentView.layer.contains(point) {
        if !contentView.layer.contains(point) {
            //: self.dismiss()
            self.barTable()
        }
    }
}
