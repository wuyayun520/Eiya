
//: Declare String Begin

/*: "msgType" :*/
fileprivate let kNameData:[Character] = ["m","s","g","T","y","p","e"]

/*: "tips" :*/
fileprivate let k_finishId:[Character] = ["t","i","p","s"]

/*: "extra" :*/
fileprivate let k_priceColorTitle:String = "extrlist"

/*: "msgInfo" :*/
fileprivate let kSucceedName:String = "false list as screenmsgInfo"

/*: "user" :*/
fileprivate let kLabValue:[Character] = ["u","s","e","r"]

/*: "uid" :*/
fileprivate let k_voiceValue:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let k_topText:[UInt8] = [0xb8,0xbf,0xb5,0xbd,0xb8,0xb7,0xbb,0xb3]

private func changeModel(point num: UInt8) -> UInt8 {
    return num ^ 214
}

/*: "headPic" :*/
fileprivate let k_toKey:[UInt8] = [0x9b,0x96,0x92,0x97,0xa3,0x9a,0x90]

private func sumDelete(replacement num: UInt8) -> UInt8 {
    return num ^ 243
}

/*: "age" :*/
fileprivate let kScriptFileTitleData:[UInt8] = [0xe1,0xe7,0xe5]

private func photoFrame(border num: UInt8) -> UInt8 {
    return num ^ 128
}

/*: "gift" :*/
fileprivate let k_selectMsg:[Character] = ["g","i","f","t"]

/*: "isGroup" :*/
fileprivate let k_tapName:String = "isGroting succeed"
fileprivate let kSizePath:String = "UP"

/*: "WantGift" :*/
fileprivate let kMakeMessage:[Character] = ["W"]
fileprivate let kLabelValue:String = "antGiftup gift self page"

/*: "txt" :*/
fileprivate let k_topStr:[Character] = ["t","x","t"]

/*: "img" :*/
fileprivate let k_textKey:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let k_versionMsg:String = "videmake"

/*: "audio" :*/
fileprivate let k_labelActualCellUrl:[Character] = ["a","u","d","i","o"]

/*: "messageType" :*/
fileprivate let kAddThanValue:[Character] = ["m","e","s"]
fileprivate let k_moreManagerName:[Character] = ["s","a","g","e","T","y","p","e"]

/*: "contentType" :*/
fileprivate let kStylePath:String = "copricet"
fileprivate let kExtraUrl:[Character] = ["e","n","t","T","y","p","e"]

/*: "ImgMsg" :*/
fileprivate let k_makeMessage:[Character] = ["I","m","g","M","s","g"]

/*: "imageUri" :*/
fileprivate let kTapErrorMessage:String = "imageUriturn begin type let"

/*: "type" :*/
fileprivate let k_nameId:String = "tysound"

/*: "pic" :*/
fileprivate let k_capacityMenuValue:[Character] = ["p","i","c"]

/*: "isBurn" :*/
fileprivate let kHidePageValue:String = "var now succeed end textisBurn"

/*: "giftId" :*/
fileprivate let kUserViewTextName:[Character] = ["g","i","f","t","I","d"]

/*: "params" :*/
fileprivate let kContainPath:[Character] = ["p","a","r","a","m"]
fileprivate let k_constraintMessage:String = "content"

/*: "audioLength" :*/
fileprivate let kIndexFormat:String = "hidden white push sharedaudioLen"
fileprivate let k_viewData:[Character] = ["g","t","h"]

/*: "len" :*/
fileprivate let kTopMakeMessage:String = "senden"

/*: "audioSandbox" :*/
fileprivate let k_angleName:String = "amodeldio"

/*: "senderId" :*/
fileprivate let k_finishData:String = "view modelsenderId"

/*: "AudioMsg" :*/
fileprivate let k_agePath:String = "Audikit tool as view"

/*: "content" :*/
fileprivate let k_appUrl:String = "cowrapewrap"

/*: "TxtMsg" :*/
fileprivate let k_objectData:[Character] = ["T","x","t","M","s"]
fileprivate let kAddName:[Character] = ["g"]

/*: "message" :*/
fileprivate let kUserMessage:String = "imagee"
fileprivate let kModeKindValue:[Character] = ["s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let k_normalTitle:String = "qstylete"
fileprivate let k_toSectionData:String = "file view return makeMsgId"

/*: "quoteMsgInfo" :*/
fileprivate let k_listPath:[Character] = ["q","u","o","t","e","M","s"]
fileprivate let k_pathValue:String = "gInfofollow result view title to"

/*: "rYMsgType" :*/
fileprivate let kPostMessage:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let kMakeSearchedStr:String = "key"
fileprivate let k_deadlineUserName:String = "action return phone front:Sess"
fileprivate let kLikeMessage:String = "reet:equal bar name a height"
fileprivate let k_hiddenModeName:String = "stomodel"

/*: "changeDirection" :*/
fileprivate let k_errorMsg:[Character] = ["c","h","a","n","g","e","D","i","r"]
fileprivate let kNorKey:[Character] = ["e","c","t","i","o","n"]

/*: "system" :*/
fileprivate let kGiftStr:String = "systuserm"

/*: "title" :*/
fileprivate let kModelFormat:String = "titlshow"

/*: "id" :*/
fileprivate let k_nameUrl:String = "iimage"

/*: "icon" :*/
fileprivate let k_interactionUrl:String = "ICON"

/*: "all" :*/
fileprivate let kInfoRequestText:[Character] = ["a","l","l"]

/*: "icon_chats_pcr" :*/
fileprivate let k_modelUrl:String = "background collection shared top viewicon_c"
fileprivate let k_socialUrl:[Character] = ["h","a","t","s","_","p","c","r"]

/*: "status" :*/
fileprivate let kPageUrl:String = "susertus"

/*: "msgIncome" :*/
fileprivate let k_liveUrl:String = "share"
fileprivate let k_requestTitle:String = "if left intimategIncome"

/*: "msgId" :*/
fileprivate let kColorTitleUrl:[Character] = ["m","s","g","I","d"]

/*: "AddWidthQuantity" :*/
fileprivate let k_validName:[Character] = ["T","M","s","g","T"]
fileprivate let kPagePath:[Character] = ["r"]
fileprivate let kTingMsg:String = "keysla"

/*: "TMsgTranslatedContent" :*/
fileprivate let kTopText:String = "first item failTMsgTr"
fileprivate let kRawTitle:String = "dContentequal name self app remove"

/*: "formatType" :*/
fileprivate let k_upTitle:String = "FORMA"
fileprivate let k_positionName:String = "self lab bottomtType"

/*: "plain" :*/
fileprivate let k_centerDataMessage:[Character] = ["p","l","a","i","n"]

/*: "imageData" :*/
fileprivate let k_equalLogPath:[Character] = ["i","m","a","g","e"]
fileprivate let k_ofPath:[Character] = ["D","a","t","a"]

/*: "VideoMsg" :*/
fileprivate let kEndName:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "match" :*/
fileprivate let kMessageShowTitle:String = "mainatch"

/*: "mf" :*/
fileprivate let kFromValue:String = "contentf"

/*: "userIntro" :*/
fileprivate let k_managerPathMsg:[Character] = ["u","s","e","r","I","n","t","r","o"]

/*: "targetId" :*/
fileprivate let kBurnPath:String = "targetIstatus publicly in class"
fileprivate let k_maybeId:[Character] = ["d"]

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let kAppearId:String = "hiddenhidden"
fileprivate let k_tipMessage:String = "dd HHfor true"
fileprivate let k_makeValue:[Character] = ["s"]

/*: "sendTime" :*/
fileprivate let kPathTitle:[Character] = ["s","e","n","d","T","i","m","e"]

/*: "isRead" :*/
fileprivate let kViewKey:String = "isReadmodel add new text"

/*: "audioData" :*/
fileprivate let kBeautyMatchName:String = "aumakeio"

/*: "audioUri" :*/
fileprivate let k_headKey:String = "aphoto"
fileprivate let kStyleUrl:String = "dioUrishare add case search top"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  IsoclinalThen.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum HiddenMultiplierTarget: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - IsoclinalThen

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class IsoclinalThen: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func acrossPhone() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> InscriptionThen? {
    public class func implantOfTip(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> InscriptionThen? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = netCompare(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: ResumeV2Listener.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            ResumeV2Listener.shared.electPrivete(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> InscriptionThen? {
    public class func netCompare(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> InscriptionThen? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(kNameData))] = (String(k_finishId))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(k_priceColorTitle.replacingOccurrences(of: "list", with: "a")): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = currentMain(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = TitleThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ContentModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? MakeModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(kSucceedName.suffix(7)))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(String(kLabValue))] = [
            //: "uid": DistanceAppManager.share.loginUserMode.userID,
            (String(k_voiceValue)): DistanceAppManager.share.loginUserMode.userID,
            //: "nickname": DistanceAppManager.share.loginUserMode.nickname,
            String(bytes: k_topText.map{changeModel(point: $0)}, encoding: .utf8)!: DistanceAppManager.share.loginUserMode.nickname,
            //: "headPic": DistanceAppManager.share.loginUserMode.headPic,
            String(bytes: k_toKey.map{sumDelete(replacement: $0)}, encoding: .utf8)!: DistanceAppManager.share.loginUserMode.headPic,
            //: "age": DistanceAppManager.share.loginUserMode.age,
            String(bytes: kScriptFileTitleData.map{photoFrame(border: $0)}, encoding: .utf8)!: DistanceAppManager.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(k_priceColorTitle.replacingOccurrences(of: "list", with: "a")): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = currentMain(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(kNameData))] as? String == (String(k_selectMsg)) {
            //: var data: InscriptionThen
            var data: InscriptionThen
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(k_tapName.prefix(5)) + kSizePath.lowercased())] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ItemCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = PitchReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = PitchReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ContentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(kNameData))] as? String == (String(kMakeMessage) + String(kLabelValue.prefix(7))) {
            //: let data = ModeReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ModeReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ContentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(kNameData))] as? String == (String(k_topStr)) {
            //: var data: InscriptionThen
            var data: InscriptionThen
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(k_tapName.prefix(5)) + kSizePath.lowercased())] as? String == "1" { // 群聊
                //: data = ToModelCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ToModelCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ToCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ToCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ContentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(kNameData))] as? String == (String(k_textKey)) {
            //: let data = ChatCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ChatCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ContentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(kNameData))] as? String == (k_versionMsg.replacingOccurrences(of: "make", with: "o")) {
            //: let data = EqualKeyCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = EqualKeyCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ContentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(kNameData))] as? String == (String(k_labelActualCellUrl)) {
            //: let data = TitleCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = TitleCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ContentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(String(k_selectMsg))] != nil && (msgInfo[(String(kAddThanValue) + String(k_moreManagerName))] as? Int) == 4 {
            //: let data = ToCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ToCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ContentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: InscriptionThen) -> InscriptionThen? {
    public class func baseballTeam(withData cellData: InscriptionThen) -> InscriptionThen? {
        // 图片
        //: if let reCellData = cellData as? ChatCellData {
        if let reCellData = cellData as? ChatCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(UserTextMacroDefine.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(UserTextMacroDefine.cookieData())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(kStylePath.replacingOccurrences(of: "price", with: "n") + String(kExtraUrl))] = (String(k_makeMessage))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(kNameData))] = (String(k_textKey))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(kTapErrorMessage.prefix(8)))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(kSucceedName.suffix(7)))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(k_nameId.replacingOccurrences(of: "sound", with: "pe"))] = (String(k_makeMessage))
            //: params["pic"] = filePath
            params[(String(k_capacityMenuValue))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(kHidePageValue.suffix(6)))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(kHidePageValue.suffix(6)))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(kUserViewTextName))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(kUserViewTextName))] = "0"
            }

            //: dic["params"] = params
            dic[(String(kContainPath) + k_constraintMessage.replacingOccurrences(of: "content", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = netCompare(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? EqualKeyCellData {
        if let reCellData = cellData as? EqualKeyCellData {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? TitleCellData {
        if let reCellData = cellData as? TitleCellData {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = AppNameThen.starExecute(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(kIndexFormat.suffix(8)) + String(k_viewData))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(kTopMakeMessage.replacingOccurrences(of: "send", with: "l"))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(k_angleName.replacingOccurrences(of: "model", with: "u") + "Sandbox")] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(k_angleName.replacingOccurrences(of: "model", with: "u") + "Sandbox")] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(kNameData))] = (String(k_labelActualCellUrl))
            //: msgInfo["senderId"] = DistanceAppManager.share.loginUserMode.userID
            msgInfo[(String(k_finishData.suffix(8)))] = DistanceAppManager.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(kSucceedName.suffix(7)))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(k_nameId.replacingOccurrences(of: "sound", with: "pe"))] = (String(k_agePath.prefix(4)) + "oMsg")
            //: dic["params"] = params
            dic[(String(kContainPath) + k_constraintMessage.replacingOccurrences(of: "content", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = netCompare(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? ToCellData {
        if let reCellData = cellData as? ToCellData {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(k_appUrl.replacingOccurrences(of: "wrap", with: "nt"))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(kNameData))] = (String(k_topStr))
            //: dic["msgInfo"] = msgInfo
            dic[(String(kSucceedName.suffix(7)))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(k_nameId.replacingOccurrences(of: "sound", with: "pe"))] = (String(k_objectData) + String(kAddName))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(kUserMessage.replacingOccurrences(of: "image", with: "m") + String(kModeKindValue))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(k_normalTitle.replacingOccurrences(of: "style", with: "uo") + String(k_toSectionData.suffix(5)))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(k_listPath) + String(k_pathValue.prefix(5)))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(String(kContainPath) + k_constraintMessage.replacingOccurrences(of: "content", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = netCompare(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> InscriptionThen? {
    public class func handleThroughFramework(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> InscriptionThen? {
        //: var cellData: InscriptionThen?
        var cellData: InscriptionThen?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(k_priceColorTitle.replacingOccurrences(of: "list", with: "a"))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(kSucceedName.suffix(7)))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(String(kLabValue))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? ContentModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == DistanceAppManager.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == DistanceAppManager.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == DistanceAppManager.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == DistanceAppManager.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(kPostMessage))] as? String == (kMakeSearchedStr.replacingOccurrences(of: "key", with: "GJ") + String(k_deadlineUserName.suffix(5)) + ":AutoG" + String(kLikeMessage.prefix(5)) + "TxtCu" + k_hiddenModeName.replacingOccurrences(of: "model", with: "m")) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(String(k_errorMsg) + String(kNorKey))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = stack(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(kNameData))] as? String == (kGiftStr.replacingOccurrences(of: "user", with: "e")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ContentModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? MakeModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = TitleThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(k_appUrl.replacingOccurrences(of: "wrap", with: "nt"))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = ChatCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ChatCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = EqualKeyCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = EqualKeyCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ItemCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = PitchReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = PitchReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = secure(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(k_priceColorTitle.replacingOccurrences(of: "list", with: "a"))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(kSucceedName.suffix(7)))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? WithSizeModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = ModeReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ModeReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = TitleCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = TitleCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? TitleCellData {
            if let audioCellData = cellData as? TitleCellData {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = tabEnable(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = BearEnableMsgTable.sharedMini(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = BearEnableMsgTable.placeDb(dict(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CoverCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(kModelFormat.replacingOccurrences(of: "show", with: "e"))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(k_appUrl.replacingOccurrences(of: "wrap", with: "nt"))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(String(k_textKey))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = CompartmentSingleCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = ToModelCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ToModelCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = ToCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ToCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? HeritageModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(k_interactionUrl.lowercased())] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: DistanceAppManager.share.loginUserMode.headPic ?? ""
                        DistanceAppManager.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = WithSizeModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(k_appUrl.replacingOccurrences(of: "wrap", with: "nt"))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(k_priceColorTitle.replacingOccurrences(of: "list", with: "a"))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = OutputModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (String(kInfoRequestText))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(k_modelUrl.suffix(6)) + String(k_socialUrl))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.sinceFrom(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(kPageUrl.replacingOccurrences(of: "user", with: "ta"))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(k_liveUrl.replacingOccurrences(of: "share", with: "ms") + String(k_requestTitle.suffix(7)))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(String(kAddThanValue) + String(k_moreManagerName))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(kColorTitleUrl))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(k_validName) + String(kPagePath) + kTingMsg.replacingOccurrences(of: "key", with: "an") + "teStatus")] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = AddWidthQuantity(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(kTopText.suffix(6)) + "anslate" + String(kRawTitle.prefix(8)))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = TimeExposureModel.loadLocalIntimatePhotoDict()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = TimeExposureModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = OeuvreModel.loadLocalIntimateVideoDict()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = OeuvreModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = SizeModel.loadAskForGiftArr()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func stack(_ extraDic: [String: Any]) -> HiddenMultiplierTarget {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(k_priceColorTitle.replacingOccurrences(of: "list", with: "a"))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(kSucceedName.suffix(7)))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? ContentModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(k_upTitle.lowercased() + String(k_positionName.suffix(5)))] as? String == (String(k_centerDataMessage)) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(kTapErrorMessage.prefix(8)))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(k_equalLogPath) + String(k_ofPath))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(kEndName)) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = dataRender(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(kMessageShowTitle.replacingOccurrences(of: "main", with: "m"))] as? Int) == 1 || (msgInfo?[(kMessageShowTitle.replacingOccurrences(of: "main", with: "m"))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(kNameData))] as? String == (String(k_finishId)) || (dic?[(String(kAddThanValue) + String(k_moreManagerName))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(k_upTitle.lowercased() + String(k_positionName.suffix(5)))] as? String == "mf") && ((dic?[(String(kAddThanValue) + String(k_moreManagerName))] as? Int) == 15 || (dic?[(String(kAddThanValue) + String(k_moreManagerName))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(kNameData))] as? String == (String(k_managerPathMsg)) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(kNameData))] as? String == (String(kMakeMessage) + String(kLabelValue.prefix(7))) || (msgInfo?[(String(kAddThanValue) + String(k_moreManagerName))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func dict(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.sinceFrom(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(k_priceColorTitle.replacingOccurrences(of: "list", with: "a"))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(kSucceedName.suffix(7)))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(String(kLabValue))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(String(k_finishData.suffix(8)))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == DistanceAppManager.share.loginUserMode.userID {
        if senderId == DistanceAppManager.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(String(k_finishData.suffix(8)))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(kBurnPath.prefix(7)) + String(k_maybeId))] = message.userID ?? (user?[(String(kBurnPath.prefix(7)) + String(k_maybeId))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(k_finishData.suffix(8)))] = "\(msgInfo?[(String(k_voiceValue))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(kBurnPath.prefix(7)) + String(k_maybeId))] = "\(msgInfo?[(String(k_voiceValue))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(kColorTitleUrl))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(kColorTitleUrl))] = "\(msgInfo?[(String(kColorTitleUrl))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.sinceFrom(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(kColorTitleUrl))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(kColorTitleUrl))] = "\(customDic[(String(kColorTitleUrl))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(kColorTitleUrl))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((k_angleName.replacingOccurrences(of: "model", with: "u") + "Sandbox")) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(k_angleName.replacingOccurrences(of: "model", with: "u") + "Sandbox")] = "\(msgInfo?[(k_angleName.replacingOccurrences(of: "model", with: "u") + "Sandbox")] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(kIndexFormat.suffix(8)) + String(k_viewData))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(kIndexFormat.suffix(8)) + String(k_viewData))] = "\(msgInfo?[(String(kIndexFormat.suffix(8)) + String(k_viewData))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (kAppearId.replacingOccurrences(of: "hidden", with: "yy") + "-MM-" + String(k_tipMessage.prefix(5)) + ":mm:s" + String(k_makeValue))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(kPathTitle))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == DistanceAppManager.share.loginUserMode.userID ? "1" : "0"
        info?[(String(kViewKey.prefix(6)))] = senderId == DistanceAppManager.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func tabEnable(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = dict(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(kColorTitleUrl))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func secure(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func currentMain(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func dataRender(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(kNameData))] as? String, msgType == (String(k_labelActualCellUrl)) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(kStylePath.replacingOccurrences(of: "price", with: "n") + String(kExtraUrl))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(k_agePath.prefix(4)) + "oMsg")
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(kBeautyMatchName.replacingOccurrences(of: "make", with: "d") + "Data")] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(k_headKey.replacingOccurrences(of: "photo", with: "u") + String(kStyleUrl.prefix(6)))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
