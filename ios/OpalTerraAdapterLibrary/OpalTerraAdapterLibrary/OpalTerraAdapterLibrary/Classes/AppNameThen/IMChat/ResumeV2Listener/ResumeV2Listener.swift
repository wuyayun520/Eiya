
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let kBeMessage:String = "add guard fatal arrayIM退出"
fileprivate let kRawMessage:String = "code:filter let"

/*: , desc: :*/
fileprivate let kIntervalDataValue:String = "list var, desc:"

/*: ." :*/
fileprivate let k_modelData:String = "lab"

/*: "IM登录失败，code: :*/
fileprivate let k_tabText:String = "make letIM登录"
fileprivate let kTimeMsg:String = "ode:section stop to image"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let kLiveMsg:String = "model constraintIM获取未读"
fileprivate let kWorldTitle:String = "lab number zone，code:"

/*: "crushBadge" :*/
fileprivate let kReloadFrameText:String = "lab flow else contentcrushB"
fileprivate let kBlockTitle:[Character] = ["a","d","g","e"]

/*: "badNumber" :*/
fileprivate let k_liveFormat:[UInt8] = [0xe3,0xe2,0xe5,0xcf,0xf6,0xee,0xe3,0xe6,0xf3]

fileprivate func equalCell(to num: UInt8) -> UInt8 {
    let value = Int(num) - 129
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "plaza" :*/
fileprivate let kAllowValue:String = "itemaza"

/*: "roomNum" :*/
fileprivate let k_messageCurrentPath:String = "position live sizeroomNum"

/*: "num" :*/
fileprivate let kTopKey:[Character] = ["n","u","m"]

/*: "onlyOneRoomId" :*/
fileprivate let k_indexMessage:String = "name plainonlyO"
fileprivate let k_contentPath:[Character] = ["n","e","R","o","o","m","I","d"]

/*: "LIVE-CHAT" :*/
fileprivate let k_videoName:[Character] = ["L","I","V","E","-","C"]
fileprivate let k_giftContent:[Character] = ["H","A","T"]

/*: "PARTY-CHAT" :*/
fileprivate let kLeadingId:String = "let leadingPARTY-CH"
fileprivate let kSearchedPath:String = "at"

/*: "extra" :*/
fileprivate let k_titleResumeValue:String = "EXTRA"

/*: "msgType" :*/
fileprivate let k_postTitle:[Character] = ["m","s"]
fileprivate let kAppearMessage:String = "gTypecontrol inside"

/*: "newPkgGift" :*/
fileprivate let kSoundText:[Character] = ["n","e","w","P","k","g","G","i","f","t"]

/*: "MF:ChatMentionMsg" :*/
fileprivate let k_recordMsg:String = "normal labMF:Ch"
fileprivate let kViewFormat:String = "atMmessage var"
fileprivate let kSharedStr:String = "entistop"

/*: "uid" :*/
fileprivate let kRequestKey:[UInt8] = [0x8b,0x97,0x9a]

private func giftTo(hidden num: UInt8) -> UInt8 {
    return num ^ 254
}

/*: "onlineStatus" :*/
fileprivate let kAddName:[UInt8] = [0xd1,0xd0,0xd2,0xd7,0xd0,0xdb,0xed,0xca,0xdf,0xca,0xcb,0xcd]

private func rawLet(sex num: UInt8) -> UInt8 {
    return num ^ 190
}

/*: "isNewUser" :*/
fileprivate let k_giftData:[UInt8] = [0x72,0x65,0x73,0x55,0x77,0x65,0x4e,0x73,0x69]

/*: "c2c_ :*/
fileprivate let kParaKey:String = "array color name toc2c_"

/*: "IM获取会话失败，convId: :*/
fileprivate let kToolName:[Character] = ["I","M","获","\u{53d6}","会","话"]
fileprivate let kSessionPath:String = "失败\u{ff0c}"
fileprivate let k_disappearFormat:[Character] = ["c","o","n","v","I","d",":"]

/*: , code: :*/
fileprivate let kBackPath:String = "view"
fileprivate let kManagerId:[Character] = [" ","c","o","d","e",":"]

/*: "IM插入消息失败，targetId: :*/
fileprivate let k_topData:[Character] = ["I","M","插","入","\u{6d88}","息","失","败","\u{ff0c}","t","a","r","g","e","t","I","d"]
fileprivate let k_pointName:String = "show"

/*: "changeDirection" :*/
fileprivate let k_requestData:String = "startange"
fileprivate let k_postNameFormat:String = "sightn"

/*: "rYMsgType" :*/
fileprivate let k_changeName:String = "if shared in blockrYMsgTyp"
fileprivate let k_userFormat:String = "component"

/*: "GJ:CallCustom" :*/
fileprivate let kInfoFormat:[Character] = ["G","J",":","C","a"]
fileprivate let k_moreFormat:[Character] = ["l","l","C","u","s","t","o","m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let kFemaleUrl:String = "IM获取会话列kit let need self action"
fileprivate let k_textSectionLetValue:[Character] = ["表","失","\u{8d25}"]
fileprivate let kKeyText:[Character] = ["\u{ff0c}","c","o","d","e",":"]

/*: "msgInfo" :*/
fileprivate let kDataTitleText:String = "manager add selected if extensionmsgInfo"

/*: "needFold" :*/
fileprivate let k_pagePath:String = "needFoldself label close size"

/*: "group_ :*/
fileprivate let k_makePath:String = "group_value true pop"

/*: "IM删除会话失败，convID: :*/
fileprivate let kGameKey:String = "IM删除会\u{8bdd}"
fileprivate let k_makeId:String = "vID:if let send fill"

/*: "IM置顶会话失败，convId: :*/
fileprivate let kBackgroundPath:String = "IM\u{7f6e}\u{9876}会"
fileprivate let kKitPresentTitleUrl:String = "table false equal type makeconvId:"

/*: "SDK 正在连接到服务器" :*/
fileprivate let kLayerContent:String = "SDK 正在连button strength in segment bottom"
fileprivate let k_byKey:[Character] = ["\u{63a5}","到","服","务","器"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let kGiftPath:String = "SDK \u{5df2}经"
fileprivate let k_fatalHiddenTitle:[Character] = ["\u{5668}"]

/*: "IM连接服务器失败，code: :*/
fileprivate let k_smallId:String = "IM连接服\u{52a1}器"
fileprivate let kCustomBackTitle:String = "失\u{8d25}，code:"

/*: , error: :*/
fileprivate let k_costMessage:String = ", ershare model self"

/*: "IM账号被挤下线。" :*/
fileprivate let k_hiddenViewPath:[Character] = ["I","M","账"]
fileprivate let kMethodStr:String = "号\u{88ab}挤\u{4e0b}线。"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let kBackgroundData:[UInt8] = [0x15,0x2b,0x31,0x2e,0xdc,0x1d,0x1f,0x1f,0x2b,0x31,0x2a,0x30,0xdc,0x25,0x2f,0xdc,0x28,0x2b,0x23,0x23,0x21,0x20,0xdc,0x25,0x2a,0xdc,0x2b,0x2a,0xdc,0x1d,0x2a,0x2b,0x30,0x24,0x21,0x2e,0xdc,0x20,0x21,0x32,0x25,0x1f,0x21,0xdc,0x1d,0x2a,0x20,0xdc,0x35,0x2b,0x31,0xdc,0x1d,0x2e,0x21,0xdc,0x22,0x2b,0x2e,0x1f,0x21,0x20,0xdc,0x30,0x2b,0xdc,0x23,0x2b,0xdc,0x2b,0x22,0x22,0x28,0x25,0x2a,0x21,0xdd,0xdc,0x5,0x22,0xdc,0x25,0x30,0xdc,0x25,0x2f,0xdc,0x2a,0x2b,0x30,0xdc,0x20,0x2b,0x2a,0x21,0xdc,0x1e,0x35,0xdc,0x29,0x21,0xe8,0xdc,0x30,0x24,0x21,0xdc,0x2c,0x1d,0x2f,0x2f,0x33,0x2b,0x2e,0x20,0xdc,0x29,0x1d,0x35,0xdc,0x24,0x1d,0x32,0x21,0xdc,0x1e,0x21,0x21,0x2a,0xdc,0x28,0x21,0x1d,0x27,0x21,0x20,0xe8,0xdc,0x1d,0x2a,0x20,0xdc,0x25,0x30,0xdc,0x25,0x2f,0xdc,0x2e,0x21,0x1f,0x2b,0x29,0x29,0x21,0x2a,0x20,0x21,0x20,0xdc,0x30,0x2b,0xdc,0x1f,0x24,0x1d,0x2a,0x23,0x21,0xdc,0x30,0x24,0x21,0xdc,0x2c,0x1d,0x2f,0x2f,0x33,0x2b,0x2e,0x20,0xea]

fileprivate func leadingIcon(button num: UInt8) -> UInt8 {
    let value = Int(num) + 68
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Offline Notification" :*/
fileprivate let kLimitMessage:String = "Offlinstyle model"
fileprivate let kContentMsg:String = "fmodelation"

/*: "Cancel" :*/
fileprivate let k_errorMessage:[Character] = ["C","a","n","c","e","l"]

/*: "Re Login" :*/
fileprivate let kShareMsg:[Character] = ["R","e"," ","L","o","g","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ResumeV2Listener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol AlongManagerDelegate: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func placePair(data: [ViewConversationModel])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func appDoing(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func attention(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum FileDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class ResumeV2Listener: NSObject {
@objcMembers public class ResumeV2Listener: NSObject {
    //: public static let shared = ResumeV2Listener()
    public static let shared = ResumeV2Listener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [ViewConversationModel] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [ViewConversationModel] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [ViewConversationModel] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [ViewConversationModel] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [ViewConversationModel] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: ViewConversationModel]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.ok()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func indexInit() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(const_deviceText, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func ok() {
        //: func__removeAllDelegate()
        prepare()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.funcResume()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func funcResume() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = ViewConversationModel.toForm(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = ViewConversationModel.toForm(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func statusEqual() -> [ViewConversationModel] {
        // 审核模式，在消息列表移除官方客服
        //: if DistanceAppManager.share.appStatus == AppSkinStatus.special.rawValue {
        if DistanceAppManager.share.appStatus == NameQuickLookable.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == UserTextMacroDefine.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == UserTextMacroDefine.lengthId() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension ResumeV2Listener {
extension ResumeV2Listener {
    //: func func__removeAllDelegate() {
    func prepare() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func recordDelegate(_ delegate: AlongManagerDelegate) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func valueFrom(_ delegate: AlongManagerDelegate) {
        //: self.func__invokeDelegate {
        self.icon {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func icon(_ invocation: (AlongManagerDelegate) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! AlongManagerDelegate)
        }
    }
}

// MARK: - IM登录态管理

//: extension ResumeV2Listener {
extension ResumeV2Listener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func language() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            UploadDisplayThen.makeSelected(msg: (String(kBeMessage.suffix(4)) + "登录失败，" + String(kRawMessage.prefix(5))) + "\(code)" + (String(kIntervalDataValue.suffix(7))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.ok()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func loging(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = DistanceAppManager.share.appUserConfigMode.uSig
        let uSigStr = DistanceAppManager.share.appUserConfigMode.uSig
        //: let uidStr = DistanceAppManager.share.loginUserMode.userID
        let uidStr = DistanceAppManager.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.bag()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            UploadDisplayThen.makeSelected(msg: (String(k_tabText.suffix(4)) + "\u{5931}\u{8d25}，c" + String(kTimeMsg.prefix(4))) + "\(code)" + (String(kIntervalDataValue.suffix(7))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func demonstrate() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || DistanceAppManager.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || DistanceAppManager.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.arcInfo(showMsg: appViewPingValue)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension ResumeV2Listener {
extension ResumeV2Listener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func bag() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            UploadDisplayThen.makeSelected(msg: (String(kLiveMsg.suffix(6)) + "消息总数失败" + String(kWorldTitle.suffix(6))) + "\(code)" + (String(kIntervalDataValue.suffix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func insideCornerMsg() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: PlayerRequestTool.req_sysMessageList { _, result in
        PlayerRequestTool.soundAwake { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(kReloadFrameText.suffix(6)) + String(kBlockTitle))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: notiVideoValue, object: self, userInfo: [String(bytes: k_liveFormat.map{equalCell(to: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func modelView(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: PlayerRequestTool.req_getChatRoomInfo { succeed, result, _ in
        PlayerRequestTool.mini { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(kAllowValue.replacingOccurrences(of: "item", with: "pl"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(k_messageCurrentPath.suffix(7)))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = ViewConversationModel.toForm(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(String(kTopKey))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(k_indexMessage.suffix(5)) + String(k_contentPath))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension ResumeV2Listener: V2TIMAdvancedMsgListener {
extension ResumeV2Listener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(k_videoName) + String(k_giftContent))) || msg.groupID.contains((String(kLeadingId.suffix(8)) + kSearchedPath.uppercased()))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            WithReactiveCompatible.appearShould().afterMax(msg: msg)
            //: return
            return
        }
        //: if msg.sender == DistanceAppManager.share.loginUserMode.userID { return }
        if msg.sender == DistanceAppManager.share.loginUserMode.userID { return }

        //: let dic = DistrictManagerReactiveCompatible.parseTXMessageData(data: msg.customElem.data)
        let dic = DistrictManagerReactiveCompatible.productData(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(k_titleResumeValue.lowercased())] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? ContentModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: const_recordTableContent, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(k_postTitle) + String(kAppearMessage.prefix(5)))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(kSoundText)) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: app_micContent, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if DistrictManagerReactiveCompatible.checkVoiceMessage(message: msg) {
            if DistrictManagerReactiveCompatible.applicationMessage(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = IsoclinalThen.dict(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                BearEnableMsgTable.placeDb(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.electrifyDic(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            ViewMakeReactiveCompatible.shared.mottleUponPhoneMessage(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            ViewMakeReactiveCompatible.shared.accentuation(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(k_recordMsg.suffix(5)) + String(kViewFormat.prefix(3)) + kSharedStr.replacingOccurrences(of: "stop", with: "o") + "nMsg") { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            SeparateChatManager.share.lubricate(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: JoinThen.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: JoinThen.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != UserTextMacroDefine.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != UserTextMacroDefine.publicationRequest() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.icon { $0.attention?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            NotificationReactiveCompatible.shared.share(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: kRequestKey.map{giftTo(hidden: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: kAddName.map{rawLet(sex: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: k_giftData.reversed(), encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: main_screenData,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            AppNameThen.server(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            JoinThen.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: mainUserNameValue, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: InscriptionThen, isPrivete: Bool) {
    @objc public func electPrivete(targetId: String, msgCellData: InscriptionThen, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.referencePointButton(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(kParaKey.suffix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.writeAcross(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.establish(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                UploadDisplayThen.makeSelected(msg: (String(kToolName) + kSessionPath + String(k_disappearFormat)) + "\(convId)" + (kBackPath.replacingOccurrences(of: "view", with: ",") + String(kManagerId)) + "\(code)" + (String(kIntervalDataValue.suffix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func referencePointButton(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = DistanceAppManager.share.loginUserMode.userID
        let senderStr = DistanceAppManager.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                UploadDisplayThen.makeSelected(msg: (String(k_topData) + k_pointName.replacingOccurrences(of: "show", with: ":")) + "\(targetId)" + (kBackPath.replacingOccurrences(of: "view", with: ",") + String(kManagerId)) + "\(code)" + (String(kIntervalDataValue.suffix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    func getString() -> String {
        return (String(kInfoFormat) + String(k_moreFormat))
    }
    
    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func electrifyDic(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(k_requestData.replacingOccurrences(of: "start", with: "ch") + "Directi" + k_postNameFormat.replacingOccurrences(of: "sight", with: "o"))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && AddReactiveCompatible.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic["rYMsg"+"Type"].stringValue == getString() && isSelf && AddReactiveCompatible.share.pageBy()!.isKind(of: DistantViewController.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: appNoName) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: appNoName)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension ResumeV2Listener: V2TIMConversationListener {
extension ResumeV2Listener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.writeAcross(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.establish(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.writeAcross(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.establish(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: DistanceAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        DistanceAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.icon {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.appDoing?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func establish(conVersationList: [ViewConversationModel]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if AppNameThen.pushShared().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                AppNameThen.pushShared().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.icon {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.placePair?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func tool(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [ViewConversationModel] = []

        // V2TIMConversation->ViewConversationModel
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = ViewConversationModel(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.toShow(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [ViewConversationModel] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = ConcealedInfoTable.enableEqual(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.momentTop(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension ResumeV2Listener {
extension ResumeV2Listener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func sharedImage(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.tool(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.establish(conVersationList: arr as? [ViewConversationModel] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            UploadDisplayThen.makeSelected(msg: (String(kFemaleUrl.prefix(7)) + String(k_textSectionLetValue) + String(kKeyText)) + "\(code)" + (String(kIntervalDataValue.suffix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func keyBottomLab(targetId: String) -> ViewConversationModel? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func writeAcross(convList: [V2TIMConversation]) -> [ViewConversationModel] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [ViewConversationModel] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = ViewConversationModel(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.toShow(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.momentTop(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func momentTop(convModel: ViewConversationModel, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.funcResume()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: FileDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - DistanceAppManager.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - DistanceAppManager.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = AppNameThen.imageBar(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = DistrictManagerReactiveCompatible.parseTXMessageData(data: elemData)
            let dict = DistrictManagerReactiveCompatible.productData(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(k_titleResumeValue.lowercased())][(String(kDataTitleText.suffix(7)))][(String(k_pagePath.prefix(8)))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.cellType(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.cellType(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func cellType(convList: [ViewConversationModel], dataType: FileDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func equalType(targetId: String, dataType: FileDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(kParaKey.suffix(4))) + "\(targetId)" : (String(k_makePath.prefix(6))) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            AppNameThen.contentWith(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(DistanceAppManager.share.loginUserMode.userID, toUid: targetId)
            BearEnableMsgTable.transform(DistanceAppManager.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.bag()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            UploadDisplayThen.makeSelected(msg: (kGameKey + "失败，con" + String(k_makeId.prefix(4))) + "\(convID)" + (kBackPath.replacingOccurrences(of: "view", with: ",") + String(kManagerId)) + "\(code)" + (String(kIntervalDataValue.suffix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func topSelect(convModel: ViewConversationModel, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            UploadDisplayThen.makeSelected(msg: (kBackgroundPath + "话失败，" + String(kKitPresentTitleUrl.suffix(7))) + "\(convModel.conversationID)" + (kBackPath.replacingOccurrences(of: "view", with: ",") + String(kManagerId)) + "\(code)" + (String(kIntervalDataValue.suffix(7))) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.cellType(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.cellType(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension ResumeV2Listener: V2TIMSDKListener {
extension ResumeV2Listener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(kLayerContent.prefix(7)) + String(k_byKey)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (kGiftPath + "\u{6210}功连接到服务" + String(k_fatalHiddenTitle)))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        UploadDisplayThen.makeSelected(msg: (k_smallId + kCustomBackTitle) + "\(code)" + (String(k_costMessage.prefix(4)) + "ror:") + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        UploadDisplayThen.makeSelected(msg: (String(k_hiddenViewPath) + kMethodStr))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        ListSocketManager.shared.tableApp(userLoginOut: true)
        //: ResumeV2Listener.shared.isConnection = false
        ResumeV2Listener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (TouchMakeReactiveCompatible.shared as! TouchMakeReactiveCompatible).textAdd()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: kBackgroundData.map{leadingIcon(button: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = VideoAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if UserViewToolThen.share.interfaceLang == PlayerSubqueryConvertible.ar.rawValue || UserViewToolThen.share.interfaceLang == PlayerSubqueryConvertible.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            UserViewToolThen.share.interfaceLang == PlayerSubqueryConvertible.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.tapShared(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        FrameHiddenReactiveCompatible.theory()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        FrameHiddenReactiveCompatible.theory(title: (String(kLimitMessage.prefix(6)) + "e Noti" + kContentMsg.replacingOccurrences(of: "model", with: "ic")).localized, message: msg, leftBtnTitle: (String(k_errorMessage)).localized, rightBtnTitle: (String(kShareMsg)).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ValueThen.kindShared().feeDoing()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            CoverManager.byRest().voiceOverShared()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            IngatheringPullReactiveCompatible.harvestMoon { t in
                //: EndProgressHUD.popEnable()
                EndProgressHUD.popEnable()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: notiButtonData, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            IngatheringPullReactiveCompatible.listRecordSort { t in
                //: if t {
                if t {
                    //: ResumeV2Listener.shared.func__LogingIn { _ in
                    ResumeV2Listener.shared.loging { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if ValueThen.kindShared().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(ValueThen.kindShared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    ListSocketManager.shared.stateAction()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
