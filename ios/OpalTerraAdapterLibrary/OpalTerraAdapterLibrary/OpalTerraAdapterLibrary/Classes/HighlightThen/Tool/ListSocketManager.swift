
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let kTextMsg:String = "SOCKET_Alanguage var at"
fileprivate let k_dataId:[Character] = ["D","D","R","_","K","E","Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let k_timePath:[UInt8] = [0x6e,0x72,0x7e,0x76,0x78,0x69,0x62,0x6b,0x74,0x79,0x78,0x72,0x7e,0x75,0x7c,0x69,0x62,0x6f,0x78,0x6c,0x68,0x78,0x6e,0x69,0x7e,0x7c,0x71,0x71,0x62,0x73,0x72,0x69,0x74,0x7b,0x74,0x7e,0x7c,0x69,0x74,0x72,0x73]

private func titleApp(content num: UInt8) -> UInt8 {
    return num ^ 61
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let kLeadingRawText:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4e,0x45,0x45,0x52,0x43,0x53,0x54,0x41,0x4f,0x4c,0x46,0x5f,0x45,0x56,0x49,0x43,0x45,0x52]

/*: "isCalling" :*/
fileprivate let k_normalValueKey:String = "isCalist value"
 
/*: "SOCKET 连接成功." :*/
fileprivate let k_topName:[Character] = ["S","O","C","K","E","T"," ","连","接","\u{6210}","功","."]

/*: "SOCKET 连接失败, error: :*/
fileprivate let k_timeText:String = "SOCKlab"
fileprivate let kColorPath:String = "status label equal to hidden, error:"

/*: ." :*/
fileprivate let k_arrayImageUrl:String = "model"

/*: "SOCKET 失败 :*/
fileprivate let kObserverFormat:[Character] = ["S","O","C","K","E","T"," "]
fileprivate let kOnId:String = "失suite"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let kAppColorName:String = "次, medium"
fileprivate let k_toFormat:String = "et域\u{540d}重新"

/*: "SOCKET 已关闭, code: :*/
fileprivate let k_positionStr:[Character] = ["S","O","C"]
fileprivate let kActionMoreEqualFormat:[Character] = ["K","E","T"," ","\u{5df2}","\u{5173}","\u{95ed}",","," ","c","o","d","e",":"]

/*: , reason: :*/
fileprivate let kEqualAssetStr:String = ","
fileprivate let k_centerArrayData:[Character] = [" ","r","e","a","s","o","n",":"]

/*: "ws:// :*/
fileprivate let k_selectFilePath:[Character] = ["w","s",":","/","/"]

/*: /?sid= :*/
fileprivate let k_nameViewData:String = "safe show/?sid="

/*: &uid= :*/
fileprivate let k_videoPhotoUrl:[Character] = ["&","u","i","d","="]

/*: &platform=iphone&version= :*/
fileprivate let k_giftName:String = "of in text manager name&platf"
fileprivate let kImageDataText:String = "honhead"
fileprivate let k_plainStr:String = "="

/*: &packageId= :*/
fileprivate let k_cellData:String = "color with label actual&pack"
fileprivate let k_errorShadowUrl:[Character] = ["="]

/*: "1v1server. :*/
fileprivate let k_mediumMessage:String = "record class file1v1serv"
fileprivate let k_modelId:String = "er.loop item self smart"

/*: .com:7766" :*/
fileprivate let kPositionStr:String = ".commake video color"
fileprivate let k_enableTitle:[Character] = ["6"]

/*: "43.129.185.194:7767" :*/
fileprivate let k_withContent:String = "43.12"
fileprivate let k_arrayToolUrl:String = ".194:776"
fileprivate let k_labData:[Character] = ["7"]

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let k_totalPointName:[UInt8] = [0xdc,0xc0,0xcc,0xc4,0xca,0xdb,0xaf,0x68,0x32,0x1e,0x68,0x34,0x13,0xfd,0xea,0xee,0xec,0xe7,0xee,0xed,0xe6,0xe3,0xe6,0xfb,0xf6,0xcc,0xe7,0xee,0xe1,0xe8,0xea,0xeb,0x68,0x14,0x1e,0x6a,0x1f,0x23,0x60,0x33,0x3,0x69,0x19,0x22,0x68,0x32,0x1e,0x6a,0xa,0x3c,0x66,0x18,0x22,0xa1]

private func labelUser(arc num: UInt8) -> UInt8 {
    return num ^ 143
}

/*: "cmd" :*/
fileprivate let k_toolKey:String = "ccelld"

/*: "ping" :*/
fileprivate let k_succeedFormat:String = "pipic"

/*: "callHeartbeat" :*/
fileprivate let kEqualKey:[Character] = ["c","a","l","l","H","e","a","r","t","b","e","a"]
fileprivate let k_viewFormat:[Character] = ["t"]

/*: "recievedDate" :*/
fileprivate let kSizeStr:[Character] = ["r","e","c","i","e","v","e","d"]
fileprivate let kLoadMessage:[Character] = ["D","a","t","e"]

/*: "liveHeartBeat" :*/
fileprivate let k_itemPath:String = "LIVE"
fileprivate let kUserTitle:String = "sizeat"

/*: "🔗 关闭 socket" :*/
fileprivate let k_makeGiftUrl:String = "user normal🔗 关闭 s"

/*: "The user logs out" :*/
fileprivate let k_listFormat:String = "standard direction user super popThe us"
fileprivate let kContentResultKey:String = "enter import model leading succeedgs ou"
fileprivate let k_labStr:String = "target"

/*: "SOCKET  :*/
fileprivate let k_followingMsg:String = "SOCKEsucceed"
fileprivate let kReportText:String = "name"

/*: 重连. 重试次数： :*/
fileprivate let k_modelKeyPath:[Character] = ["重","连","."," ","重","\u{8bd5}","次","数","："]

/*: , 地址： :*/
fileprivate let k_enablePath:[Character] = [","," ","地","址","："]

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let k_infoScreenUrl:[Character] = ["S","O","C","K","E","T"," ","连","接","正","常"," ","不","\u{9700}","要","重","新"]
fileprivate let k_mainStr:[Character] = ["连","接","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let k_frameUrl:String = "in path target coatSOCKET 正"
fileprivate let k_countStyleProMessage:String = "在连bar "
fileprivate let kKeyName:String = "\u{65b0}连接."

/*: "&resume=1" :*/
fileprivate let kFirstUrl:String = "&resumin view corner"
fileprivate let kMessageDismissStretchContent:String = "to leading for page assete=1"

/*: "&lang= :*/
fileprivate let kViewPath:String = "bottom equal&lang="

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let kValueMessage:[Character] = ["S","O","C","K","E"]
fileprivate let k_backgroundMainStr:[Character] = ["T"," ","开","始","\u{8fde}","接",","," ","地","址","："]

/*: "onNetDisabled" :*/
fileprivate let kEndId:[Character] = ["o","n","N","e","t","D"]
fileprivate let kPlayerMsg:String = "isabldata"

/*: "errno" :*/
fileprivate let k_contentMatchDefineName:String = "efeedbackno"

/*: "data" :*/
fileprivate let k_makeValue:String = "dlayera"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let kEqualTurnMessage:[Character] = ["S","O","C","K","E","T"," ","连","接","保","活","c","m","d",":"," ","p","i"]
fileprivate let kCollectionData:String = "ng.type ratio content jump play"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let k_modeStr:[UInt8] = [0xdd,0xd9,0xcd,0xd5,0xcf,0xde,0xaa,0xfe,0xfc,0xfe,0xed,0x6e,0x49,0x27,0x70,0x3e,0x45,0xed,0xf7,0xee,0xc4,0xaa,0xed,0xeb,0xf6,0xf6,0xd2,0xef,0xeb,0xfc,0xfe,0xec,0xef,0xeb,0xfe,0xb8]

fileprivate func labelError(interaction num: UInt8) -> UInt8 {
    let value = Int(num) + 118
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let k_aboutStr:[UInt8] = [0xaa,0xb6,0xba,0xb2,0xbc,0xad,0xd9,0x95,0x90,0x8f,0x9c,0x1d,0x46,0x64,0x1f,0x4d,0x42,0x9a,0x94,0x9d,0xc3,0xd9,0x95,0x90,0x8f,0x9c,0xb1,0x9c,0x98,0x8b,0x8d,0xbb,0x9c,0x98,0x8d,0xd7]

private func filterLoad(make num: UInt8) -> UInt8 {
    return num ^ 249
}

/*: "onP" :*/
fileprivate let kClearStr:[Character] = ["o","n","P"]

/*: "msg" :*/
fileprivate let k_titleIndexValue:[Character] = ["m","s","g"]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let kSessionMessage:String = "🔗 \u{5904}理\u{6d88}息"
fileprivate let k_addMessage:String = "as lab selfame:"

/*: , errNo: :*/
fileprivate let kSubmitHeadText:String = ", erempty normal to"

/*: , msg: :*/
fileprivate let kCenterStr:String = ", msg:make value background"

/*: , data: :*/
fileprivate let k_fileName:[Character] = [","," ","d","a","t","a",":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let k_endFormat:String = "SOCbackgroundE"
fileprivate let k_iconKey:String = "异\u{5e38}\u{6d88}息：cm"
fileprivate let kTextFormat:String = "color vardName:"

/*: "onReceivedGift" :*/
fileprivate let k_infoShowGestureTitle:[Character] = ["o","n","R","e","c","e","i","v","e","d","G","i","f"]
fileprivate let kSubmitTitle:String = "recording"

/*: "onVideoMatch" :*/
fileprivate let kRawName:[Character] = ["o","n","V","i","d","e","o","M","a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let k_messageId:String = "data modelonReq"
fileprivate let kVoiceName:[Character] = ["l","l"]

/*: "onCancelCall" :*/
fileprivate let kToText:[Character] = ["o","n","C","a","n","c","e","l","C","a","l","l"]

/*: "onRefuseCall" :*/
fileprivate let kModelValue:String = "onRpush match"
fileprivate let k_pathContent:[Character] = ["e","f","u","s","e","C","a","l","l"]

/*: "onAcceptCall" :*/
fileprivate let k_transformMsg:String = "to"
fileprivate let k_beanName:String = "array cell backgroundnAcc"

/*: "onStopCall" :*/
fileprivate let kViewUrl:[Character] = ["o","n","S","t"]
fileprivate let kImageStr:[Character] = ["o","p","C","a","l","l"]

/*: "freeCallTimes" :*/
fileprivate let k_executeMessage:String = "freeClist mode"
fileprivate let kPostUrl:String = "succeed class tapallTimes"

/*: "mfCoin" :*/
fileprivate let k_areaValue:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let kCookieMsg:String = "section format falseonMissC"
fileprivate let kDataName:[Character] = ["o","i","n"]

/*: "acceptCall" :*/
fileprivate let kPostBlockKey:String = "userce"

/*: "stopCall" :*/
fileprivate let k_collectionViewPath:String = "stopCamodel var"
fileprivate let kMakeMessage:String = "transaction"

/*: "requestCall" :*/
fileprivate let k_modelDataKey:String = "reqwithe"

/*: "[Invite you to a video call...]" :*/
fileprivate let k_appText:[UInt8] = [0xe,0xfc,0x21,0x29,0x1c,0x27,0x18,0xd3,0x2c,0x22,0x28,0xd3,0x27,0x22,0xd3,0x14,0xd3,0x29,0x1c,0x17,0x18,0x22,0xd3,0x16,0x14,0x1f,0x1f,0xe1,0xe1,0xe1,0x10]

fileprivate func reloadIndex(manager num: UInt8) -> UInt8 {
    let value = Int(num) - 179
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let kLeadingFormat:String = "tonpe"

/*: "[Invite you to a voice call...]" :*/
fileprivate let kPhotoText:[UInt8] = [0x91,0x83,0xa4,0xbc,0xa3,0xbe,0xaf,0xea,0xb3,0xa5,0xbf,0xea,0xbe,0xa5,0xea,0xab,0xea,0xbc,0xa5,0xa3,0xa9,0xaf,0xea,0xa9,0xab,0xa6,0xa6,0xe4,0xe4,0xe4,0x97]

private func valueTip(define num: UInt8) -> UInt8 {
    return num ^ 202
}

/*: "uid" :*/
fileprivate let k_viewData:String = "uiof"

/*: "nickname" :*/
fileprivate let kSourceFormat:String = "nrankkname"

/*: "headPic" :*/
fileprivate let k_imageFormat:String = "style"
fileprivate let kModelTitle:[Character] = ["e","a","d","P","i","c"]

/*: "onCallResume" :*/
fileprivate let k_showEqualItemTitle:String = "show modeonCall"
fileprivate let k_labelPath:[Character] = ["R"]
fileprivate let kHiddenKey:[Character] = ["e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let k_resFormat:String = "tracknc"

/*: "refuseCall" :*/
fileprivate let kDirectionName:String = "reffectu"

/*: "onCallMotivation" :*/
fileprivate let k_removeFormat:String = "self status let always backonCal"
fileprivate let kLifeUrl:String = "iedget"
fileprivate let k_screenData:[Character] = ["i","o","n"]

/*: "onLiveWarning" :*/
fileprivate let k_normalPath:[Character] = ["o","n","L","i","v","e"]
fileprivate let kMenuClickStr:[Character] = ["W"]
fileprivate let kValidFormat:String = "arnsuccessg"

/*: "reason" :*/
fileprivate let k_valueBottomTitleId:String = "reasconstraint"
fileprivate let k_theName:String = "raw"

/*: "time" :*/
fileprivate let kInfoValue:String = "tilayer"

/*: "onStopLive" :*/
fileprivate let kRecordSubmitContentPath:String = "red direction path varonSt"
fileprivate let kVoiceKey:String = "text"

/*: "onSendProgramGift" :*/
fileprivate let k_moreStr:String = "for public will recordonSen"
fileprivate let kAppKey:String = "dProcamera selection view frame"
fileprivate let k_styleStatusValue:[Character] = ["g","r","a","m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let kModelPath:String = "onGameFsmodel kind"

/*: "broadStatus" :*/
fileprivate let kContactContent:String = "brat"
fileprivate let k_modeShowMsg:String = "contents"

/*: "onCallChatMsg" :*/
fileprivate let kLineContent:[Character] = ["o","n","C"]
fileprivate let kTextKey:[Character] = ["a","l","l","C","h","a","t","M","s","g"]

/*: "onNotificationPopupWin" :*/
fileprivate let k_lessFormat:String = "equal in varonNoti"
fileprivate let kStrengthMsg:String = "ionPoview to if"
fileprivate let k_sizeId:[Character] = ["n"]

/*: "onUserBlock" :*/
fileprivate let kPlayerId:[Character] = ["o","n","U","s","e","r","B","l","o","c"]
fileprivate let k_lineTitle:String = "add"

/*: "freeCallWin" :*/
fileprivate let k_tableSharedText:String = "FREE"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ListSocketManager.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let const_failScreenValue = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let k_halfButtonValue = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let main_viewValue = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let showTableUserName = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let noti_countPageData = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let noti_recordText = (String(kTextMsg.prefix(8)) + String(k_dataId))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let dataPopName = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let userSystemData = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let app_buttonClickData = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let show_bottomSenseData = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let app_plainValue = NSNotification.Name(rawValue: String(bytes: k_timePath.map{titleApp(content: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let dataKeyTitle = NSNotification.Name(rawValue: String(bytes: kLeadingRawText.reversed(), encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol PerformanceErrorDelegate: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func socket(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol JibeMatchDelegate: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func interval(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func ingot(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol ItemCallerDelegate: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func positionData(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func entitle(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func motivation(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func currencyHide(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol VisualCommunicationObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func fosterling(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func addUser(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func life(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class ListSocketManager: NSObject {
    //: enum SocketRetryType: String {
    enum WillItem: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currMyVC: AnyObject?
    var currMyVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: MakeLanguageManager.shared.direction == .rightToLeft ? 0 : constFormalContent,
        //: y: StatusBarHeight+190
        y: main_netValue + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: MakeLanguageManager.shared.direction == .rightToLeft ? 0 : constFormalContent,
        //: y: StatusBarHeight+190
        y: main_netValue + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: MakeLanguageManager.shared.direction == .rightToLeft ? 0 : constFormalContent,
        //: y: StatusBarHeight+500
        y: main_netValue + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: PerformanceErrorDelegate? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: JibeMatchDelegate? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: ItemCallerDelegate? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: VisualCommunicationObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    
    @objc dynamic var isSpace = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = ListSocketManager()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(dataBy(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(queryUpCloseInTinkleMessage),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(k_normalValueKey.prefix(4)) + "lling"))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (TouchMakeReactiveCompatible.shared as! TouchMakeReactiveCompatible).gapAdd()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            
            .observeWeakly(Bool.self, "isSpace")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (TouchMakeReactiveCompatible.shared as! TouchMakeReactiveCompatible).gapAdd()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension ListSocketManager: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        UploadDisplayThen.makeSelected(msg: (String(k_topName)))
        //: sendPing()
        turnWithSkitter()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: k_halfData,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: noti_recordText)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        measureViewBuildMiniskirt()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        UploadDisplayThen.makeSelected(msg: (k_timeText.replacingOccurrences(of: "lab", with: "E") + "T \u{8fde}接失败" + String(kColorPath.suffix(8))) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = DistanceAppManager.share.reachability?.connection, status != .unavailable else {
        guard let status = DistanceAppManager.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == showTableUserName {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = disableRetry(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            UploadDisplayThen.makeSelected(msg: (String(kObserverFormat) + kOnId.replacingOccurrences(of: "suite", with: "败")) + "\(showTableUserName)" + (kAppColorName.replacingOccurrences(of: "medium", with: "切") + "换\u{8fc7}Sock" + k_toFormat + "尝试，地址：") + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.reconnectAndType(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.doHideShared(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        UploadDisplayThen.makeSelected(msg: (String(k_positionStr) + String(kActionMoreEqualFormat)) + "\(code)" + (kEqualAssetStr.capitalized + String(k_centerArrayData)) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.tableApp(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == show_bottomSenseData {
            //: return
            return
        }
        //: if let status = DistanceAppManager.share.reachability?.connection, status != .unavailable {
        if let status = DistanceAppManager.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.reconnectAndType(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func disableRetry(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = DistanceAppManager.share.appConfigMode.ws
        self.linkAddrArr = DistanceAppManager.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = demonstrateArr()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: noti_recordText)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = DistanceAppManager.share.loginUid
        let uid = DistanceAppManager.share.loginUid
        //: let sid = DistanceAppManager.share.loginSessionId
        let sid = DistanceAppManager.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(k_nameViewData.suffix(6))) + "\(sid)" + (String(k_videoPhotoUrl)) + "\(uid)" + (String(k_giftName.suffix(6)) + "orm=ip" + kImageDataText.replacingOccurrences(of: "head", with: "e") + "&version" + k_plainStr.capitalized) + "\(mainConversationName)" + (String(k_cellData.suffix(5)) + "ageId" + String(k_errorShadowUrl)) + "\(const_userName)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func demonstrateArr() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return main_failData ? [(String(k_mediumMessage.suffix(7)) + String(k_modelId.prefix(3))) + "\(main_keyValue)" + (String(kPositionStr.prefix(4)) + ":776" + String(k_enableTitle))] : [(k_withContent.capitalized + "9.185" + k_arrayToolUrl.capitalized + String(k_labData))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension ListSocketManager {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func dataBy(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            UploadDisplayThen.makeSelected(msg: String(bytes: k_totalPointName.map{labelUser(arc: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.tableApp(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.reconnectAndType(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func queryUpCloseInTinkleMessage() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.reconnectAndType(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.route()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension ListSocketManager {
    /// socket保活心跳包
    //: private func sendPing() {
    private func turnWithSkitter() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: const_failScreenValue, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.pathTab(info: [(k_toolKey.replacingOccurrences(of: "cell", with: "m")): (k_succeedFormat.replacingOccurrences(of: "pic", with: "ng"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func exceptTop() {
        //: endTalkingPing()
        someColor()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: k_halfButtonValue, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isSpace || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currMyVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currMyVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isSpace || (self.selectStart()?.isKind(of: KeyDataViewController.self) == false && self.selectStart()?.isKind(of: MakeViewController.self) == false) && (self.currMyVC?.isKind(of: KeyDataViewController.self) == false && self.currMyVC?.isKind(of: MakeViewController.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.pathTab(info: [(k_toolKey.replacingOccurrences(of: "cell", with: "m")): (String(kEqualKey) + String(k_viewFormat))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func someColor() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func route() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(kSizeStr) + String(kLoadMessage))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < notiTableName {
                //: DistanceAppManager.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                DistanceAppManager.share.recieve(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension ListSocketManager {
    /// 开启直播心跳包
    //: func startLivePing() {
    func withPing() {
        //: endLivePing()
        endBe()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: main_viewValue, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.pathTab(info: [(k_toolKey.replacingOccurrences(of: "cell", with: "m")): (k_itemPath.lowercased() + "HeartB" + kUserTitle.replacingOccurrences(of: "size", with: "e"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func endBe() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension ListSocketManager {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func measureViewBuildMiniskirt() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = MyContainerView()
        //: let keyWindow = UserTextMacroDefine.getWindow()
        let keyWindow = UserTextMacroDefine.clearstoryNo()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isSpace
        self.containerView?.isHidden = !self.isSpace
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func bindTo() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard ValueThen.kindShared().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard CoverManager.byRest().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension ListSocketManager {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func tableApp(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(k_makeGiftUrl.suffix(6)) + "ocket"))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: show_bottomSenseData, reason: (String(k_listFormat.suffix(6)) + "er lo" + String(kContentResultKey.suffix(5)) + k_labStr.replacingOccurrences(of: "target", with: "t")))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        someColor()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            bindTo()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func stateAction() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = ListSocketManager.shared.disableRetry(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.reconnectAndType(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func reconnectAndType(timeInterval: TimeInterval, type: WillItem) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            UploadDisplayThen.makeSelected(msg: (k_followingMsg.replacingOccurrences(of: "succeed", with: "T") + kReportText.replacingOccurrences(of: "name", with: " ")) + "\(type.rawValue)" + (String(k_modelKeyPath)) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.spectaclesStr(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func spectaclesStr(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                UploadDisplayThen.makeSelected(msg: (String(k_infoScreenUrl) + String(k_mainStr)))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                UploadDisplayThen.makeSelected(msg: (String(k_frameUrl.suffix(8)) + k_countStyleProMessage.replacingOccurrences(of: "bar", with: "接") + "不需要重" + kKeyName))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isSpace {
        if self.isSpace {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(kFirstUrl.prefix(6)) + String(kMessageDismissStretchContent.suffix(3))))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(kViewPath.suffix(6))) + "\(UserViewToolThen.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        tableApp(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = noti_countPageData
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        UploadDisplayThen.makeSelected(msg: (String(kValueMessage) + String(k_backgroundMainStr)) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func pathTab(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if DistanceAppManager.share.reachability?.connection == nil || DistanceAppManager.share.reachability?.connection == .unavailable {
        if DistanceAppManager.share.reachability?.connection == nil || DistanceAppManager.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(k_toolKey.replacingOccurrences(of: "cell", with: "m")): (String(kEndId) + kPlayerMsg.replacingOccurrences(of: "data", with: "ed")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (k_contentMatchDefineName.replacingOccurrences(of: "feedback", with: "rr")): "\(app_buttonClickData)",
                                       //: "data": [:]]
                                       (k_makeValue.replacingOccurrences(of: "layer", with: "at")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            doHideShared(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(k_toolKey.replacingOccurrences(of: "cell", with: "m"))] as? String == (k_succeedFormat.replacingOccurrences(of: "pic", with: "ng")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                UploadDisplayThen.makeSelected(msg: (String(kEqualTurnMessage) + String(kCollectionData.prefix(3))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(k_toolKey.replacingOccurrences(of: "cell", with: "m"))] as? String == (String(kEqualKey) + String(k_viewFormat)) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                UploadDisplayThen.makeSelected(msg: String(bytes: k_modeStr.map{labelError(interaction: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(k_toolKey.replacingOccurrences(of: "cell", with: "m"))] as? String == (k_itemPath.lowercased() + "HeartB" + kUserTitle.replacingOccurrences(of: "size", with: "e")) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                UploadDisplayThen.makeSelected(msg: String(bytes: k_aboutStr.map{filterLoad(make: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(k_toolKey.replacingOccurrences(of: "cell", with: "m"))] as? String == (k_succeedFormat.replacingOccurrences(of: "pic", with: "ng")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(k_toolKey.replacingOccurrences(of: "cell", with: "m")): (String(kClearStr)),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (k_contentMatchDefineName.replacingOccurrences(of: "feedback", with: "rr")): "\(dataPopName)",
                                           //: "data": [:]]
                                           (k_makeValue.replacingOccurrences(of: "layer", with: "at")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                doHideShared(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.reconnectAndType(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func doHideShared(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(k_toolKey.replacingOccurrences(of: "cell", with: "m"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(k_contentMatchDefineName.replacingOccurrences(of: "feedback", with: "rr"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(String(k_titleIndexValue))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(k_makeValue.replacingOccurrences(of: "layer", with: "at"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (kSessionMessage + "：cmdN" + String(k_addMessage.suffix(4))) + "\(cmdName)" + (String(kSubmitHeadText.prefix(4)) + "rNo:") + "\(errorNo)" + (String(kCenterStr.prefix(6))) + "\(msg)" + (String(k_fileName)) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        constraintLive(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            UploadDisplayThen.makeSelected(msg: (k_endFormat.replacingOccurrences(of: "background", with: "K") + "T 接收" + k_iconKey + String(kTextFormat.suffix(6))) + "\(cmdName)" + (String(kSubmitHeadText.prefix(4)) + "rNo:") + "\(errorNo)" + (String(kCenterStr.prefix(6))) + "\(msg)" + (String(k_fileName)) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: DistanceAppManager.share.loginUserMode.freeCallTimes = 0
                DistanceAppManager.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !DistanceAppManager.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !DistanceAppManager.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                FrameReactiveCompatible.shared.clickTo()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.socket(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(k_infoShowGestureTitle) + kSubmitTitle.replacingOccurrences(of: "recording", with: "t")) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: app_noValue, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(kRawName)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.interval(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(k_messageId.suffix(5)) + "uestCa" + String(kVoiceName)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.ingot(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(kToText)) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(kModelValue.prefix(3)) + String(k_pathContent)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (k_transformMsg.replacingOccurrences(of: "to", with: "o") + String(k_beanName.suffix(4)) + "eptCall") { // 接收方发送acceptCall回执
            //: self.isSpace = true
            self.isSpace = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            exceptTop()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.addUser(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(kViewUrl) + String(kImageStr)) { // 接收方发送stopCall回执
            //: self.isSpace = false
            self.isSpace = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            someColor()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(k_executeMessage.prefix(5)) + String(kPostUrl.suffix(8)))] as? Int {
                //: DistanceAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
                DistanceAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: DistanceAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            DistanceAppManager.share.loginUserMode.mf_coin = json[(k_makeValue.replacingOccurrences(of: "layer", with: "at"))][(String(k_areaValue))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: constEventValue, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (TouchMakeReactiveCompatible.shared as! TouchMakeReactiveCompatible).equalReport()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(kCookieMsg.suffix(7)) + String(kDataName)) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.currencyHide(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (kPostBlockKey.replacingOccurrences(of: "user", with: "ac") + "ptCall") { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isSpace = true
            self.isSpace = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            exceptTop()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.positionData(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(k_collectionViewPath.prefix(6)) + kMakeMessage.replacingOccurrences(of: "transaction", with: "ll")) { // 接收方/拨打方 挂断了通话
            //: self.isSpace = false
            self.isSpace = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            someColor()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.entitle(data: data)
            // 更新余额
            //: DistanceAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            DistanceAppManager.share.loginUserMode.mf_coin = json[(k_makeValue.replacingOccurrences(of: "layer", with: "at"))][(String(k_areaValue))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            allowView(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: constEventValue, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (TouchMakeReactiveCompatible.shared as! TouchMakeReactiveCompatible).equalReport()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (k_modelDataKey.replacingOccurrences(of: "with", with: "u") + "stCall") { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: app_plainValue, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: DistanceAppManager.share.recieve1v1TalkRequest(info: data)
                DistanceAppManager.share.recieve(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(kSizeStr) + String(kLoadMessage))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: k_appText.map{reloadIndex(manager: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(k_makeValue.replacingOccurrences(of: "layer", with: "at"))][(kLeadingFormat.replacingOccurrences(of: "on", with: "y"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: kPhotoText.map{valueTip(define: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                RegisterDelegateHelper.equalClick(uid: String(json[(k_makeValue.replacingOccurrences(of: "layer", with: "at"))][(k_viewData.replacingOccurrences(of: "of", with: "d"))].intValue),
                                             //: title: data["nickname"] as? String,
                                             title: data[(kSourceFormat.replacingOccurrences(of: "rank", with: "ic"))] as? String,
                                             //: body: body,
                                             body: body,
                                             //: imageUrl: data["headPic"] as? String)
                                             imageUrl: data[(k_imageFormat.replacingOccurrences(of: "style", with: "h") + String(kModelTitle))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                RegisterDelegateHelper.shared.backCustom()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(k_showEqualItemTitle.suffix(6)) + String(k_labelPath) + String(kHiddenKey)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isSpace = true
            self.isSpace = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            exceptTop()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(k_showEqualItemTitle.suffix(6)) + String(k_labelPath) + String(kHiddenKey))] = "1"
            //: DistanceAppManager.share.recieve1v1TalkResumeRequest(info: resumeData)
            DistanceAppManager.share.postContent(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (TouchMakeReactiveCompatible.shared as! TouchMakeReactiveCompatible).gapAdd()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (k_resFormat.replacingOccurrences(of: "track", with: "ca") + "elCall") { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isSpace = false
            self.isSpace = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            someColor()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.motivation(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                RegisterDelegateHelper.local(identifier: String(json[(k_makeValue.replacingOccurrences(of: "layer", with: "at"))][(k_viewData.replacingOccurrences(of: "of", with: "d"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                RegisterDelegateHelper.shared.equality()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (kDirectionName.replacingOccurrences(of: "effect", with: "ef") + "seCall") { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isSpace = false
            self.isSpace = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            someColor()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.fosterling(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(k_removeFormat.suffix(5)) + "lMot" + kLifeUrl.replacingOccurrences(of: "edge", with: "va") + String(k_screenData)) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if DistanceAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.life?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(k_normalPath) + String(kMenuClickStr) + kValidFormat.replacingOccurrences(of: "success", with: "in")) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            ValueThen.equalFuturism(content: msg, reason: json[(k_makeValue.replacingOccurrences(of: "layer", with: "at"))][(k_valueBottomTitleId.replacingOccurrences(of: "constraint", with: "o") + k_theName.replacingOccurrences(of: "raw", with: "n"))].stringValue, time: json[(k_makeValue.replacingOccurrences(of: "layer", with: "at"))][(kInfoValue.replacingOccurrences(of: "layer", with: "me"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(kRecordSubmitContentPath.suffix(4)) + "opLiv" + kVoiceKey.replacingOccurrences(of: "text", with: "e")) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ValueThen.kindShared().feeDoing()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(k_moreStr.suffix(5)) + String(kAppKey.prefix(4)) + String(k_styleStatusValue)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.rear(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(kModelPath.prefix(8))) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(kContactContent.replacingOccurrences(of: "at", with: "oa") + "dStat" + k_modeShowMsg.replacingOccurrences(of: "content", with: "u"))] as? Int ?? -1
            //: if DistanceAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if DistanceAppManager.share.appStatus == NameQuickLookable.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: dataKeyTitle, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: app_noValue, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: dataKeyTitle, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(kLineContent) + String(kTextKey)) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            DirectorDanmuManager.blockCurrent().sequenceData(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(k_lessFormat.suffix(6)) + "ficat" + String(kStrengthMsg.prefix(5)) + "pupWi" + String(k_sizeId)) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            FrameReactiveCompatible.shared.afterDic(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(kPlayerId) + k_lineTitle.replacingOccurrences(of: "add", with: "k")) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ValueThen.kindShared().feeDoing()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            CoverManager.byRest().voiceOverShared()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: notiButtonData, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension ListSocketManager {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func constraintLive(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (k_modelDataKey.replacingOccurrences(of: "with", with: "u") + "stCall") || cmdName == (String(k_messageId.suffix(5)) + "uestCa" + String(kVoiceName)) {
            //: if TalkingLiveManager.shared().isLive == true {
            if ValueThen.kindShared().isLive == true {
                //: NEED_RESTORE_LIVE = true
                app_limitScaleText = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                ValueThen.mentalPictureStart(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                ValueThen.kindShared().feeDoing()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard app_limitScaleText == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (k_resFormat.replacingOccurrences(of: "track", with: "ca") + "elCall") || cmdName == (String(kToText)) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (kDirectionName.replacingOccurrences(of: "effect", with: "ef") + "seCall") || cmdName == (String(kModelValue.prefix(3)) + String(k_pathContent)) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(k_collectionViewPath.prefix(6)) + kMakeMessage.replacingOccurrences(of: "transaction", with: "ll")) || cmdName == (String(kViewUrl) + String(kImageStr))
        {
            //: NEED_RESTORE_LIVE = false
            app_limitScaleText = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = CancelReactiveCompatible(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                ValueThen.kindShared().phoneHandler()
            }
            //: alert.show()
            alert.infoShow()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func allowView(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(k_executeMessage.prefix(5)) + String(kPostUrl.suffix(8)))] as? Int {
            //: DistanceAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            DistanceAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(k_tableSharedText.lowercased() + "CallWin")] as? Bool
        //: if freeCallWin == true && DistanceAppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = ServerMakeErrorDelegate()
            //: view.show()
            view.pressAppear()
            //: view.setShowData(data: data)
            view.bestowmentData(data: data)
        }
    }
}
