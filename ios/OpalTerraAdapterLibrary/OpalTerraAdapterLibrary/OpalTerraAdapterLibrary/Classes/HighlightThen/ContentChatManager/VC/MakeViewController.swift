
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kOfKey:[UInt8] = [0x13,0x14,0x13,0xe,0x52,0x19,0x15,0x1e,0x1f,0x8,0x40,0x53,0x5a,0x12,0x1b,0x9,0x5a,0x14,0x15,0xe,0x5a,0x18,0x1f,0x1f,0x14,0x5a,0x13,0x17,0xa,0x16,0x1f,0x17,0x1f,0x14,0xe,0x1f,0x1e]

private func truckBed(file num: UInt8) -> UInt8 {
    return num ^ 122
}

/*: "uid" :*/
fileprivate let k_endValue:[UInt8] = [0x23,0x3f,0x32]

private func effectNeed(cell num: UInt8) -> UInt8 {
    return num ^ 86
}

/*: "headPic" :*/
fileprivate let k_mainMessage:String = "headPimage icon message user let"
fileprivate let kSubmitData:String = "itarget"

/*: "nickname" :*/
fileprivate let kKitFormat:String = "nickndateme"

/*: "birthday" :*/
fileprivate let k_indexUrl:[Character] = ["b","i","r","t","h","d","a","y"]

/*: "videoUrl" :*/
fileprivate let k_withId:[Character] = ["v","i","d","e","o"]
fileprivate let kManagerMessage:String = "user arrayUrl"

/*: "location" :*/
fileprivate let kNameActualStr:String = "lreca"

/*: "cmd" :*/
fileprivate let k_viewPath:[UInt8] = [0xbb,0xb5,0xbc]

private func sessionFeed(background num: UInt8) -> UInt8 {
    return num ^ 216
}

/*: "refuseCall" :*/
fileprivate let k_backUrl:[Character] = ["r","e","f","u","s","e"]
fileprivate let kImageText:[Character] = ["C","a","l","l"]

/*: "data" :*/
fileprivate let k_viewToNamePath:[UInt8] = [0x2b,0x2e,0x3b,0x2e]

private func signTop(random num: UInt8) -> UInt8 {
    return num ^ 79
}

/*: "Call interruption" :*/
fileprivate let kNameCookieData:[Character] = ["C","a","l","l"," ","i","n"]
fileprivate let kModelIndexPath:String = "TER"
fileprivate let kPointStr:[Character] = ["r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let k_userEffectPlayerMsg:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "logId" :*/
fileprivate let k_viewId:[UInt8] = [0x6d,0x6e,0x66,0x48,0x65]

/*: "cancelCall" :*/
fileprivate let kTextFormat:String = "ciconncel"

/*: "No answer" :*/
fileprivate let k_minValueTitle:String = "No ansof from value"
fileprivate let k_editName:[Character] = ["w","e","r"]

/*: "duration" :*/
fileprivate let k_whiteMsg:String = "duratitaskn"

/*: "She has hung up" :*/
fileprivate let k_makeName:String = "gift interaction log viewShe "
fileprivate let kRegularTitle:[Character] = ["n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let k_collectionText:String = "key"
fileprivate let kColorUrl:String = "name messageancelSex"

/*: "He has hung up" :*/
fileprivate let kCreateerData:[Character] = ["H","e"," ","h","a","s"," ","h"]
fileprivate let kMessageData:String = "ung upmore max self gift"

/*: "endType" :*/
fileprivate let kActualMsg:String = "endTypuser view make"
fileprivate let kTopUrl:String = "white"

/*: "stopSex" :*/
fileprivate let k_pageId:[Character] = ["s"]
fileprivate let kRequestData:[Character] = ["t","o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let kMakeAddStr:[UInt8] = [0x64,0x65,0x64,0x6e,0x65,0x20,0x73,0x61,0x68,0x20,0x6c,0x6c,0x61,0x63,0x20,0x2c,0x73,0x6e,0x69,0x6f,0x63,0x20,0x64,0x6c,0x6f,0x67,0x20,0x74,0x6e,0x65,0x69,0x63,0x69,0x66,0x66,0x75,0x73,0x6e,0x49]

/*: "The other hung up the phone" :*/
fileprivate let kUserTitle:String = "The omake extension put"
fileprivate let kTextMessage:String = "type if equalther "
fileprivate let kNumberData:String = " up thenone system view color frame"
fileprivate let k_actionUrl:String = " phonearray else content value bean"

/*: "type" :*/
fileprivate let k_timeMessage:String = "ttablee"

/*: "jumpUrl" :*/
fileprivate let k_imageIndexData:[Character] = ["j","u","m","p","U"]
fileprivate let k_equalMakeValue:String = "rel"

/*: "Cancel" :*/
fileprivate let k_requestData:[Character] = ["C","a","n","c","e","l"]

/*: "Get gold coins" :*/
fileprivate let k_logReasonVarTitle:String = "page background self key labGet g"
fileprivate let k_changeFormat:String = "oirest"

/*: "addBean" :*/
fileprivate let k_tempKey:String = "addBeof tag target"
fileprivate let kDomainMessage:[Character] = ["a","n"]

/*: "refuseSex" :*/
fileprivate let k_managerJumpStr:String = "value"
fileprivate let k_ofKey:[Character] = ["e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let k_listContent:String = "onAccepstop bottom first touch"
fileprivate let k_textData:[Character] = ["t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let k_cellPath:String = "acceptCview string path"
fileprivate let kIconVideoFormat:[Character] = ["a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let k_strengthName:String = "Calls file return size live"
fileprivate let kMakeMsg:[Character] = ["a","r","e"," ","c"]
fileprivate let kTopText:String = "agiftelled"

/*: "The call has ended" :*/
fileprivate let kPhotoMessageMsg:String = "gift varTh"
fileprivate let k_keyMsg:String = "extension ife cal"
fileprivate let kTextMsg:String = " endedback front"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MakeViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let noti_appData = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class MakeViewController: StartThen {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ViewChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ModePlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: ListMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        ListSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ListSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ListSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kOfKey.map{truckBed(file: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! MissiveThen
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.fromSub()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            bottomAdd()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currMyVC = nil
            ListSocketManager.shared.currMyVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.bestowal()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        addOpen()
        //: setupSubViewsConstraint()
        isIcon()
        //: initNotificationObserver()
        shrugOn()
        //: req_callGetUserInfo()
        videoImage()
        //: timeoutRules()
        bySub()
        //: resumeTalk()
        resumeVoice()
        //: setUpBGMPlayer()
        compoundPlayer()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if DistanceAppManager.share.appConfigMode.disableShootScreen {
            if DistanceAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ValueShieldView(frame: CGRect(x: 0, y: 0, width: constFormalContent, height: user_recordMarginValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isSpace = false
        ListSocketManager.shared.isSpace = false
        //: self.stopVCAllActivties()
        self.activtiesConstraint()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: FatalReactiveCompatible = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = FatalReactiveCompatible(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: PopCloudDelegate = {
        //: let m = TalkingVoiceChatManager()
        let m = PopCloudDelegate()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension MakeViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func videoImage() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ContentChatManager.physicsLaboratory(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: k_endValue.map{effectNeed(cell: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(k_mainMessage.prefix(5)) + kSubmitData.replacingOccurrences(of: "target", with: "c"))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(kKitFormat.replacingOccurrences(of: "date", with: "a"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(k_indexUrl))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(k_withId) + String(kManagerMessage.suffix(3)))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(kNameActualStr.replacingOccurrences(of: "re", with: "o") + "tion")].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.fosterChildModel()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension MakeViewController {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func bottomAdd() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        ToolThen.cellTact(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.arcInfo(showMsg: main_intimateName)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ListSocketManager.shared.pathTab(info: [String(bytes: k_viewPath.map{sessionFeed(background: $0)}, encoding: .utf8)!: (String(k_backUrl) + String(kImageText)), String(bytes: k_viewToNamePath.map{signTop(random: $0)}, encoding: .utf8)!: [String(bytes: k_endValue.map{effectNeed(cell: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.abstractEntity()
                //: return
                return
            }

            //: self.stopBGM()
            self.pivotingWindow()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ModePlayer.medicamentCheck(type: .Response)
            //: self.startBGM()
            self.fromSub()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func activtiesConstraint() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.pivotingWindow()
        //: TalkingSocketManager.shared.callerDelegate = nil
        ListSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        ListSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        ListSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func abstractEntity(animated: Bool = true) {
        //: self.stopBGM()
        self.pivotingWindow()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func incorporate() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.gazette(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func resumeVoice() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            incorporate()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension MakeViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func shrugOn() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(atPage(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(giftNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: k_halfData,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        noti_appData.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func atPage(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            abstractEntity()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func giftNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard ListSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        ListSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ListSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ListSocketManager.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.multi(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension MakeViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.arcInfo(showMsg: (String(kNameCookieData) + kModelIndexPath.lowercased() + String(kPointStr)).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ListSocketManager.shared.pathTab(info: [String(bytes: k_viewPath.map{sessionFeed(background: $0)}, encoding: .utf8)!: (String(k_userEffectPlayerMsg)), String(bytes: k_viewToNamePath.map{signTop(random: $0)}, encoding: .utf8)!: [String(bytes: k_viewId.map{$0^1}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                ListSocketManager.shared.pathTab(info: [String(bytes: k_viewPath.map{sessionFeed(background: $0)}, encoding: .utf8)!: (kTextFormat.replacingOccurrences(of: "icon", with: "a") + "Call"), String(bytes: k_viewToNamePath.map{signTop(random: $0)}, encoding: .utf8)!: [String(bytes: k_viewId.map{$0^1}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ListSocketManager.shared.pathTab(info: [String(bytes: k_viewPath.map{sessionFeed(background: $0)}, encoding: .utf8)!: (String(k_backUrl) + String(kImageText)), String(bytes: k_viewToNamePath.map{signTop(random: $0)}, encoding: .utf8)!: [String(bytes: k_endValue.map{effectNeed(cell: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.spiralBlindfoldLacrimalApparatusWithoutAdjourn()
                //: TalkingSocketManager.shared.currMyVC = nil
                ListSocketManager.shared.currMyVC = nil
            }
            //: popCurrentViewController()
            abstractEntity()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.disburden(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension MakeViewController {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func compoundPlayer() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? HighlightExpressionConvertible.Call : HighlightExpressionConvertible.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ModePlayer.medicamentCheck(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func fromSub() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.nap()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func pivotingWindow() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.liveTo()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension MakeViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func bySub() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? showLimitHomeText : self.chatModel?.keepUponInstance()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            heliogram()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.heliogram()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func heliogram() {
        //: guard !TalkingSocketManager.shared.isSpace else { return }
        guard !ListSocketManager.shared.isSpace else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.arcInfo(showMsg: (String(k_minValueTitle.prefix(6)) + String(k_editName)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: k_viewPath.map{sessionFeed(background: $0)}, encoding: .utf8)!: (kTextFormat.replacingOccurrences(of: "icon", with: "a") + "Call"), String(bytes: k_viewToNamePath.map{signTop(random: $0)}, encoding: .utf8)!: [String(bytes: k_viewId.map{$0^1}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: k_viewPath.map{sessionFeed(background: $0)}, encoding: .utf8)!: (String(k_backUrl) + String(kImageText)), String(bytes: k_viewToNamePath.map{signTop(random: $0)}, encoding: .utf8)!: [String(bytes: k_endValue.map{effectNeed(cell: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        ListSocketManager.shared.pathTab(info: socketDict)
        //: popCurrentViewController()
        abstractEntity()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension MakeViewController {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func hiddenResume(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: k_viewId.map{$0^1}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(k_whiteMsg.replacingOccurrences(of: "task", with: "o"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.mentalFacultyTime(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.reActionStage()
            //: createTalkRoom()
            incorporate()
            //: self.stopBGM()
            self.pivotingWindow()
        }
    }
}

// MARK: - ItemCallerDelegate【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension MakeViewController: ItemCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func motivation(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(k_makeName.suffix(4)) + "has hu" + String(kRegularTitle)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(k_collectionText.replacingOccurrences(of: "key", with: "c") + String(kColorUrl.suffix(8)))].stringValue == ManagerQuickLookable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(kCreateerData) + String(kMessageData.prefix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.arcInfo(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.spiralBlindfoldLacrimalApparatusWithoutAdjourn()
            //: TalkingSocketManager.shared.currMyVC = nil
            ListSocketManager.shared.currMyVC = nil
        }

        //: popCurrentViewController()
        abstractEntity()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func positionData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: k_viewId.map{$0^1}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.pivotingWindow()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.reActionStage()
        //: self.createTalkRoom()
        self.incorporate()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func entitle(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(kActualMsg.prefix(6)) + kTopUrl.replacingOccurrences(of: "white", with: "e"))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(k_makeName.suffix(4)) + "has hu" + String(kRegularTitle)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(k_pageId) + String(kRequestData))].stringValue == ManagerQuickLookable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(kCreateerData) + String(kMessageData.prefix(6))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if DistanceAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if DistanceAppManager.share.appStatus == NameQuickLookable.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: kMakeAddStr.reversed(), encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(kUserTitle.prefix(5)) + String(kTextMessage.suffix(5)) + "hung" + String(kNumberData.prefix(7)) + String(k_actionUrl.prefix(6))).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.arcInfo(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.spiralBlindfoldLacrimalApparatusWithoutAdjourn()
            //: TalkingSocketManager.shared.currMyVC = nil
            ListSocketManager.shared.currMyVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.inheritance()
        //: self.popCurrentViewController()
        self.abstractEntity()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: constEventValue, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func currencyHide(msg: String, data: [String: Any]) {
        //: guard DistanceAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard DistanceAppManager.share.appStatus == NameQuickLookable.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(k_timeMessage.replacingOccurrences(of: "table", with: "yp"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(k_imageIndexData) + k_equalMakeValue.replacingOccurrences(of: "re", with: "r"))] as? String else { return }
            //: let httpsUrl = DistanceAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = DistanceAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = AppConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: AddReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            AddReactiveCompatible.share.pathAcross(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            FrameHiddenReactiveCompatible.theory(title: nil,
                                    //: message: msg,
                                    message: msg,
                                    //: leftBtnTitle: "Cancel".localized,
                                    leftBtnTitle: (String(k_requestData)).localized,
                                    //: rightBtnTitle: "Get gold coins".localized) {
                                    rightBtnTitle: (String(k_logReasonVarTitle.suffix(5)) + "old c" + k_changeFormat.replacingOccurrences(of: "rest", with: "ns")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                FrameHiddenReactiveCompatible.middleText()
                //: } rightBlock: {
            } rightBlock: {
                //: AddReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                AddReactiveCompatible.share.viewParams(clickEvent: show_restoreName, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func life(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(k_tempKey.prefix(5)) + String(kDomainMessage))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.valuePoint(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.loadTeam(point: addBean)
        }
    }
}

// MARK: - VisualCommunicationObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension MakeViewController: VisualCommunicationObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func fosterling(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(k_makeName.suffix(4)) + "has hu" + String(kRegularTitle)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(k_managerJumpStr.replacingOccurrences(of: "value", with: "r") + String(k_ofKey))].stringValue == ManagerQuickLookable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(kCreateerData) + String(kMessageData.prefix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.arcInfo(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.spiralBlindfoldLacrimalApparatusWithoutAdjourn()
            //: TalkingSocketManager.shared.currMyVC = nil
            ListSocketManager.shared.currMyVC = nil
        }
        //: popCurrentViewController()
        abstractEntity()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func addUser(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: k_viewId.map{$0^1}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.pivotingWindow()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.reActionStage()
        //: self.createTalkRoom()
        self.incorporate()
    }
}

// MARK: - PerformanceErrorDelegate【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension MakeViewController: PerformanceErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func socket(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(k_backUrl) + String(kImageText)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.arcInfo(showMsg: (String(k_minValueTitle.prefix(6)) + String(k_editName)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.spiralBlindfoldLacrimalApparatusWithoutAdjourn()
                //: TalkingSocketManager.shared.currMyVC = nil
                ListSocketManager.shared.currMyVC = nil
            }
            //: self.popCurrentViewController()
            self.abstractEntity()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(k_listContent.prefix(7)) + String(k_textData)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard DistanceAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard DistanceAppManager.share.appStatus == NameQuickLookable.normal.rawValue else { return }
            //: AddReactiveCompatible.share.func__jumpToWebRecharge(sufficient: false)
            AddReactiveCompatible.share.viewParams(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(k_listContent.prefix(7)) + String(k_textData)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.arcInfo(showMsg: msg)
            //: self.popCurrentViewController()
            self.abstractEntity()
        }
    }
}

// MARK: - ViewObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension MakeViewController: ViewObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func shouldTagViewCounteractionJudge() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        ListSocketManager.shared.pathTab(info: [String(bytes: k_viewPath.map{sessionFeed(background: $0)}, encoding: .utf8)!: (String(k_cellPath.prefix(7)) + String(kIconVideoFormat)), String(bytes: k_viewToNamePath.map{signTop(random: $0)}, encoding: .utf8)!: [String(bytes: k_endValue.map{effectNeed(cell: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func cleanRemove() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.arcInfo(showMsg: (String(k_strengthName.prefix(6)) + String(kMakeMsg) + kTopText.replacingOccurrences(of: "gift", with: "nc")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            ListSocketManager.shared.pathTab(info: [String(bytes: k_viewPath.map{sessionFeed(background: $0)}, encoding: .utf8)!: (kTextFormat.replacingOccurrences(of: "icon", with: "a") + "Call"), String(bytes: k_viewToNamePath.map{signTop(random: $0)}, encoding: .utf8)!: [String(bytes: k_viewId.map{$0^1}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.arcInfo(showMsg: (String(k_strengthName.prefix(6)) + String(kMakeMsg) + kTopText.replacingOccurrences(of: "gift", with: "nc")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            ListSocketManager.shared.pathTab(info: [String(bytes: k_viewPath.map{sessionFeed(background: $0)}, encoding: .utf8)!: (String(k_backUrl) + String(kImageText)), String(bytes: k_viewToNamePath.map{signTop(random: $0)}, encoding: .utf8)!: [String(bytes: k_endValue.map{effectNeed(cell: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.arcInfo(showMsg: (String(kPhotoMessageMsg.suffix(2)) + String(k_keyMsg.suffix(5)) + "l has" + String(kTextMsg.prefix(6))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            ListSocketManager.shared.pathTab(info: [String(bytes: k_viewPath.map{sessionFeed(background: $0)}, encoding: .utf8)!: (String(k_userEffectPlayerMsg)), String(bytes: k_viewToNamePath.map{signTop(random: $0)}, encoding: .utf8)!: [String(bytes: k_viewId.map{$0^1}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.inheritance()
        //: self.popCurrentViewController()
        self.abstractEntity()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func topState(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.slowingAction(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func nameHandsfree(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.follow(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func stormSash() {
        //: TalkingSocketManager.shared.currMyVC = self
        ListSocketManager.shared.currMyVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = ListMiniView.activityView()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.selectStart()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.currentTime()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.subsequently(talkingTime)
        //: popCurrentViewController()
        abstractEntity()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func counteractionJoinExample(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.subsequently(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func derive(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.masthead(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension MakeViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func addOpen() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func isIcon() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
