
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kKitValue:[UInt8] = [0x44,0x43,0x44,0x59,0x5,0x4e,0x42,0x49,0x48,0x5f,0x17,0x4,0xd,0x45,0x4c,0x5e,0xd,0x43,0x42,0x59,0xd,0x4f,0x48,0x48,0x43,0xd,0x44,0x40,0x5d,0x41,0x48,0x40,0x48,0x43,0x59,0x48,0x49]

private func linguaFranca(cell num: UInt8) -> UInt8 {
    return num ^ 45
}

/*: "uid" :*/
fileprivate let k_viewArcStr:[UInt8] = [0x19,0x5,0x8]

/*: "headPic" :*/
fileprivate let k_timeMsg:String = "on"
fileprivate let kAppMsg:String = "make enable title or leadingeadPic"

/*: "nickname" :*/
fileprivate let k_userData:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let k_pathName:String = "birthrangeay"

/*: "videoUrl" :*/
fileprivate let k_momentTitle:[Character] = ["v","i","d","e","o","U","r"]
fileprivate let kWithLicenseName:String = "value"

/*: "cover" :*/
fileprivate let k_viewFormat:String = "covlanguage"

/*: "location" :*/
fileprivate let kCurrentValue:String = "lsharea"

/*: "cmd" :*/
fileprivate let k_blockValueMessage:[UInt8] = [0x88,0x92,0x89]

fileprivate func submitAdd(max num: UInt8) -> UInt8 {
    let value = Int(num) + 219
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let kTableStr:String = "title"
fileprivate let kNoseGestureId:String = "gender transform labelfuseCall"

/*: "data" :*/
fileprivate let k_makeTitle:[UInt8] = [0x82,0x87,0x92,0x87]

/*: "Call interruption" :*/
fileprivate let k_errorKey:[Character] = ["C","a","l","l"," ","i","n","t","e","r","r","u"]
fileprivate let kAppPartyName:String = "pfemaleon"

/*: "stopCall" :*/
fileprivate let kViewId:String = "view ping key style textstopCall"

/*: "logId" :*/
fileprivate let kRequestButtonMsg:[UInt8] = [0xed,0xee,0xe6,0xc8,0xe5]

private func roundTap(app num: UInt8) -> UInt8 {
    return num ^ 129
}

/*: "cancelCall" :*/
fileprivate let k_noseUrl:String = "CANCE"

/*: "No answer" :*/
fileprivate let kIconValue:[Character] = ["N","o"," ","a","n","s","w","e"]
fileprivate let kBeautyName:String = "R"

/*: "duration" :*/
fileprivate let kSubmitPath:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let kHiddenPath:String = "if super touch frame behaviorShe h"
fileprivate let k_tableValue:[Character] = ["a","s"," ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let k_addMsg:String = "cagift"
fileprivate let kToInviteMessage:String = "view lastcelSex"

/*: "He has hung up" :*/
fileprivate let k_styleUrl:String = "item cellHe has"
fileprivate let kTableText:String = " upcontent actual equal path"

/*: "endType" :*/
fileprivate let k_sendId:String = "normal forendType"

/*: "stopSex" :*/
fileprivate let kKitTitle:String = "button falsestopSex"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let k_shareUrl:[UInt8] = [0x2,0x25,0x38,0x3e,0x2d,0x2d,0x22,0x28,0x22,0x2e,0x25,0x3f,0x6b,0x2c,0x24,0x27,0x2f,0x6b,0x28,0x24,0x22,0x25,0x38,0x67,0x6b,0x28,0x2a,0x27,0x27,0x6b,0x23,0x2a,0x38,0x6b,0x2e,0x25,0x2f,0x2e,0x2f]

private func refreshTo(remove num: UInt8) -> UInt8 {
    return num ^ 75
}

/*: "The other hung up the phone" :*/
fileprivate let k_modelCompeteText:String = "The othercompete type view"
fileprivate let kEffectName:[Character] = [" ","h","u","n","g"," ","u","p"," ","t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let k_stageStr:String = "tmanagere"

/*: "jumpUrl" :*/
fileprivate let k_mainData:String = "view selectedjumpUrl"

/*: "Cancel" :*/
fileprivate let k_extraId:String = "style let top modelCancel"

/*: "Get gold coins" :*/
fileprivate let k_fullId:String = "Get golayer ready view true to"
fileprivate let kLessVideoImageTitle:[Character] = ["l","d"," ","c","o","i","n","s"]

/*: "addBean" :*/
fileprivate let kViewName:String = "addBeanunder balloon self the return"

/*: "refuseSex" :*/
fileprivate let kStyleStr:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let k_homeListKey:[Character] = ["o","n","A","c","c","e"]
fileprivate let k_equalPath:[Character] = ["p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let k_colorKey:String = "accelengtht"
fileprivate let kLabKey:String = "model self show manager letCall"

/*: "Calls are cancelled" :*/
fileprivate let k_floorContent:[Character] = ["C","a","l","l","s"," ","a","r"]
fileprivate let k_sectionStr:String = "male"
fileprivate let kPlayerId:String = "let type image will can can"

/*: "The call has ended" :*/
fileprivate let k_responseAddContent:String = "video equal countThe "
fileprivate let k_textData:String = "as manager space private height"
fileprivate let kResumeMessage:[Character] = ["e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  KeyDataViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let main_maxValue = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class KeyDataViewController: StartThen {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ViewChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: ModePlayer? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: EndItemMiniView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: ServerTextThen? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        ListSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ListSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ListSocketManager.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kKitValue.map{linguaFranca(cell: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! MissiveThen
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.propRoot()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            titleUse()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            reload(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currMyVC = nil
            ListSocketManager.shared.currMyVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.sodom()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        albumWithSubviews()
        //: setupSubViewsConstraint()
        dateConstraint()
        //: initNotificationObserver()
        listenerBackground()
        //: req_callGetUserInfo()
        m()
        //: timeoutRules()
        me()
        //: setUpBGMPlayer()
        buttonPlayer()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.cycleCamera()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if DistanceAppManager.share.appConfigMode.disableShootScreen {
            if DistanceAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ValueShieldView(frame: CGRect(x: 0, y: 0, width: constFormalContent, height: user_recordMarginValue))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isSpace = false
        ListSocketManager.shared.isSpace = false
        //: self.stopVCAllActivties()
        self.drawUp()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: PopThen = {
        //: let v = TalkingBGVideoView()
        let v = PopThen()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.topShow(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: AddPublishThen = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = AddPublishThen(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: CoverWindowSashView = {
        //: let v = TalkingVideoWindowView()
        let v = CoverWindowSashView()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = constFormalContent - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if MakeLanguageManager.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: main_netValue + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.tipDoing()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ContentChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = ContentChatManager()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension KeyDataViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func m() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ContentChatManager.physicsLaboratory(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: k_viewArcStr.map{$0^108}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(k_timeMsg.replacingOccurrences(of: "on", with: "h") + String(kAppMsg.suffix(6)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(k_userData))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(k_pathName.replacingOccurrences(of: "range", with: "d"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(k_momentTitle) + kWithLicenseName.replacingOccurrences(of: "value", with: "l"))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(k_viewFormat.replacingOccurrences(of: "language", with: "er"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(kCurrentValue.replacingOccurrences(of: "share", with: "oc") + "tion")].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.laterOnThroughInfo()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.changeUser()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func changeUser() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = WithCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = WithCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.nameOn(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.managerCamera(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension KeyDataViewController {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func titleUse() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        ToolThen.drunkComponent { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.arcInfo(showMsg: main_intimateName)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ListSocketManager.shared.pathTab(info: [String(bytes: k_blockValueMessage.map{submitAdd(max: $0)}, encoding: .utf8)!: (kTableStr.replacingOccurrences(of: "title", with: "re") + String(kNoseGestureId.suffix(8))), String(bytes: k_makeTitle.map{$0^230}, encoding: .utf8)!: [String(bytes: k_viewArcStr.map{$0^108}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.seek()
                //: return
                return
            }

            //: self.startPreview()
            self.cycleCamera()
            //: self.stopBGM()
            self.sinceSmall()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = ModePlayer.medicamentCheck(type: .Response)
            //: self.startBGM()
            self.propRoot()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func cycleCamera() {
        //: self.videoManager.startPreview()
        self.videoManager.preview()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func drawUp() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.sinceSmall()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        DirectorDanmuManager.doAdd()
        //: TalkingSocketManager.shared.callerDelegate = nil
        ListSocketManager.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        ListSocketManager.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        ListSocketManager.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func seek(animated: Bool = true) {
        //: self.stopBGM()
        self.sinceSmall()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func tipDoing() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.keySizeAction()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.changeUser()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func aboveApp() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.writeForPublish()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.changeUser()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.machine()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func machine() {
        //: guard DistanceAppManager.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = ServerTextThen()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.whenAnimation()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension KeyDataViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func listenerBackground() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(fromImage(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(infoUp(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: userNetText,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(phaseOfTheMoon(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: k_halfData,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        main_maxValue.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func fromImage(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            seek()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func phaseOfTheMoon(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard ListSocketManager.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        ListSocketManager.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ListSocketManager.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ListSocketManager.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.modeAvailable()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func infoUp(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.changeUser()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension KeyDataViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.arcInfo(showMsg: (String(k_errorKey) + kAppPartyName.replacingOccurrences(of: "female", with: "ti")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ListSocketManager.shared.pathTab(info: [String(bytes: k_blockValueMessage.map{submitAdd(max: $0)}, encoding: .utf8)!: (String(kViewId.suffix(8))), String(bytes: k_makeTitle.map{$0^230}, encoding: .utf8)!: [String(bytes: kRequestButtonMsg.map{roundTap(app: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                ListSocketManager.shared.pathTab(info: [String(bytes: k_blockValueMessage.map{submitAdd(max: $0)}, encoding: .utf8)!: (k_noseUrl.lowercased() + "lCall"), String(bytes: k_makeTitle.map{$0^230}, encoding: .utf8)!: [String(bytes: kRequestButtonMsg.map{roundTap(app: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ListSocketManager.shared.pathTab(info: [String(bytes: k_blockValueMessage.map{submitAdd(max: $0)}, encoding: .utf8)!: (kTableStr.replacingOccurrences(of: "title", with: "re") + String(kNoseGestureId.suffix(8))), String(bytes: k_makeTitle.map{$0^230}, encoding: .utf8)!: [String(bytes: k_viewArcStr.map{$0^108}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.spiralBlindfoldLacrimalApparatusWithoutAdjourn()
                //: TalkingSocketManager.shared.currMyVC = nil
                ListSocketManager.shared.currMyVC = nil
            }
            //: popCurrentViewController()
            seek()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension KeyDataViewController {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func buttonPlayer() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? HighlightExpressionConvertible.Call : HighlightExpressionConvertible.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = ModePlayer.medicamentCheck(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func propRoot() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.nap()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func sinceSmall() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.liveTo()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension KeyDataViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func me() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? showLimitHomeText : self.chatModel?.keepUponInstance()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            electronicImage()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.electronicImage()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func electronicImage() {
        //: guard !TalkingSocketManager.shared.isSpace else { return }
        guard !ListSocketManager.shared.isSpace else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.arcInfo(showMsg: (String(kIconValue) + kBeautyName.lowercased()).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: k_blockValueMessage.map{submitAdd(max: $0)}, encoding: .utf8)!: (k_noseUrl.lowercased() + "lCall"), String(bytes: k_makeTitle.map{$0^230}, encoding: .utf8)!: [String(bytes: kRequestButtonMsg.map{roundTap(app: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: k_blockValueMessage.map{submitAdd(max: $0)}, encoding: .utf8)!: (kTableStr.replacingOccurrences(of: "title", with: "re") + String(kNoseGestureId.suffix(8))), String(bytes: k_makeTitle.map{$0^230}, encoding: .utf8)!: [String(bytes: k_viewArcStr.map{$0^108}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        ListSocketManager.shared.pathTab(info: socketDict)
        //: popCurrentViewController()
        seek()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension KeyDataViewController {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func talk(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: kRequestButtonMsg.map{roundTap(app: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(kSubmitPath))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.cover(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.toStage()
            //: createTalkRoom()
            aboveApp()
            //: self.stopBGM()
            self.sinceSmall()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func reload(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.keySizeAction()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.topShow(show: false)
        //: self.stopBGM()
        self.sinceSmall()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.toStage()
        //: self.createTalkRoom()
        self.aboveApp()
    }
}

// MARK: - ItemCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension KeyDataViewController: ItemCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func motivation(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(kHiddenPath.suffix(5)) + String(k_tableValue)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(k_addMsg.replacingOccurrences(of: "gift", with: "n") + String(kToInviteMessage.suffix(6)))].stringValue == ManagerQuickLookable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(k_styleUrl.suffix(6)) + " hung" + String(kTableText.prefix(3))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.arcInfo(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.spiralBlindfoldLacrimalApparatusWithoutAdjourn()
            //: TalkingSocketManager.shared.currMyVC = nil
            ListSocketManager.shared.currMyVC = nil
        }

        //: popCurrentViewController()
        seek()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func positionData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        reload(logId: json[String(bytes: kRequestButtonMsg.map{roundTap(app: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func entitle(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(k_sendId.suffix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(kHiddenPath.suffix(5)) + String(k_tableValue)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(kKitTitle.suffix(7)))].stringValue == ManagerQuickLookable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(k_styleUrl.suffix(6)) + " hung" + String(kTableText.prefix(3))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if DistanceAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if DistanceAppManager.share.appStatus == NameQuickLookable.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: k_shareUrl.map{refreshTo(remove: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(k_modelCompeteText.prefix(9)) + String(kEffectName)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.arcInfo(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.spiralBlindfoldLacrimalApparatusWithoutAdjourn()
            //: TalkingSocketManager.shared.currMyVC = nil
            ListSocketManager.shared.currMyVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.pathFinancialAidDismiss()
        //: popCurrentViewController()
        seek()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func currencyHide(msg: String, data: [String: Any]) {
        //: guard DistanceAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard DistanceAppManager.share.appStatus == NameQuickLookable.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(k_stageStr.replacingOccurrences(of: "manager", with: "yp"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(k_mainData.suffix(7)))] as? String else { return }
            //: let httpsUrl = DistanceAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = DistanceAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = AppConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: AddReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            AddReactiveCompatible.share.pathAcross(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            FrameHiddenReactiveCompatible.theory(title: nil,
                                    //: message: msg,
                                    message: msg,
                                    //: leftBtnTitle: "Cancel".localized,
                                    leftBtnTitle: (String(k_extraId.suffix(6))).localized,
                                    //: rightBtnTitle: "Get gold coins".localized) {
                                    rightBtnTitle: (String(k_fullId.prefix(6)) + String(kLessVideoImageTitle)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                FrameHiddenReactiveCompatible.middleText()
                //: } rightBlock: {
            } rightBlock: {
                //: AddReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                AddReactiveCompatible.share.viewParams(clickEvent: show_restoreName, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func objectBy(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.privacy(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func life(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(kViewName.prefix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.showPoint(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.unseeable(point: addBean)
        }
    }
}

// MARK: - VisualCommunicationObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension KeyDataViewController: VisualCommunicationObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func fosterling(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(kHiddenPath.suffix(5)) + String(k_tableValue)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(kStyleStr))].stringValue == ManagerQuickLookable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(k_styleUrl.suffix(6)) + " hung" + String(kTableText.prefix(3))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.arcInfo(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.spiralBlindfoldLacrimalApparatusWithoutAdjourn()
            //: TalkingSocketManager.shared.currMyVC = nil
            ListSocketManager.shared.currMyVC = nil
        }

        //: popCurrentViewController()
        seek()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func addUser(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        reload(logId: json[String(bytes: kRequestButtonMsg.map{roundTap(app: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - PerformanceErrorDelegate【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension KeyDataViewController: PerformanceErrorDelegate {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func socket(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (kTableStr.replacingOccurrences(of: "title", with: "re") + String(kNoseGestureId.suffix(8))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.arcInfo(showMsg: (String(kIconValue) + kBeautyName.lowercased()).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.spiralBlindfoldLacrimalApparatusWithoutAdjourn()
                //: TalkingSocketManager.shared.currMyVC = nil
                ListSocketManager.shared.currMyVC = nil
            }
            //: popCurrentViewController()
            seek()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(k_homeListKey) + String(k_equalPath)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard DistanceAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard DistanceAppManager.share.appStatus == NameQuickLookable.normal.rawValue else { return }
            //: AddReactiveCompatible.share.func__jumpToWebRecharge(sufficient: false)
            AddReactiveCompatible.share.viewParams(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(k_homeListKey) + String(k_equalPath)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.arcInfo(showMsg: msg)
            //: popCurrentViewController()
            seek()
        }
    }
}

// MARK: - HighlightObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension KeyDataViewController: HighlightObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func writerSName() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        ListSocketManager.shared.pathTab(info: [String(bytes: k_blockValueMessage.map{submitAdd(max: $0)}, encoding: .utf8)!: (k_colorKey.replacingOccurrences(of: "length", with: "p") + String(kLabKey.suffix(4))), String(bytes: k_makeTitle.map{$0^230}, encoding: .utf8)!: [String(bytes: k_viewArcStr.map{$0^108}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func withCulminate() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.arcInfo(showMsg: (String(k_floorContent) + k_sectionStr.replacingOccurrences(of: "male", with: "e") + String(kPlayerId.suffix(4)) + "celled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            ListSocketManager.shared.pathTab(info: [String(bytes: k_blockValueMessage.map{submitAdd(max: $0)}, encoding: .utf8)!: (k_noseUrl.lowercased() + "lCall"), String(bytes: k_makeTitle.map{$0^230}, encoding: .utf8)!: [String(bytes: kRequestButtonMsg.map{roundTap(app: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.arcInfo(showMsg: (String(k_floorContent) + k_sectionStr.replacingOccurrences(of: "male", with: "e") + String(kPlayerId.suffix(4)) + "celled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            ListSocketManager.shared.pathTab(info: [String(bytes: k_blockValueMessage.map{submitAdd(max: $0)}, encoding: .utf8)!: (kTableStr.replacingOccurrences(of: "title", with: "re") + String(kNoseGestureId.suffix(8))), String(bytes: k_makeTitle.map{$0^230}, encoding: .utf8)!: [String(bytes: k_viewArcStr.map{$0^108}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.arcInfo(showMsg: (String(k_responseAddContent.suffix(4)) + "call h" + String(k_textData.prefix(3)) + String(kResumeMessage)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            ListSocketManager.shared.pathTab(info: [String(bytes: k_blockValueMessage.map{submitAdd(max: $0)}, encoding: .utf8)!: (String(kViewId.suffix(8))), String(bytes: k_makeTitle.map{$0^230}, encoding: .utf8)!: [String(bytes: kRequestButtonMsg.map{roundTap(app: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        seek()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func toFront(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.disappearBy(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func builder(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.topShow(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.boxCamera(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.changeUser()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func onAdd() {
        //: TalkingSocketManager.shared.currMyVC = self
        ListSocketManager.shared.currMyVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = EndItemMiniView.stroke()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.rankInput()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.renderUser()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.selectStart()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        seek()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func depictionApp() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension KeyDataViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func albumWithSubviews() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.buttonHandTo(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func dateConstraint() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
