
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_selectUrl:[UInt8] = [0xb2,0xb5,0xb2,0xaf,0xf3,0xb8,0xb4,0xbf,0xbe,0xa9,0xe1,0xf2,0xfb,0xb3,0xba,0xa8,0xfb,0xb5,0xb4,0xaf,0xfb,0xb9,0xbe,0xbe,0xb5,0xfb,0xb2,0xb6,0xab,0xb7,0xbe,0xb6,0xbe,0xb5,0xaf,0xbe,0xbf]

private func floatName(application num: UInt8) -> UInt8 {
    return num ^ 219
}

/*: "icon_home_position" :*/
fileprivate let kTitleText:[Character] = ["i","c","o","n","_","h","o"]
fileprivate let k_inputViewTitle:String = "add"
fileprivate let k_sharedSucceedId:String = "voice page valuee_pos"

/*: "btn_video_minimize" :*/
fileprivate let k_countVideoId:[Character] = ["b","t","n","_","v","i","d","e","o","_","m"]
fileprivate let kAspectMakeStr:[Character] = ["i","n"]
fileprivate let k_textSnapIfName:[Character] = ["i","m","i","z","e"]

/*: "btn_video_drop_nor" :*/
fileprivate let k_momentKey:String = "btn_vin lab stream"
fileprivate let k_makeData:String = "p_norgift equal view user count"

/*: "btn_video_call_nor" :*/
fileprivate let kShareKey:String = "btn_vias vertical"
fileprivate let k_keyId:String = "all_norcontent enable show make"

/*: "transform.scale" :*/
fileprivate let kDirectionId:[Character] = ["t","r","a","n","s","f"]
fileprivate let k_modeStr:[Character] = ["o","r","m"]
fileprivate let k_normalStr:String = ".scaledetail normal actual me touch"

/*: "animation" :*/
fileprivate let k_appMessage:String = "animaticlick"

/*: "icon_videocall_female_answer_bg" :*/
fileprivate let kMakeValueMessage:[UInt8] = [0x87,0x8d,0x81,0x80,0xb1,0x98,0x87,0x8a,0x8b,0x81,0x8d,0x8f,0x82,0x82,0xb1,0x88,0x8b,0x83,0x8f,0x82,0x8b,0xb1,0x8f,0x80,0x9d,0x99,0x8b,0x9c,0xb1,0x8c,0x89]

private func pageFilter(interval num: UInt8) -> UInt8 {
    return num ^ 238
}

/*: "icon_videocall_female_answer" :*/
fileprivate let kFramePath:[Character] = ["i","c","o","n","_","v"]
fileprivate let k_modeId:[Character] = ["i","d","e","o","c","a","l","l","_","f","e","m","a","l","e","_","a","n","s","w","e","r"]

/*: "icon_calltalk_points" :*/
fileprivate let k_hiddenMessage:[Character] = ["i","c","o","n","_","c","a","l"]
fileprivate let k_sendHalfValue:String = "loopalk"
fileprivate let k_byKey:String = "begin title else live up_points"

/*: "get json error" :*/
fileprivate let kModelPushUrl:String = "get jsview make lab on"

/*: "btn_video_turn_nor" :*/
fileprivate let k_secondUrl:String = "btn_vitype scale"
fileprivate let kCellName:String = "urn_norreturn name"

/*: "btn_video_turn_pre" :*/
fileprivate let k_viewFormat:String = "voice name cardbtn_vid"
fileprivate let k_feedbackData:[Character] = ["e","o","_","t","u","r","n","_","p","r","e"]

/*: "btn_video_start_nor" :*/
fileprivate let k_deviceStr:[Character] = ["b","t","n","_","v","i","d","e"]
fileprivate let kLabelTitle:String = "o_view status filter"
fileprivate let kListTitle:String = "start_norvalue self request"

/*: "btn_video_start_pre" :*/
fileprivate let k_giftStr:[Character] = ["b","t","n","_","v","i","d","e","o","_","s","t","a","r","t","_","p","r","e"]

/*: "icon_complaint" :*/
fileprivate let kSizeStr:[Character] = ["i","c","o","n","_","c","o","m","p"]
fileprivate let k_infoData:String = "iconint"

/*: "showBullet" :*/
fileprivate let kValuePath:String = "user at self asshowB"
fileprivate let kBottomLabKey:[Character] = ["u","l","l","e","t"]

/*: "icon_videoCall_coin_nor" :*/
fileprivate let kFeatureId:String = "with var let equalicon_"
fileprivate let kPathMessage:String = "all_come make normal time"
fileprivate let kManagerMessage:String = "C"
fileprivate let k_routeTitle:[Character] = ["o","i","n","_","n","o","r"]

/*: "btn_video_gift_nor" :*/
fileprivate let k_countryTitle:[Character] = ["b","t","n","_","v","i","d","e","o","_","g","i","f","t"]
fileprivate let k_requestName:[Character] = ["_","n","o","r"]

/*: "   " :*/
fileprivate let k_disabledVideoStr:String = "equalequalequal"

/*: "Say something...     " :*/
fileprivate let k_nameHiddenMakeData:String = "return new inSay so"
fileprivate let kModelMsg:String = "ing... feed var pull data max"
fileprivate let kEffectAddContent:[Character] = [" "," "," "," "]

/*: "waiting for the other party to accept the video invitation..." :*/
fileprivate let k_miniName:[UInt8] = [0x54,0x3e,0x46,0x51,0x46,0x4b,0x44,0xfd,0x43,0x4c,0x4f,0xfd,0x51,0x45,0x42,0xfd,0x4c,0x51,0x45,0x42,0x4f,0xfd,0x4d,0x3e,0x4f,0x51,0x56,0xfd,0x51,0x4c,0xfd,0x3e,0x40,0x40,0x42,0x4d,0x51,0xfd,0x51,0x45,0x42,0xfd,0x53,0x46,0x41,0x42,0x4c,0xfd,0x46,0x4b,0x53,0x46,0x51,0x3e,0x51,0x46,0x4c,0x4b,0xb,0xb,0xb]

fileprivate func customActual(own num: UInt8) -> UInt8 {
    let value = Int(num) - 221
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Invite you to a video call..." :*/
fileprivate let kValueData:String = "model camera model time returnInvi"
fileprivate let k_colorFormat:String = "ou tlog photo"
fileprivate let kStackStr:String = "and context case viewdeo "

/*: "yyyy-MM-dd" :*/
fileprivate let k_leadingLinkName:String = "yyyy-manager section let make"

/*: "00:00" :*/
fileprivate let k_equalMakeStr:[Character] = ["0","0",":","0","0"]

/*: " +" :*/
fileprivate let k_cellKey:[Character] = [" ","+"]

/*: "%@ points/min" :*/
fileprivate let k_sharedContent:[Character] = ["%","@"," ","p","o","i","n","t","s","/","m","i"]
fileprivate let kUserFormat:[Character] = ["n"]

/*: "Camera must be on" :*/
fileprivate let kUnlessMsg:String = "var modeCame"
fileprivate let kLocationKey:String = "ra mmodel block"

/*: "msgId" :*/
fileprivate let kFrameId:[UInt8] = [0xf3,0xf9,0xed,0xcf,0xea]

fileprivate func ofReplace(tolerance num: UInt8) -> UInt8 {
    let value = Int(num) - 134
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "senderUid" :*/
fileprivate let kLabelPath:[UInt8] = [0x7f,0x69,0x62,0x68,0x69,0x7e,0x59,0x65,0x68]

private func layerTo(text num: UInt8) -> UInt8 {
    return num ^ 12
}

/*: "toUid" :*/
fileprivate let k_equalNowName:String = "model case computer datatoUid"

/*: "nickname" :*/
fileprivate let kColorAtUrl:[UInt8] = [0x65,0x6d,0x61,0x6e,0x6b,0x63,0x69,0x6e]

/*: "content" :*/
fileprivate let k_arrayPath:[UInt8] = [0x48,0x44,0x45,0x5f,0x4e,0x45,0x5f]

private func whiteManagerValue(app num: UInt8) -> UInt8 {
    return num ^ 43
}

/*: "isMySender" :*/
fileprivate let kHouseUrl:[UInt8] = [0xbc,0xa6,0x98,0xac,0x86,0xb0,0xbb,0xb1,0xb0,0xa7]

private func yearBack(from num: UInt8) -> UInt8 {
    return num ^ 213
}

/*: "hasFace" :*/
fileprivate let kModelText:String = "hasFacsize view"
fileprivate let kDataKey:String = "time"

/*: "Automatically answer for you in %@s" :*/
fileprivate let kMainUrl:[UInt8] = [0x5,0x31,0x30,0x2b,0x29,0x25,0x30,0x2d,0x27,0x25,0x28,0x28,0x3d,0x64,0x25,0x2a,0x37,0x33,0x21,0x36,0x64,0x22,0x2b,0x36,0x64,0x3d,0x2b,0x31,0x64,0x2d,0x2a,0x64,0x61,0x4,0x37]

private func faceEach(bottom num: UInt8) -> UInt8 {
    return num ^ 68
}

/*: "giftId" :*/
fileprivate let kToKey:String = "giftIdautomatically app equal"

/*: "giftNum" :*/
fileprivate let k_cosKey:[Character] = ["g","i","f","t","N","u","m"]

/*: "from" :*/
fileprivate let k_equalPath:[Character] = ["f","r","o","m"]

/*: "videoCall" :*/
fileprivate let k_makeId:String = "videoCaltop color extra"
fileprivate let kTargetFormat:String = "single"

/*: "programSort" :*/
fileprivate let k_logText:[Character] = ["p","r","o","g","r"]
fileprivate let k_statuteId:[Character] = ["a"]
fileprivate let kFrameMsg:String = "mSortfor of view detail"

/*: "pkgItemsetId" :*/
fileprivate let kActualIconMessage:[Character] = ["p","k","g","I","t","e"]
fileprivate let kAfterFormat:[Character] = ["m","s","e","t","I","d"]

/*: "msgInfo" :*/
fileprivate let kFemaleTitle:String = "msgInto push conversation gift"
fileprivate let k_centerUrl:String = "size"

/*: "msgType" :*/
fileprivate let k_messCurrentKey:String = "lab"
fileprivate let kToUrl:String = "gTypemanager white can item center"

/*: "gift" :*/
fileprivate let kMomentName:String = "headift"

/*: "totalMfCoin" :*/
fileprivate let k_whitePath:[Character] = ["t","o","t","a","l","M"]
fileprivate let k_effectName:[Character] = ["f","C","o","i","n"]

/*: "cellData" :*/
fileprivate let kPhoneFormat:[UInt8] = [0xc6,0xc0,0xc9,0xc9,0xe1,0xc4,0xd1,0xc4]

private func userView(nor num: UInt8) -> UInt8 {
    return num ^ 165
}

/*: "%02i:%02i" :*/
fileprivate let k_colorData:String = "if image false data%02i:%"
fileprivate let kHiddenText:[Character] = ["0","2","i"]

/*: "..." :*/
fileprivate let k_sharedUrl:String = "establishestablishestablish"

/*: ".." :*/
fileprivate let k_bagUrl:String = "equalequal"

/*: "FaceActionEvent_Interval_12_frame" :*/
fileprivate let kTheFinishData:[UInt8] = [0x9b,0xb6,0xb8,0xba,0x96,0xb8,0xc9,0xbe,0xc4,0xc3,0x9a,0xcb,0xba,0xc3,0xc9,0xb4,0x9e,0xc3,0xc9,0xba,0xc7,0xcb,0xb6,0xc1,0xb4,0x86,0x87,0xb4,0xbb,0xc7,0xb6,0xc2,0xba]

fileprivate func modelRoute(app num: UInt8) -> UInt8 {
    let value = Int(num) + 171
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AddPublishThen.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import UIKit
import UIKit

//: protocol Talking1v1VideoInteractionViewDelegate: NSObjectProtocol {
protocol HighlightObjectProtocol: NSObjectProtocol {
    /// 发送方结束通话
    //: func interactionView_shouldEndCall()
    func withCulminate()

    /// 接听视频通话
    //: func interactionView_shouldAcceptCall()
    func writerSName()

    /// 旋转摄像头
    /// - Parameter isFront: 是否前置（默认前置）
    //: func interactionView_shouldChangeCamera(isFront: Bool)
    func toFront(isFront: Bool)

    /// 禁用/开启摄像头
    /// - Parameter forbidden: 是否禁用摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool)
    func builder(forbidden: Bool)

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow()
    func onAdd()

    /// 投诉成功
    //: func interactionView_reportSucceed()
    func depictionApp()
}

//: class Talking1v1VideoInteractionView: UIView {
class AddPublishThen: UIView {
    //: let place = (ScreenWidth-actualWidth(w: 45)*5)/6
    let place = (constFormalContent - actualWidth(w: 45) * 5) / 6
    //: weak var delegate: Talking1v1VideoInteractionViewDelegate?
    weak var delegate: HighlightObjectProtocol?
    //: private var chatModel = TalkingVideoChatModel()
    private var chatModel = ViewChatModel() // 引用类型，整个视频通话共用同一个model
    //: private var waitTimer: Timer?
    private var waitTimer: Timer? // 等待接听动画定时器
    //: private var startTime: TimeInterval?
    private var startTime: TimeInterval? // 开始的时间戳记 since1970
    //: private var countTimer: Timer?
    private var countTimer: Timer? // 通话时间定时器
    //: private var freeCountTimer: Timer?
    private var freeCountTimer: Timer? // 免费通话剩余时间定时器
//    var isShowRecharge  = false                         // 是否展示充值页中
    //: var isShowInput  = false
    var isShowInput = false // 是否在聊天中
    //: private var autoAcceptTimer: DispatchSourceTimer?
    private var autoAcceptTimer: DispatchSourceTimer? // 自动接听定时器

    //: init(frame: CGRect, chatModel: TalkingVideoChatModel?) {
    init(frame: CGRect, chatModel: ViewChatModel?) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        disappear()
        //: setupSubViewsConstraint()
        lens()
        //: if chatModel != nil {
        if chatModel != nil {
            //: self.chatModel = chatModel!
            self.chatModel = chatModel!
            //: self.startTime = chatModel!.startTime
            self.startTime = chatModel!.startTime
        }
        //: showViewsViaStage()
        toStage()
        //: bindInteraction()
        firstMagnitudeeraction()
        //: initNotificationObserver()
        spectator()
        //: initCachEffectData()
        sodom()
        //: isShowHangupBtn()
        addClickBtn()

        //: TalkingVideoDanmuManager.shared().delegate = self
        DirectorDanmuManager.blockCurrent().delegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_selectUrl.map{floatName(application: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: if waitTimer != nil {
        if waitTimer != nil {
            //: waitTimer?.invalidate()
            waitTimer?.invalidate()
            //: waitTimer = nil
            waitTimer = nil
        }
        //: if countTimer != nil {
        if countTimer != nil {
            //: countTimer?.invalidate()
            countTimer?.invalidate()
            //: countTimer = nil
            countTimer = nil
        }
        //: if freeCountTimer != nil {
        if freeCountTimer != nil {
            //: freeCountTimer?.invalidate()
            freeCountTimer?.invalidate()
            //: freeCountTimer = nil
            freeCountTimer = nil
        }
        //: cancelAutoAcceptTimer()
        inputWith()
        //: player.stopPlay()
        player.halt()
        //: invalidateIdleTimer()
        alongPair()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    // 点击展示/隐藏交互按钮
    //: private lazy var touchHidenBtn: UIButton = {
    private lazy var touchHidenBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(touchHidenBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(councilMember), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像背景
    //: private lazy var pairInfoBg: UIView = {
    private lazy var pairInfoBg: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = UIColor(white: 0, alpha: 0.4)
        v.backgroundColor = UIColor(white: 0, alpha: 0.4)
        //: v.layer.cornerRadius = actualWidth(w: 50)/2
        v.layer.cornerRadius = actualWidth(w: 50) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var locationBtn: TalkingButton = {
    private lazy var locationBtn: ResumeControl = {
        //: let btn = TalkingButton()
        let btn = ResumeControl()
        //: btn.setImage(UIImage.submitGift(name: "icon_home_position"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(kTitleText) + k_inputViewTitle.replacingOccurrences(of: "add", with: "m") + String(k_sharedSucceedId.suffix(5)) + "ition")), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 12)
        btn.titleLabel?.font = UIFont.targetFor(fontSize: 12)
        //: btn.contentHorizontalAlignment = LanguageManager.shared.direction == .rightToLeft ? .right:.left
        btn.contentHorizontalAlignment = MakeLanguageManager.shared.direction == .rightToLeft ? .right : .left
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像
    //: private lazy var pairPicV: UIImageView = {
    private lazy var pairPicV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.layer.cornerRadius = actualWidth(w: 40)/2
        v.layer.cornerRadius = actualWidth(w: 40) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: v.layer.borderWidth = 1
        v.layer.borderWidth = 1
        //: v.layer.borderColor = UIColor.white.cgColor
        v.layer.borderColor = UIColor.white.cgColor
        //: return v
        return v
        //: }()
    }()

    // 接收方昵称
    //: private lazy var pairNameLab: UILabel = {
    private lazy var pairNameLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 17)
        lab.font = UIFont.tapShared(fontSize: 17)
        //: return lab
        return lab
        //: }()
    }()

    // 呼叫中描述
    //: private lazy var desLab: UILabel = {
    private lazy var desLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.targetFor(fontSize: 17)
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    // 最小化按钮
    //: private lazy var minimizeBtn: UIButton = {
    private lazy var minimizeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.submitGift(name: "btn_video_minimize"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(k_countVideoId) + String(kAspectMakeStr) + String(k_textSnapIfName))), for: .normal)
        //: btn.addTarget(self, action: #selector(minimizeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(locationDrop), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 挂断/取消 按钮
    //: private lazy var hangupBtn: UIButton = {
    private lazy var hangupBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.submitGift(name: "btn_video_drop_nor"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(k_momentKey.prefix(5)) + "ideo_dro" + String(k_makeData.prefix(5)))), for: .normal)
        //: btn.addTarget(self, action: #selector(hangupButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(anFixing), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接听按钮
    //: private lazy var acceptBtn: UIButton = {
    private lazy var acceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.submitGift(name: "btn_video_call_nor"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(kShareKey.prefix(6)) + "deo_c" + String(k_keyId.prefix(7)))), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(clickImage), for: .touchUpInside)

        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (String(kDirectionId) + String(k_modeStr) + String(k_normalStr.prefix(6)))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80/70), y: actualWidth(w: 80/70))),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80 / 70), y: actualWidth(w: 80 / 70))),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
        //: keyAnimation.duration = 1.2
        keyAnimation.duration = 1.2
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (k_appMessage.replacingOccurrences(of: "click", with: "on")))

        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBGBtn: UIButton = {
    private lazy var femaleAcceptBGBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setBackgroundImage(UIImage.submitGift(name: "icon_videocall_female_answer_bg"), for: .normal)
        btn.setBackgroundImage(UIImage.submitGift(name: String(bytes: kMakeValueMessage.map{pageFilter(interval: $0)}, encoding: .utf8)!), for: .normal)
        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (String(kDirectionId) + String(k_modeStr) + String(k_normalStr.prefix(6)))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
        //: keyAnimation.duration = 0.6
        keyAnimation.duration = 0.6
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (k_appMessage.replacingOccurrences(of: "click", with: "on")))
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBtn: UIButton = {
    private lazy var femaleAcceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.submitGift(name: "icon_videocall_female_answer"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(kFramePath) + String(k_modeId))), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(clickImage), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听积分提示
    //: private lazy var femaleAcceptTipsBtn: TalkingButton = {
    private lazy var femaleAcceptTipsBtn: ResumeControl = {
        //: let btn = TalkingButton()
        let btn = ResumeControl()
        //: btn.setImage(UIImage.submitGift(name: "icon_calltalk_points"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(k_hiddenMessage) + k_sendHalfValue.replacingOccurrences(of: "loop", with: "lt") + String(k_byKey.suffix(7)))), for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 15)
        btn.titleLabel?.font = UIFont.targetFor(fontSize: 15)
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var acceptPlayer: SVGAPlayer = {
    lazy var acceptPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: .CallTalk_Accept)
        let url = VideoEffectTool.default.conType(type: .CallTalk_Accept)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(kModelPushUrl.prefix(6)) + "on error"))
        }
        //: if LanguageManager.shared.direction == .rightToLeft {
        if MakeLanguageManager.shared.direction == .rightToLeft {
            //: player.transform = CGAffineTransform(scaleX: -1, y: 1)
            player.transform = CGAffineTransform(scaleX: -1, y: 1)
        }
        //: return player
        return player
        //: }()
    }()

    // 旋转摄像头按钮
    //: private lazy var rotateCameraBtn: UIButton = {
    private lazy var rotateCameraBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.submitGift(name: "btn_video_turn_nor"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(k_secondUrl.prefix(6)) + "deo_t" + String(kCellName.prefix(7)))), for: .normal)
        //: btn.setImage(UIImage.submitGift(name: "btn_video_turn_pre"), for: .selected)
        btn.setImage(UIImage.submitGift(name: (String(k_viewFormat.suffix(7)) + String(k_feedbackData))), for: .selected)
        //: btn.addTarget(self, action: #selector(rotateCameraButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(moveScreen), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 开启/关闭摄像头按钮
    //: private lazy var openCameraBtn: UIButton = {
    private lazy var openCameraBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.submitGift(name: "btn_video_start_nor"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(k_deviceStr) + String(kLabelTitle.prefix(2)) + String(kListTitle.prefix(9)))), for: .normal)
        //: btn.setImage(UIImage.submitGift(name: "btn_video_start_pre"), for: .selected)
        btn.setImage(UIImage.submitGift(name: (String(k_giftStr))), for: .selected)
        //: btn.addTarget(self, action: #selector(openCameraButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(shadowPhotographicEquipment), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 投诉按钮（男性）
    //: private lazy var reportBtn: UIButton = {
    private lazy var reportBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.submitGift(name: "icon_complaint"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(kSizeStr) + k_infoData.replacingOccurrences(of: "icon", with: "la"))), for: .normal)
        //: btn.addTarget(self, action: #selector(reportButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(representation), for: .touchUpInside)
        //: let data = JSON(DistanceAppManager.share.appConfigMode.videoReport)
        let data = JSON(DistanceAppManager.share.appConfigMode.videoReport)
        //: if data["showBullet"].boolValue == true {
        if data[(String(kValuePath.suffix(5)) + String(kBottomLabKey))].boolValue == true {
            //: btn.isEnabled = true
            btn.isEnabled = true
            //: } else {
        } else {
            //: btn.isEnabled = false
            btn.isEnabled = false
        }
        //: return btn
        return btn
        //: }()
    }()

    // 充值按钮
    //: private lazy var rechargeBtn: UIButton = {
    private lazy var rechargeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.submitGift(name: "icon_videoCall_coin_nor"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(kFeatureId.suffix(5)) + "videoC" + String(kPathMessage.prefix(4)) + kManagerMessage.lowercased() + String(k_routeTitle))), for: .normal)
        //: btn.setImage(UIImage.submitGift(name: "icon_videoCall_coin_nor"), for: .selected)
        btn.setImage(UIImage.submitGift(name: (String(kFeatureId.suffix(5)) + "videoC" + String(kPathMessage.prefix(4)) + kManagerMessage.lowercased() + String(k_routeTitle))), for: .selected)
        //: btn.addTarget(self, action: #selector(rechargeBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(gestureScreen), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 送礼按钮
    //: private lazy var sendGiftBtn: UIButton = {
    private lazy var sendGiftBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.submitGift(name: "btn_video_gift_nor"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(k_countryTitle) + String(k_requestName))), for: .normal)
        //: btn.setImage(UIImage.submitGift(name: "btn_video_gift_nor"), for: .selected)
        btn.setImage(UIImage.submitGift(name: (String(k_countryTitle) + String(k_requestName))), for: .selected)
        //: btn.addTarget(self, action: #selector(sendGiftButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(buttonToday), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 18)
        lab.font = UIFont.tapShared(fontSize: 18)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var freeTimeView: TalkingFreeCallRechargeTipsView = {
    private lazy var freeTimeView: TipsView = {
        //: let view = TalkingFreeCallRechargeTipsView()
        let view = TipsView()
        //: view.backgroundColor = .clear
        view.backgroundColor = .clear
        //: view.touchRechargeBtnBlock = { [weak self] in
        view.touchRechargeBtnBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
//            self.isShowRecharge = true
        }
        //: return view
        return view
        //: }()
    }()

    //: lazy var giftView: TalkingChatGiftView = {
    lazy var giftView: MenuView = {
        //: let giftV = TalkingChatGiftView.init(style: .call)
        let giftV = MenuView(style: .call)
        //: giftV.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        giftV.frame = CGRect(x: 0, y: 0, width: constFormalContent, height: user_recordMarginValue)
        //: return giftV
        return giftV
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: SizeAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView()
        let effectView = SizeAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var giftTrackView: TalkingGiftTrackView = {
    lazy var giftTrackView: SizeTouchTrackView = {
        //: let trackView = TalkingGiftTrackView()
        let trackView = SizeTouchTrackView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: lazy var pointAnimView: TalkingPointAnimationView = {
    lazy var pointAnimView: WitherView = {
        //: let pointView = TalkingPointAnimationView()
        let pointView = WitherView()
        //: pointView.isUserInteractionEnabled = false
        pointView.isUserInteractionEnabled = false
        //: return pointView
        return pointView
        //: }()
    }()

    // 视频播放器
    //: private lazy var player: TalkingVideoPlayerManager = {
    private lazy var player: DecisionMakerThen = {
        //: let player = TalkingVideoPlayerManager()
        let player = DecisionMakerThen()
        //: player.setMute(bEnable: true)
        player.setFor(bEnable: true)
        //: return player
        return player
        //: }()
    }()

    // 视频封面
    //: private lazy var coverImg: UIImageView = {
    private lazy var coverImg: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    /// 1.7.2 聊天消息
    //: private lazy var chatBtn: UIButton = {
    private lazy var chatBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.backgroundColor = UIColor.init(white: 0, alpha: 0.25)
        btn.backgroundColor = UIColor(white: 0, alpha: 0.25)
        //: btn.layer.cornerRadius = 17
        btn.layer.cornerRadius = 17
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.setTitle("   "+"Say something...     ".localized, for: .normal)
        btn.setTitle("   " + (String(k_nameHiddenMakeData.suffix(6)) + "meth" + String(kModelMsg.prefix(7)) + String(kEffectAddContent)).localized, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 14)
        btn.titleLabel?.font = UIFont.targetFor(fontSize: 14)
        //: btn.setTitleColor(UIColor.init(white: 1, alpha: 0.8), for: .normal)
        btn.setTitleColor(UIColor(white: 1, alpha: 0.8), for: .normal)
        //: btn.contentHorizontalAlignment = LanguageManager.shared.direction == .rightToLeft ? .right:.left
        btn.contentHorizontalAlignment = MakeLanguageManager.shared.direction == .rightToLeft ? .right : .left
        //: btn.addTarget(self, action: #selector(commentBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(mortal), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var danmuView: TalkingVideoDanmuMsgListTableView = {
    private lazy var danmuView: DanmuThen = {
        //: let view = TalkingVideoDanmuMsgListTableView.init()
        let view = DanmuThen()
        //: view.touchTableGesblock = { [weak self] in
        view.touchTableGesblock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.touchHidenBtnClick()
            self.councilMember()
        }
        //: return view
        return view
        //: }()
    }()

    //: private lazy var commInputView: TalkingVideoCallInputView = {
    private lazy var commInputView: EndInputView = {
        //: let text = TalkingVideoCallInputView.init()
        let text = EndInputView()
        //: text.delegate = self
        text.delegate = self
        //: text.isHidden = true
        text.isHidden = true
        //: return text
        return text
        //: }()
    }()

    // 自动接听文案描述（女性）
    //: private lazy var autoAcceptDesLab: UILabel = {
    private lazy var autoAcceptDesLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.targetFor(fontSize: 17)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()
}

// MARK: - Event

//: extension Talking1v1VideoInteractionView {
extension AddPublishThen {
    /// 展示视图状态
    //: func showViewsViaStage() {
    func toStage() {
        //: switch chatModel.videoStage {
        switch chatModel.videoStage {
        //: case .Waiting:
        case .Waiting:
            //: desLab.text = "waiting for the other party to accept the video invitation...".localized
            desLab.text = String(bytes: k_miniName.map{customActual(own: $0)}, encoding: .utf8)!.localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: pairInfoBg.isHidden = false
            pairInfoBg.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: if DistanceAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.male.rawValue {
                //: rotateCameraBtn.isHidden = true
                rotateCameraBtn.isHidden = true
                //: openCameraBtn.isHidden = true
                openCameraBtn.isHidden = true
            }
            //: reportBtn.isHidden = true
            reportBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: chatBtn.isHidden = true
            chatBtn.isHidden = true
            //: danmuView.isHidden = true
            danmuView.isHidden = true
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
        //: case .Recieved:
        case .Recieved:
            //: desLab.text = "Invite you to a video call...".localized
            desLab.text = (String(kValueData.suffix(4)) + "te y" + String(k_colorFormat.prefix(4)) + "o a vi" + String(kStackStr.suffix(4)) + "call...").localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: pairInfoBg.isHidden = false
            pairInfoBg.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
            //: if DistanceAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.male.rawValue {
                //: acceptBtn.isHidden = false
                acceptBtn.isHidden = false
                //: femaleAcceptBtn.isHidden = true
                femaleAcceptBtn.isHidden = true
                //: femaleAcceptTipsBtn.isHidden = true
                femaleAcceptTipsBtn.isHidden = true
                //: acceptPlayer.isHidden = true
                acceptPlayer.isHidden = true
                //: femaleAcceptBGBtn.isHidden = true
                femaleAcceptBGBtn.isHidden = true
                //: rotateCameraBtn.isHidden = true
                rotateCameraBtn.isHidden = true
                //: openCameraBtn.isHidden = true
                openCameraBtn.isHidden = true
                //: } else {
            } else {
                //: acceptBtn.isHidden = true
                acceptBtn.isHidden = true
                //: femaleAcceptBtn.isHidden = false
                femaleAcceptBtn.isHidden = false
                //: femaleAcceptTipsBtn.isHidden = false
                femaleAcceptTipsBtn.isHidden = false
                //: femaleAcceptBGBtn.isHidden = false
                femaleAcceptBGBtn.isHidden = false
                //: let date = Defaults.object(forKey: TalkingCallShowTag)
                let date = show_blockText.object(forKey: show_userInsertName)
                //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                let today = NSDate.nameDateDayOfTheMonthAppearanceTimeGet(date: Date(), dateFormat: (String(k_leadingLinkName.prefix(5)) + "MM-dd"))
                //: if date == nil || (date as! String) != today {
                if date == nil || (date as! String) != today {
                    //: Defaults.set(today, forKey: TalkingCallShowTag)
                    show_blockText.set(today, forKey: show_userInsertName)
                    //: acceptPlayer.isHidden = false
                    acceptPlayer.isHidden = false
                    //: } else {
                } else {
                    //: acceptPlayer.isHidden = true
                    acceptPlayer.isHidden = true
                }
                //: autoAcceptDesLab.isHidden = false
                autoAcceptDesLab.isHidden = false
                // 直播时自动接听
                //: if DistanceAppManager.share.appConfigMode.autoAnswer == 2 &&
                if DistanceAppManager.share.appConfigMode.autoAnswer == 2 &&
                    //: NEED_RESTORE_LIVE == true {
                    app_limitScaleText == true
                {
                    //: showOrHidenAutoRecieved(autoAccept: true)
                    ofView(autoAccept: true)
                    //: } else {
                } else {
                    //: showOrHidenAutoRecieved()
                    ofView()
                }
            }
            //: reportBtn.isHidden = true
            reportBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: chatBtn.isHidden = true
            chatBtn.isHidden = true
            //: danmuView.isHidden = true
            danmuView.isHidden = true
        //: case .Calling:
        case .Calling:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: pairInfoBg.isHidden = true
            pairInfoBg.isHidden = true
            //: minimizeBtn.isHidden = false
            minimizeBtn.isHidden = false
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: rotateCameraBtn.isHidden = false
            rotateCameraBtn.isHidden = false
            //: openCameraBtn.isHidden = false
            openCameraBtn.isHidden = false
            //: reportBtn.isHidden = !reportBtn.isEnabled || DistanceAppManager.share.loginUserMode.sex == Gender.female.rawValue
            reportBtn.isHidden = !reportBtn.isEnabled || DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.female.rawValue
            //: sendGiftBtn.isHidden = false
            sendGiftBtn.isHidden = false
            //: rechargeBtn.isHidden = false
            rechargeBtn.isHidden = false
            //: touchHidenBtn.isEnabled = true
            touchHidenBtn.isEnabled = true
            //: timeLab.isHidden = false
            timeLab.isHidden = false
            //: chatBtn.isHidden = false
            chatBtn.isHidden = false
            //: danmuView.isHidden = false
            danmuView.isHidden = false
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
            //: if startTime != nil {
            if startTime != nil {
                //: changeTalkTime()
                activityIn()
                //: } else {
            } else {
                //: timeLab.text = "00:00"
                timeLab.text = (String(k_equalMakeStr))
            }
            //: startTalkCount()
            deal()
            //: touchHiddenTimer()
            hiddenEqual()
            //: if TalkingSocketManager.shared.isFreeCall {
            if ListSocketManager.shared.isFreeCall {
                //: setFreeView()
                ship()
                //: setFreeCountTimer()
                queue()
            }
            //: if reportBtn.isHidden {
            if reportBtn.isHidden {
                //: sendGiftBtn.snp.remakeConstraints { make in
                sendGiftBtn.snp.remakeConstraints { make in
                    //: make.trailing.equalToSuperview().offset(-10)
                    make.trailing.equalToSuperview().offset(-10)
                    //: make.bottom.equalTo(reportBtn)
                    make.bottom.equalTo(reportBtn)
                    //: make.size.equalTo(reportBtn)
                    make.size.equalTo(reportBtn)
                }
            }
        }

        //: updateLayout()
        layout()
        //: afterGetInfo()
        laterOnThroughInfo()
    }

    /// 刷新通话视图数据
    //: func afterGetInfo() {
    func laterOnThroughInfo() {
        //: pairNameLab.text = chatModel.pairNickName
        pairNameLab.text = chatModel.pairNickName
        //: locationBtn.isHidden = chatModel.location.count <= 0
        locationBtn.isHidden = chatModel.location.count <= 0
        //: locationBtn.setTitle("\(chatModel.location)", for: .normal)
        locationBtn.setTitle("\(chatModel.location)", for: .normal)
        //: femaleAcceptTipsBtn.setTitle(" +" + "%@ points/min".localizedArguments(chatModel.points), for: .normal)
        femaleAcceptTipsBtn.setTitle(" +" + (String(k_sharedContent) + String(kUserFormat)).edgeDelay(chatModel.points), for: .normal)
        //: let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: actualWidth(w: 20)))]
        let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: actualWidth(w: 20)))]
        //: pairPicV.setUrlImage(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        pairPicV.setUrlImage(urlStr: chatModel.pairHeadPic, options: optionsInfo)

        // 男性展示视频封面 【非通话中】
        //: if DistanceAppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.male.rawValue {
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: coverImg.setUrlImage(urlStr: chatModel.pairHeadPic)
            coverImg.setUrlImage(urlStr: chatModel.pairHeadPic)
            //: if chatModel.videoStage != .Calling, chatModel.videoUrl.count > 0 {
            if chatModel.videoStage != .Calling, chatModel.videoUrl.count > 0 {
                //: player.delegate = self
                player.delegate = self
                //: player.playerWithUrlAndVideoView(url: chatModel.videoUrl, view: self)
                player.dataView(url: chatModel.videoUrl, view: self)
            }
        }
        //: if chatModel.videoStage == .Calling {
        if chatModel.videoStage == .Calling {
            //: coverImg.isHidden = true
            coverImg.isHidden = true
            //: player.stopPlay()
            player.halt()
            //: player.removeVideoWidget()
            player.forwardClick()
        }
    }

    /// 最小化按钮事件
    //: @objc private func minimizeButtonClick() {
    @objc private func locationDrop() {
        //: delegate?.interactionView_shouldDisplayMiniWindow()
        delegate?.onAdd()
    }

    /// 添加视频通话画面窗口
    /// - Parameter view: 视频画面窗口
    //: func addWindowView(view: UIView) {
    func buttonHandTo(view: UIView) {
        //: insertSubview(view, belowSubview: pairPicV)
        insertSubview(view, belowSubview: pairPicV)
    }

    /// 挂断按钮事件
    //: @objc private func hangupButtonClick() {
    @objc private func anFixing() {
        //: delegate?.interactionView_shouldEndCall()
        delegate?.withCulminate()
    }

    /// 接听按钮事件
    //: @objc private func acceptButtonClick() {
    @objc private func clickImage() {
        //: self.cancelAutoAcceptTimer()
        self.inputWith()
        //: delegate?.interactionView_shouldAcceptCall()
        delegate?.writerSName()
    }

    /// 旋转摄像头事件
    //: @objc private func rotateCameraButtonClick() {
    @objc private func moveScreen() {
        //: if openCameraBtn.isSelected == true {
        if openCameraBtn.isSelected == true { // 腾讯美颜 && 摄像头关闭时
            //: func__showStatusBarErrorMsg(showMsg: "Camera must be on".localized)
            arcInfo(showMsg: (String(kUnlessMsg.suffix(4)) + String(kLocationKey.prefix(4)) + "ust be on").localized)
            //: return
            return
        }
        //: rotateCameraBtn.isSelected = !rotateCameraBtn.isSelected
        rotateCameraBtn.isSelected = !rotateCameraBtn.isSelected
        //: delegate?.interactionView_shouldChangeCamera(isFront: !rotateCameraBtn.isSelected)
        delegate?.toFront(isFront: !rotateCameraBtn.isSelected)
    }

    /// 是否禁用摄像头事件
    //: @objc private func openCameraButtonClick() {
    @objc private func shadowPhotographicEquipment() {
        //: openCameraBtn.isSelected = !openCameraBtn.isSelected
        openCameraBtn.isSelected = !openCameraBtn.isSelected
        //: delegate?.interactionView_shouldForbiddenCamera(forbidden: openCameraBtn.isSelected)
        delegate?.builder(forbidden: openCameraBtn.isSelected)
    }

    /// 举报按钮点击事件（男性）
    //: @objc private func reportButtonClick() {
    @objc private func representation() {
        //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportVideo, rId: String(chatModel.logId))
        let reportView = InformDataSource(frame: UIScreen.main.bounds, type: .reportVideo, rId: String(chatModel.logId))
        //: reportView.showReportViewIn(view: nil)
        reportView.magnitudeeraction(view: nil)
        //: reportView.reportHideBlock = { [weak self] succeed in
        reportView.reportHideBlock = { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            // 投诉成功
            //: self.reportBtn.isEnabled = false
            self.reportBtn.isEnabled = false
            //: self.reportBtn.isHidden = true
            self.reportBtn.isHidden = true
            //: self.delegate?.interactionView_reportSucceed()
            self.delegate?.depictionApp()
        }
    }

    /// 送礼按钮事件
    //: @objc private func sendGiftButtonClick() {
    @objc private func buttonToday() {
        //: func__sendGift()
        atInside()
    }

    /// 充值按钮事件
    //: @objc private func rechargeBtnClick() {
    @objc private func gestureScreen() {
//        self.isShowRecharge = true
        //: AddReactiveCompatible.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
        AddReactiveCompatible.share.halfDismiss(webViewType: .RechargeHalfPage)
    }

    //: @objc private func touchHidenBtnClick() {
    @objc private func councilMember() {
        //: changeInteractionShowState()
        travelKit()
        //: if isShowInput {
        if isShowInput {
            //: dismissClick()
            clickPathList()
        }
    }

    /// 点击展示/隐藏通话中的按钮
    //: @objc private func changeInteractionShowState() {
    @objc private func travelKit() {
        //: let isHidden = !rotateCameraBtn.isHidden
        let isHidden = !rotateCameraBtn.isHidden
        //: minimizeBtn.isHidden = isHidden
        minimizeBtn.isHidden = isHidden
        //: rotateCameraBtn.isHidden = isHidden
        rotateCameraBtn.isHidden = isHidden
        //: openCameraBtn.isHidden = isHidden
        openCameraBtn.isHidden = isHidden
        //: reportBtn.isHidden = (reportBtn.isEnabled ? isHidden:true) ||  DistanceAppManager.share.loginUserMode.sex == Gender.female.rawValue
        reportBtn.isHidden = (reportBtn.isEnabled ? isHidden : true) || DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.female.rawValue
        //: sendGiftBtn.isHidden = isHidden
        sendGiftBtn.isHidden = isHidden
        //: rechargeBtn.isHidden = isHidden
        rechargeBtn.isHidden = isHidden
        //: hangupBtn.isHidden = isHidden
        hangupBtn.isHidden = isHidden
        //: timeLab.isHidden = isHidden
        timeLab.isHidden = isHidden
        //: if TalkingSocketManager.shared.isFreeCall {
        if ListSocketManager.shared.isFreeCall {
            //: self.freeTimeView.isHidden = isHidden
            self.freeTimeView.isHidden = isHidden
        }
        //: if isHidden {
        if isHidden {
            //: invalidateIdleTimer()
            alongPair()
            //: } else {
        } else {
            //: touchHiddenTimer()
            hiddenEqual()
        }
    }

    /// 去电，x秒后才显示挂断按钮
    //: private func isShowHangupBtn() {
    private func addClickBtn() {
        //: if chatModel.isCaller && chatModel.videoStage == .Waiting && DistanceAppManager.share.appUserConfigMode.callCancelBtnDelayTime > 0 && DistanceAppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if chatModel.isCaller, chatModel.videoStage == .Waiting, DistanceAppManager.share.appUserConfigMode.callCancelBtnDelayTime > 0, DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.male.rawValue {
            //: hangupBtn.isHidden = true
            hangupBtn.isHidden = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now()+Double(DistanceAppManager.share.appUserConfigMode.callCancelBtnDelayTime)) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + Double(DistanceAppManager.share.appUserConfigMode.callCancelBtnDelayTime)) {
                //: self.hangupBtn.isHidden = false
                self.hangupBtn.isHidden = false
            }
        }
    }

    /// 取消GCD自动接听定时器
    //: private func cancelAutoAcceptTimer() {
    private func inputWith() {
        //: if autoAcceptTimer != nil {
        if autoAcceptTimer != nil {
            //: autoAcceptTimer?.cancel()
            autoAcceptTimer?.cancel()
            //: autoAcceptTimer = nil
            autoAcceptTimer = nil
        }
    }
}

// MARK: - DirectorDanmuManager

//: extension Talking1v1VideoInteractionView: TalkingVideoDanmuManagerDelegate {
extension AddPublishThen: BulgeOutManagerDelegate {
    //: func func__DanmuRecvNewModel(Msg: TalkingVideoCallDanmuModel) {
    func changeMsg(Msg: StartMeasurable) {
        //: danmuView.addNewMsg(msgModel: Msg)
        danmuView.mews(msgModel: Msg)
    }

    //: @objc private func commentBtnClick() {
    @objc private func mortal() {
        /// 拉起弹幕评论
        //: self.bringSubviewToFront(commInputView)
        self.bringSubviewToFront(commInputView)
        //: commInputView.updatePlaceholder()
        commInputView.brushPlaceholder()
    }
}

// MARK: - 发表弹幕代理

//: extension Talking1v1VideoInteractionView: TalkingVideoCallInputViewDelegate {
extension AddPublishThen: DrapeReactiveCompatible {
    //: @objc func dismissClick() {
    @objc func clickPathList() {
        //: self.isShowInput = false
        self.isShowInput = false
//        commInputView.inputTextView.text = ""
        //: commInputView.resignkeyBoard()
        commInputView.quantityernalRepresentation()
    }

    //: func func__sendTextMsg(msgStr: String) {
    func terminalWithoutText(msgStr: String) {
        //: self.isShowInput = false
        self.isShowInput = false
        //: sendToTextMsg(msgStr: msgStr)
        meanSolarDay(msgStr: msgStr)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func digitizerPage(heightToBottom: CGFloat) {
        //: if heightToBottom > 0 {
        if heightToBottom > 0 {
            //: self.isShowInput = true
            self.isShowInput = true
        }
        //: commInputView.snp.updateConstraints { make in
        commInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self).offset(-heightToBottom)
            make.bottom.equalTo(self).offset(-heightToBottom)
        }
        //: danmuView.snp.updateConstraints { make in
        danmuView.snp.updateConstraints { make in
            //: let height = heightToBottom == 0 ? -10 : -(heightToBottom-chatBtn.height+commInputView.height-kDeviceSafeBottomHeight+10)
            let height = heightToBottom == 0 ? -10 : -(heightToBottom - chatBtn.height + commInputView.height - appButtonData + 10)
            //: make.bottom.equalTo(chatBtn.snp.top).offset(height)
            make.bottom.equalTo(chatBtn.snp.top).offset(height)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func anima(height: CGFloat) {
        //: commInputView.snp.updateConstraints { make in
        commInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }

        //: self.needsUpdateConstraints()
        self.needsUpdateConstraints()
        //: self.updateConstraintsIfNeeded()
        self.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.layoutIfNeeded()
            self.layoutIfNeeded()
            //: })
        })
    }

    /// 发送文本消息
    //: func sendToTextMsg(msgStr: String) {
    func meanSolarDay(msgStr: String) {
        //: TalkingVideoDanmuManager.uploadToTextMsg(logId: "\(chatModel.logId)", content: msgStr) { succeed, result, errorModel in
        DirectorDanmuManager.untilSend(logId: "\(chatModel.logId)", content: msgStr) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let json = JSON(result!)
                let json = JSON(result!)
                //: let msgId = json["msgId"].string
                let msgId = json[String(bytes: kFrameId.map{ofReplace(tolerance: $0)}, encoding: .utf8)!].string
                //: var toUid: String?
                var toUid: String?
                //: if self.chatModel.iUid == DistanceAppManager.share.loginUserMode.userID {
                if self.chatModel.iUid == DistanceAppManager.share.loginUserMode.userID {
                    //: toUid = self.chatModel.pairUid
                    toUid = self.chatModel.pairUid
                    //: } else {
                } else {
                    //: toUid = self.chatModel.iUid
                    toUid = self.chatModel.iUid
                }
                //: let data = ["msgId": msgId, "senderUid": DistanceAppManager.share.loginUserMode.userID, "toUid": toUid, "nickname": DistanceAppManager.share.loginUserMode.nickname, "content": msgStr, "isMySender": "1"]
                let data = [String(bytes: kFrameId.map{ofReplace(tolerance: $0)}, encoding: .utf8)!: msgId, String(bytes: kLabelPath.map{layerTo(text: $0)}, encoding: .utf8)!: DistanceAppManager.share.loginUserMode.userID, (String(k_equalNowName.suffix(5))): toUid, String(bytes: kColorAtUrl.reversed(), encoding: .utf8)!: DistanceAppManager.share.loginUserMode.nickname, String(bytes: k_arrayPath.map{whiteManagerValue(app: $0)}, encoding: .utf8)!: msgStr, String(bytes: kHouseUrl.map{yearBack(from: $0)}, encoding: .utf8)!: "1"]
                //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data as [String: Any])
                DirectorDanmuManager.blockCurrent().sequenceData(msg: data as [String: Any])
                //: self.rotateCameraBtn.isHidden = true
                self.rotateCameraBtn.isHidden = true
                //: self.changeInteractionShowState()
                self.travelKit()
                //: self.commInputView.inputTextView.attributedText = nil
                self.commInputView.inputTextView.attributedText = nil
            }
        }
    }
}

// MARK: - PlayerMakeThen【视频播放器】

//: extension Talking1v1VideoInteractionView: TalkingVideoPlayerDelegate {
extension AddPublishThen: PlayerMakeThen {
    //: func func__playerStatus(player: TalkingVideoPlayerManager, status: VideoPlayerStatus) {
    func menu(player _: DecisionMakerThen, status: ObjectPlayerStatus) {
        //: if status == .Playing {
        if status == .Playing {
            //: self.player.setRenderMode(renderMode: .FILL_SCREEN)
            self.player.sharedMode(renderMode: .FILL_SCREEN)
            //: self.coverImg.isHidden = true
            self.coverImg.isHidden = true
        }
    }

    //: func func__playerDuration(player: TalkingVideoPlayerManager, duration: Int, currentTime: Int) {}
    func decisionMaker(player _: DecisionMakerThen, duration _: Int, currentTime _: Int) {}

    //: func func__playerProgress(player: TalkingVideoPlayerManager, progress: CGFloat) {}
    func playerAction(player _: DecisionMakerThen, progress _: CGFloat) {}
}

// MARK: - 通知事件

//: extension Talking1v1VideoInteractionView {
extension AddPublishThen {
    /// 商汤美颜是否捕获到人脸通知（女性）
    //: @objc private func catchFaceResultNotification(notification: NSNotification) {
    @objc private func surprise(notification: NSNotification) {
        // 女性 && 等待接听
        //: guard DistanceAppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.female.rawValue else { return }
        //: guard self.chatModel.videoStage == .Recieved else { return }
        guard self.chatModel.videoStage == .Recieved else { return }

        //: let userInfo = notification.userInfo as! [String: AnyObject]
        let userInfo = notification.userInfo as! [String: AnyObject]
        //: let hasFace = (userInfo["hasFace"] as! NSNumber).description.boolValue
        let hasFace = (userInfo[(String(kModelText.prefix(6)) + kDataKey.replacingOccurrences(of: "time", with: "e"))] as! NSNumber).description.boolValue
        //: showOrHidenAutoRecieved(autoAccept: hasFace)
        ofView(autoAccept: hasFace)
    }

    /// 是否展示自动接听按钮
    /// - Parameter autoAccept: 是否自动接听（默认：false）
    //: private func showOrHidenAutoRecieved(autoAccept: Bool = false) {
    private func ofView(autoAccept: Bool = false) {
        //: var isActive = false
        var isActive = false

        //: if DistanceAppManager.share.appConfigMode.autoAnswer == 1 {
        if DistanceAppManager.share.appConfigMode.autoAnswer == 1 {
            //: let timeDifference = Date().timeIntervalSince1970 - (TalkingApplication.shared as! TalkingApplication).lastEventTime
            let timeDifference = Date().timeIntervalSince1970 - (TouchMakeReactiveCompatible.shared as! TouchMakeReactiveCompatible).lastEventTime
            //: if timeDifference < 5 {
            if timeDifference < 5 {
                //: isActive = true
                isActive = true
            }
        }

        //: DispatchQueue.main.async { [weak self] in
        DispatchQueue.main.async { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if autoAccept || isActive {
            if autoAccept || isActive { // 自动接听
                //: self.autoAcceptDesLab.isHidden = false
                self.autoAcceptDesLab.isHidden = false

                //: var countdown = DistanceAppManager.share.appConfigMode.autoAnswerCountdownTime
                var countdown = DistanceAppManager.share.appConfigMode.autoAnswerCountdownTime
                // 防止多次初始化
                //: guard self.autoAcceptTimer == nil else { return }
                guard self.autoAcceptTimer == nil else { return }
                //: self.autoAcceptTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.global())
                self.autoAcceptTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.global())
                //: self.autoAcceptTimer?.schedule(deadline: .now(), repeating: .seconds(1))
                self.autoAcceptTimer?.schedule(deadline: .now(), repeating: .seconds(1))
                //: self.autoAcceptTimer?.setEventHandler {
                self.autoAcceptTimer?.setEventHandler {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: guard countdown > 0 else {
                        guard countdown > 0 else {
                            //: if self.chatModel.videoStage == .Recieved {
                            if self.chatModel.videoStage == .Recieved {
                                //: self.acceptButtonClick()
                                self.clickImage()
                            }
                            //: return
                            return
                        }
                        //: self.autoAcceptDesLab.text = "Automatically answer for you in %@s".localizedArguments(countdown)
                        self.autoAcceptDesLab.text = String(bytes: kMainUrl.map{faceEach(bottom: $0)}, encoding: .utf8)!.edgeDelay(countdown)
                        //: countdown -= 1
                        countdown -= 1
                    }
                }
                //: self.autoAcceptTimer?.resume()
                self.autoAcceptTimer?.resume()
            }
        }
    }

    /// 半屏充值页退下
    //: @objc private func changeRechargeStatus() {
    @objc private func revolution() {}
}

// MARK: -

//: extension Talking1v1VideoInteractionView {
extension AddPublishThen {
    //: func showPointAnimaData(point: String) {
    func showPoint(point: String) {
        //: addSubview(pointAnimView)
        addSubview(pointAnimView)
        //: pointAnimView.snp.remakeConstraints { make in
        pointAnimView.snp.remakeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
            make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
        }
        //: pointAnimView.setPointData(point: point)
        pointAnimView.withBuilder(point: point)
    }
}

// MARK: - 送礼

//: extension Talking1v1VideoInteractionView {
extension AddPublishThen {
    //: func func__sendGift() {
    func atInside() {
        //: TalkingChatGiftManager.share.func__sendGiftEvent {
        AppNameGiftManager.share.searchCompletion {
            //: self.func__showGiftChoiceView()
            self.equalWarningTime()
        }
    }

    //: func func__showGiftChoiceView() {
    func equalWarningTime() {
        //: giftView.updateGiftInfo(needReload: true, mf_coin: DistanceAppManager.share.loginUserMode.mf_coin)
        giftView.dataGift(needReload: true, mf_coin: DistanceAppManager.share.loginUserMode.mf_coin)
        //: window?.addSubview(giftView)
        window?.addSubview(giftView)
        //: giftView.showView()
        giftView.paradigmView()
        //: giftView.sendActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String) in
        giftView.sendActionBlock = { [weak self] (_ giftModel: RangeMeasurable, _ num: String) in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.func__sendGiftMsg(giftId: giftModel.gid, giftNum: num, pkgItemsetId: giftModel.pkgItemsetId, programSort: nil)
            self.sort(giftId: giftModel.gid, giftNum: num, pkgItemsetId: giftModel.pkgItemsetId, programSort: nil)
        }
    }

    //: func func_giftViewDismiss() {
    func pathFinancialAidDismiss() {
        //: giftView.dismissView()
        giftView.validSight()
    }

    //: func func__sendGiftMsg(giftId: String, giftNum: String, pkgItemsetId: String?, programSort: String?) {
    func sort(giftId: String, giftNum: String, pkgItemsetId: String?, programSort: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            arcInfo(showMsg: appViewPingValue)
            //: return
            return
        }
        //: var params: [String: Any] = [:]
        var params: [String: Any] = [:]
        //: if chatModel.iUid == DistanceAppManager.share.loginUserMode.userID {
        if chatModel.iUid == DistanceAppManager.share.loginUserMode.userID {
            //: params["toUid"] = chatModel.pairUid
            params[(String(k_equalNowName.suffix(5)))] = chatModel.pairUid
            //: } else {
        } else {
            //: params["toUid"] = chatModel.iUid
            params[(String(k_equalNowName.suffix(5)))] = chatModel.iUid
        }
        //: params["giftId"] = giftId
        params[(String(kToKey.prefix(6)))] = giftId
        //: params["giftNum"] = giftNum
        params[(String(k_cosKey))] = giftNum
        //: params["from"] = "videoCall"
        params[(String(k_equalPath))] = (String(k_makeId.prefix(8)) + kTargetFormat.replacingOccurrences(of: "single", with: "l"))
        //: if programSort != nil {
        if programSort != nil {
            //: params["programSort"] = programSort
            params[(String(k_logText) + String(k_statuteId) + String(kFrameMsg.prefix(5)))] = programSort
        }
        //: if pkgItemsetId != nil {
        if pkgItemsetId != nil {
            //: params["pkgItemsetId"] = pkgItemsetId
            params[(String(kActualIconMessage) + String(kAfterFormat))] = pkgItemsetId
        }

        //: PlayerRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        PlayerRequestTool.playerLast(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.pickResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if pkgItemsetId != nil {
            if pkgItemsetId != nil {
                //: self.giftView.bags_removeGiftPackage(pkgItemsetId: pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.giftView.poke(pkgItemsetId: pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! [String: Any?])
            self.tingForExtral(extral: result as! [String: Any?])
            //: })
        })
    }

    //: func func__insertGiftMessageWithExtral(extral: [String: Any?]) {
    func tingForExtral(extral: [String: Any?]) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: [String: Any] = extralInfo["msgInfo"] as! [String: Any]
        var msgInfoDic: [String: Any] = extralInfo[(String(kFemaleTitle.prefix(5)) + k_centerUrl.replacingOccurrences(of: "size", with: "fo"))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(k_messCurrentKey.replacingOccurrences(of: "lab", with: "ms") + String(kToUrl.prefix(5)))] = (kMomentName.replacingOccurrences(of: "head", with: "g"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(kFemaleTitle.prefix(5)) + k_centerUrl.replacingOccurrences(of: "size", with: "fo"))] = msgInfoDic

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        beginSelect(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(k_whitePath) + String(k_effectName))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(k_whitePath) + String(k_effectName))] as! NSNumber
            //: DistanceAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            DistanceAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: giftView.updateGiftInfo(needReload: false, mf_coin: DistanceAppManager.share.loginUserMode.mf_coin)
        giftView.dataGift(needReload: false, mf_coin: DistanceAppManager.share.loginUserMode.mf_coin)

        //: var toUid = ""
        var toUid = ""
        //: if chatModel.iUid == DistanceAppManager.share.loginUserMode.userID {
        if chatModel.iUid == DistanceAppManager.share.loginUserMode.userID {
            //: toUid = chatModel.pairUid
            toUid = chatModel.pairUid
            //: } else {
        } else {
            //: toUid = chatModel.iUid
            toUid = chatModel.iUid
        }

        //: let cellData = TUISocialChatManager.insertTXMessage(withExtral: extralInfo, toUid: toUid, isSender: true, isTip: false)
        let cellData = IsoclinalThen.implantOfTip(withExtral: extralInfo, toUid: toUid, isSender: true, isTip: false)

        //: NotificationCenter.default.post(name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION, object: self, userInfo: ["cellData": cellData])
        NotificationCenter.default.post(name: main_userName, object: self, userInfo: [String(bytes: kPhoneFormat.map{userView(nor: $0)}, encoding: .utf8)!: cellData])
    }

    //: func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    func pickResend(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard DistanceAppManager.share.loginUserMode.status != 1 else {
            guard DistanceAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    arcInfo(showMsg: errorStr)
                }
                //: return
                return
            }
            //: AddReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            AddReactiveCompatible.share.viewParams(clickEvent: show_appData, sufficient: false)
            //: giftView.dismissView()
            giftView.validSight()
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = ValuePopView(frame: CGRect(x: 0, y: 0, width: constFormalContent, height: user_recordMarginValue))
            //: view.show()
            view.attributeStrokeHalfGroup()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                arcInfo(showMsg: errorStr)
            }
        }
    }

    //: func initCachEffectData() {
    func sodom() {
        //: var targetId = ""
        var targetId = ""
        //: if chatModel.iUid == DistanceAppManager.share.loginUserMode.userID {
        if chatModel.iUid == DistanceAppManager.share.loginUserMode.userID {
            //: targetId = chatModel.pairUid
            targetId = chatModel.pairUid
            //: } else {
        } else {
            //: targetId = chatModel.iUid
            targetId = chatModel.iUid
        }
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.exhibit(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: [String: Any]) {
    func beginSelect(giftDic: [String: Any]) {
        //: let giftModel = giftEffectView.changeGiftToModel(dict: giftDic) ?? TalkingGiftMsgAnimatModel()
        let giftModel = giftEffectView.social(dict: giftDic) ?? TitleManagerMeasurable()
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in AppNameGiftManager.share.detailItems() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [PublishHandyJSON]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.imageModel(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.asReceiveDict(dict: giftDic)
    }

    //: func addGiftEffectModelArr(giftArr: [Any]) {
    func privacy(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in AppNameGiftManager.share.detailItems() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [PublishHandyJSON]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.imageModel(model: giftModel as! TitleManagerMeasurable)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.keyArr(modelArr: giftArr)
    }
}

// MARK: - 通话计时器

//: extension Talking1v1VideoInteractionView {
extension AddPublishThen {
    /// 开始通话计时
    //: private func startTalkCount() {
    private func deal() {
        //: guard countTimer == nil else { return }
        guard countTimer == nil else { return }

        //: if startTime == nil {
        if startTime == nil {
            //: let interval = Date().timeIntervalSince1970
            let interval = Date().timeIntervalSince1970
            //: startTime = interval
            startTime = interval
            //: chatModel.startTime = startTime
            chatModel.startTime = startTime
        }
        //: countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeTalkTime()
            self.activityIn()
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 免费视频剩余时间倒计时
    //: private func setFreeCountTimer() {
    private func queue() {
        //: guard freeCountTimer == nil else { return }
        guard freeCountTimer == nil else { return }
        //: var  index = 60
        var index = 60
        //: freeCountTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        freeCountTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: index -= 1
            index -= 1
            //: self.freeTimeView.setTipsMessage(count: index)
            self.freeTimeView.failure(count: index)
            //: }, repeats: true)
        }, repeats: true)
    }

    // 展示通话时间
    //: private func changeTalkTime() {
    private func activityIn() {
        //: let duration = getCurrentTalkingTime()
        let duration = objectTime()
        //: let min = floor(duration/60)
        let min = floor(duration / 60)
        //: let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        //: timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
    }

    /// 恢复通话，更新通话时间
    /// - Parameter startTime: 通话时间
    //: func updateStartTime(startTime: TimeInterval) {
    func cover(startTime: TimeInterval) {
        //: self.startTime = startTime
        self.startTime = startTime
        //: changeTalkTime()
        activityIn()
    }

    /// 获取当前通话时间
    /// - Returns: 秒
    //: func getCurrentTalkingTime() -> Double {
    func objectTime() -> Double {
        //: guard chatModel.videoStage == .Calling else { return 0 }
        guard chatModel.videoStage == .Calling else { return 0 }
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let duration = floor(interval-(startTime ?? 0))
        let duration = floor(interval - (startTime ?? 0))
        //: return duration
        return duration
    }
}

// MARK: - 【通话中】触屏隐藏交互按钮逻辑

//: extension Talking1v1VideoInteractionView {
extension AddPublishThen {
    /// 触摸隐藏开启延时
    //: private func touchHiddenTimer() {
    private func hiddenEqual() {
        //: invalidateIdleTimer()
        alongPair()
        //: perform(#selector(hideInteractionView), afterDelay: 10)
        perform(#selector(currentRequest), afterDelay: 10)
    }

    /// 销毁延迟
    //: private func invalidateIdleTimer() {
    private func alongPair() {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(hideInteractionView), object: nil)
        NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(currentRequest), object: nil)
    }

    /// 延时到时隐藏视图
    //: @objc private func hideInteractionView() {
    @objc private func currentRequest() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.changeInteractionShowState()
            self.travelKit()
        }
    }
}

// MARK: - Layout

//: extension Talking1v1VideoInteractionView {
extension AddPublishThen {
    /// 添加视图
    //: private func setupSubviews() {
    private func disappear() {
        //: addSubview(giftEffectView)
        addSubview(giftEffectView)
        //: insertSubview(giftTrackView, belowSubview: giftEffectView)
        insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: addSubview(coverImg)
        addSubview(coverImg)
        //: addSubview(touchHidenBtn)
        addSubview(touchHidenBtn)
        //: addSubview(pairInfoBg)
        addSubview(pairInfoBg)
        //: pairInfoBg.addSubview(pairPicV)
        pairInfoBg.addSubview(pairPicV)
        //: pairInfoBg.addSubview(pairNameLab)
        pairInfoBg.addSubview(pairNameLab)
        //: pairInfoBg.addSubview(locationBtn)
        pairInfoBg.addSubview(locationBtn)
        //: addSubview(desLab)
        addSubview(desLab)
        //: addSubview(minimizeBtn)
        addSubview(minimizeBtn)
        //: addSubview(hangupBtn)
        addSubview(hangupBtn)
        //: addSubview(rotateCameraBtn)
        addSubview(rotateCameraBtn)
        //: addSubview(openCameraBtn)
        addSubview(openCameraBtn)
        //: addSubview(sendGiftBtn)
        addSubview(sendGiftBtn)
        //: addSubview(rechargeBtn)
        addSubview(rechargeBtn)
        //: addSubview(timeLab)
        addSubview(timeLab)
        //: addSubview(reportBtn)
        addSubview(reportBtn)
        //: addSubview(acceptBtn)
        addSubview(acceptBtn)
        //: addSubview(chatBtn)
        addSubview(chatBtn)
        //: addSubview(danmuView)
        addSubview(danmuView)
        //: addSubview(commInputView)
        addSubview(commInputView)
        //: addSubview(femaleAcceptBGBtn)
        addSubview(femaleAcceptBGBtn)
        //: addSubview(femaleAcceptBtn)
        addSubview(femaleAcceptBtn)
        //: femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        //: addSubview(acceptPlayer)
        addSubview(acceptPlayer)
        //: addSubview(autoAcceptDesLab)
        addSubview(autoAcceptDesLab)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func lens() {
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(self)
            make.leading.trailing.top.bottom.equalTo(self)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(self)
            make.leading.equalTo(self)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - appButtonData)
        }
        //: coverImg.snp.makeConstraints { make in
        coverImg.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: touchHidenBtn.snp.makeConstraints { make in
        touchHidenBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: pairInfoBg.snp.makeConstraints { make in
        pairInfoBg.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+16)
            make.top.equalTo(main_netValue + 16)
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 128), height: actualWidth(w: 50)))
            make.size.equalTo(CGSize(width: actualWidth(w: 128), height: actualWidth(w: 50)))
        }
        //: pairPicV.snp.makeConstraints { make in
        pairPicV.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 5))
            make.leading.equalTo(actualWidth(w: 5))
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 40), height: actualWidth(w: 40)))
            make.size.equalTo(CGSize(width: actualWidth(w: 40), height: actualWidth(w: 40)))
        }
        //: pairNameLab.snp.makeConstraints { make in
        pairNameLab.snp.makeConstraints { make in
            //: make.leading.equalTo(pairPicV.snp.trailing).offset(8)
            make.leading.equalTo(pairPicV.snp.trailing).offset(8)
            //: make.top.equalTo(7)
            make.top.equalTo(7)
            //: make.height.equalTo(17)
            make.height.equalTo(17)
            //: make.trailing.equalToSuperview().offset(-7)
            make.trailing.equalToSuperview().offset(-7)
        }
        //: locationBtn.snp.makeConstraints { make in
        locationBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(pairNameLab)
            make.leading.equalTo(pairNameLab)
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(4)
            make.top.equalTo(pairNameLab.snp.bottom).offset(4)
            //: make.trailing.equalToSuperview().offset(-10)
            make.trailing.equalToSuperview().offset(-10)
        }
        //: desLab.snp.makeConstraints { make in
        desLab.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight+86)
            make.top.equalToSuperview().offset(main_netValue + 86)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
        }
        //: minimizeBtn.snp.makeConstraints { make in
        minimizeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+10)
            make.top.equalTo(main_netValue + 10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(actualWidth(w: 42))
            make.width.height.equalTo(actualWidth(w: 42))
        }

        //: acceptBtn.snp.makeConstraints { make in
        acceptBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(hangupBtn)
            make.bottom.equalTo(hangupBtn)
            //: make.size.equalTo(hangupBtn)
            make.size.equalTo(hangupBtn)
        }
        //: femaleAcceptBtn.snp.makeConstraints { make in
        femaleAcceptBtn.snp.makeConstraints { make in
            //: make.edges.equalTo(hangupBtn)
            make.edges.equalTo(hangupBtn)
        }
        //: femaleAcceptTipsBtn.snp.makeConstraints { make in
        femaleAcceptTipsBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(-9)
            make.bottom.equalTo(-9)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(StatusBarHeight+20)
            make.top.equalTo(main_netValue + 20)
        }
        //: autoAcceptDesLab.snp.makeConstraints { make in
        autoAcceptDesLab.snp.makeConstraints { make in
            //: make.center.width.equalToSuperview()
            make.center.width.equalToSuperview()
        }
        //: chatBtn.snp.makeConstraints { make in
        chatBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-appButtonData)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.equalTo(actualWidth(w: 157))
            make.width.equalTo(actualWidth(w: 157))
            //: make.height.equalTo(actualWidth(w: 34))
            make.height.equalTo(actualWidth(w: 34))
        }
        //: danmuView.snp.makeConstraints { make in
        danmuView.snp.makeConstraints { make in
            //: make.bottom.equalTo(chatBtn.snp.top).offset(-10)
            make.bottom.equalTo(chatBtn.snp.top).offset(-10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.height.equalTo(VideoMsgTableViewHeight)
            make.height.equalTo(main_useData)
            //: make.width.equalTo(VideoMsgTableViewWidth)
            make.width.equalTo(appPositionText)
        }
        //: reportBtn.snp.makeConstraints { make in
        reportBtn.snp.makeConstraints { make in
            //: make.trailing.equalToSuperview().offset(-10)
            make.trailing.equalToSuperview().offset(-10)
            //: make.centerY.equalTo(chatBtn)
            make.centerY.equalTo(chatBtn)
            //: make.size.equalTo(actualWidth(w: 33))
            make.size.equalTo(actualWidth(w: 33))
        }
        //: sendGiftBtn.snp.makeConstraints { make in
        sendGiftBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(reportBtn.snp.leading).offset(-10)
            make.trailing.equalTo(reportBtn.snp.leading).offset(-10)
            //: make.bottom.equalTo(reportBtn)
            make.bottom.equalTo(reportBtn)
            //: make.size.equalTo(reportBtn)
            make.size.equalTo(reportBtn)
        }
        //: rechargeBtn.snp.makeConstraints { make in
        rechargeBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(sendGiftBtn.snp.leading).offset(-10)
            make.trailing.equalTo(sendGiftBtn.snp.leading).offset(-10)
            //: make.bottom.equalTo(reportBtn)
            make.bottom.equalTo(reportBtn)
            //: make.size.equalTo(reportBtn)
            make.size.equalTo(reportBtn)
        }

        //: hangupBtn.snp.makeConstraints { make in
        hangupBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
            make.bottom.equalTo(-appButtonData - 161)
            //: make.size.equalTo(actualWidth(w: 70))
            make.size.equalTo(actualWidth(w: 70))
        }
        //: commInputView.snp.makeConstraints { make in
        commInputView.snp.makeConstraints { make in
            //: make.bottom.equalTo(self)
            make.bottom.equalTo(self)
            //: make.leading.trailing.equalTo(0)
            make.leading.trailing.equalTo(0)
            //: make.height.equalTo(actualWidth(w: 55))
            make.height.equalTo(actualWidth(w: 55))
        }
    }

    /// 更新约束
    //: private func updateLayout() {
    private func layout() {
        //: if chatModel.videoStage == .Recieved {
        if chatModel.videoStage == .Recieved {
            //: if DistanceAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.male.rawValue {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2-42-70)
                    make.leading.equalTo(constFormalContent / 2 - 42 - 70)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-appButtonData - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: acceptBtn.snp.remakeConstraints { make in
                acceptBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2+42)
                    make.leading.equalTo(constFormalContent / 2 + 42)
                    //: make.bottom.equalTo(hangupBtn)
                    make.bottom.equalTo(hangupBtn)
                    //: make.size.equalTo(hangupBtn)
                    make.size.equalTo(hangupBtn)
                }
                //: } else {
            } else {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(34)
                    make.leading.equalTo(34)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-appButtonData - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: femaleAcceptBGBtn.snp.remakeConstraints { make in
                femaleAcceptBGBtn.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-33)
                    make.trailing.equalTo(-33)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-147)
                    make.bottom.equalTo(-appButtonData - 147)
                    //: make.size.equalTo(CGSize.init(width: 208, height: 99))
                    make.size.equalTo(CGSize(width: 208, height: 99))
                }
                //: femaleAcceptBtn.snp.remakeConstraints { make in
                femaleAcceptBtn.snp.remakeConstraints { make in
                    //: make.centerX.equalTo(femaleAcceptBGBtn)
                    make.centerX.equalTo(femaleAcceptBGBtn)
                    //: make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    //: make.size.equalTo(CGSize.init(width: 180, height: 70))
                    make.size.equalTo(CGSize(width: 180, height: 70))
                }
                //: acceptPlayer.snp.remakeConstraints { make in
                acceptPlayer.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-8)
                    make.trailing.equalTo(-8)
                    //: make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    //: make.size.equalTo(85)
                    make.size.equalTo(85)
                }
                //: openCameraBtn.snp.makeConstraints { make in
                openCameraBtn.snp.makeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2+12)
                    make.leading.equalTo(constFormalContent / 2 + 12)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-15)
                    make.bottom.equalTo(-appButtonData - 15)
                    //: make.size.equalTo(actualWidth(w: 45))
                    make.size.equalTo(actualWidth(w: 45))
                }
                //: rotateCameraBtn.snp.makeConstraints { make in
                rotateCameraBtn.snp.makeConstraints { make in
                    //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                    make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                    //: make.centerY.equalTo(openCameraBtn)
                    make.centerY.equalTo(openCameraBtn)
                    //: make.size.equalTo(openCameraBtn)
                    make.size.equalTo(openCameraBtn)
                }
            }
            //: } else if chatModel.videoStage == .Calling {
        } else if chatModel.videoStage == .Calling {
            //: hangupBtn.snp.remakeConstraints { make in
            hangupBtn.snp.remakeConstraints { make in
                //: make.trailing.equalTo(rotateCameraBtn.snp.leading).offset(-10)
                make.trailing.equalTo(rotateCameraBtn.snp.leading).offset(-10)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
            //: openCameraBtn.snp.remakeConstraints { make in
            openCameraBtn.snp.remakeConstraints { make in
                //: make.top.equalTo(StatusBarHeight+actualHeight(h: 25)+actualHeight(h: 170)+26+35+9)
                make.top.equalTo(main_netValue + actualHeight(h: 25) + actualHeight(h: 170) + 26 + 35 + 9)
                //: make.trailing.equalToSuperview().offset(-10)
                make.trailing.equalToSuperview().offset(-10)
                //: make.size.equalTo(actualWidth(w: 33))
                make.size.equalTo(actualWidth(w: 33))
            }
            //: rotateCameraBtn.snp.remakeConstraints { make in
            rotateCameraBtn.snp.remakeConstraints { make in
                //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-10)
                make.trailing.equalTo(openCameraBtn.snp.leading).offset(-10)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
            //: } else {
        } else {
            //: hangupBtn.snp.makeConstraints { make in
            hangupBtn.snp.makeConstraints { make in
                //: make.centerX.equalToSuperview()
                make.centerX.equalToSuperview()
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                make.bottom.equalTo(-appButtonData - 161)
                //: make.size.equalTo(actualWidth(w: 70))
                make.size.equalTo(actualWidth(w: 70))
            }
            //: openCameraBtn.snp.makeConstraints { make in
            openCameraBtn.snp.makeConstraints { make in
                //: make.leading.equalTo(ScreenWidth/2+12)
                make.leading.equalTo(constFormalContent / 2 + 12)
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-15)
                make.bottom.equalTo(-appButtonData - 15)
                //: make.size.equalTo(actualWidth(w: 45))
                make.size.equalTo(actualWidth(w: 45))
            }
            //: rotateCameraBtn.snp.makeConstraints { make in
            rotateCameraBtn.snp.makeConstraints { make in
                //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
        }
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func firstMagnitudeeraction() {
        // 视频等待...动画
        //: waitTimer = Timer.scheduledTimer(withTimeInterval: 0.5, block: { [weak self] _ in
        waitTimer = Timer.scheduledTimer(withTimeInterval: 0.5, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard var text = self.desLab.text else { return }
            guard var text = self.desLab.text else { return }

            //: if text.hasSuffix("...") {
            if text.hasSuffix("...") {
                //: text = text.replacingOccurrences(of: "...", with: ".")
                text = text.replacingOccurrences(of: "...", with: ".")
                //: } else if text.hasSuffix("..") {
            } else if text.hasSuffix("..") {
                //: text = text.replacingOccurrences(of: "..", with: "...")
                text = text.replacingOccurrences(of: "..", with: "...")
                //: } else {
            } else {
                //: text = text.replacingOccurrences(of: ".", with: "..")
                text = text.replacingOccurrences(of: ".", with: "..")
            }
            //: self.desLab.text = text
            self.desLab.text = text

            //: }, repeats: true)
        }, repeats: true)
        //: waitTimer?.fire()
        waitTimer?.fire()
    }

    //: private func setFreeView() {
    private func ship() {
        //: addSubview(freeTimeView)
        addSubview(freeTimeView)
        //: freeTimeView.snp.remakeConstraints { make in
        freeTimeView.snp.remakeConstraints { make in
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.top.equalTo(timeLab.snp.bottom).offset(actualHeight(h: 299))
            make.top.equalTo(timeLab.snp.bottom).offset(actualHeight(h: 299))
            //: make.height.equalTo(111)
            make.height.equalTo(111)
            //: make.width.equalTo(126)
            make.width.equalTo(126)
        }
    }

    /// 添加通知
    //: private func initNotificationObserver() {
    private func spectator() {
        // 女性用户 && 使用商汤 && 人脸检测自动接听
        //: if DistanceAppManager.share.loginUserMode.sex == Gender.female.rawValue &&
        if DistanceAppManager.share.loginUserMode.sex == ManagerQuickLookable.female.rawValue,
           //: SenseTime_Use == true &&
           mainUserValue == true,
           //: DistanceAppManager.share.appConfigMode.autoAnswer == 1 {
           DistanceAppManager.share.appConfigMode.autoAnswer == 1
        {
            /// 监听 是否捕获到人脸
            //: NotificationCenter.default.addObserver(self,
            NotificationCenter.default.addObserver(self,
                                                   //: selector: #selector(catchFaceResultNotification(notification:)),
                                                   selector: #selector(surprise(notification:)),
                                                   //: name: NSNotification.Name(rawValue: "FaceActionEvent_Interval_12_frame"),
                                                   name: NSNotification.Name(rawValue: String(bytes: kTheFinishData.map{modelRoute(app: $0)}, encoding: .utf8)!),
                                                   //: object: nil)
                                                   object: nil)
        }

        //: NotificationCenter.default.addObserver(self, selector: #selector(changeRechargeStatus), name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(revolution), name: main_clickData, object: nil)
    }
}
