
//: Declare String Begin

/*: "cmd" :*/
fileprivate let k_decisionId:[UInt8] = [0xf2,0xfc,0xf5]

private func vanguardFront(cover num: UInt8) -> UInt8 {
    return num ^ 145
}

/*: "cancelCall" :*/
fileprivate let k_mainStr:[Character] = ["c","a","n","c","e","l"]
fileprivate let kModelLabName:String = "Callload to self to to"

/*: "data" :*/
fileprivate let k_lineMessDismissValue:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "logId" :*/
fileprivate let kFirstData:String = "logIdstop return gift"

/*: "#322870" :*/
fileprivate let kActualStr:[Character] = ["#","3","2","2","8","7"]
fileprivate let kStrengthFormatMessage:[Character] = ["0"]

/*: "Tap to start video chatting" :*/
fileprivate let kInfoBassFormat:String = "Tap toend view kit time size"
fileprivate let kMakeMsg:String = "rt vselected send"
fileprivate let kRangeContent:String = "colordeo"
fileprivate let kRankMessage:String = "tincenter"

/*: "icon_close_nor" :*/
fileprivate let kGiftStr:String = "equal to return stringicon_c"

/*: "icon_views_number" :*/
fileprivate let kBeanPath:String = "icon_vlength collection"
fileprivate let kCellStr:[Character] = ["i","e","w","s","_","n","u","m","b","e","r"]

/*: "%@ chatting" :*/
fileprivate let k_smallMessage:[Character] = ["%","@"," ","c","h","a","t","t","i","n"]
fileprivate let kViewValue:String = "style"

/*: "icon_lounge" :*/
fileprivate let kTitlePath:String = "view please view femaleicon_l"
fileprivate let k_giftStr:String = "ouphotoe"

/*: "icon_rank_coin" :*/
fileprivate let k_shareMsg:[Character] = ["i","c","o","n","_","r","a","n","k","_","c","o","i","n"]

/*: "vip_btn_top" :*/
fileprivate let k_cookieTitleName:String = "VIP"
fileprivate let kMainPath:String = "_btn_topif button make response awake"

/*: "btn_nearby_settings_nor" :*/
fileprivate let kPlusTitle:[Character] = ["b","t","n","_","n","e","a","r","b","y","_","s","e","t","t"]
fileprivate let k_keyBindContentUrl:String = "info increase let sizeings_nor"

/*: "%@ Coins/min" :*/
fileprivate let k_micMessage:[Character] = ["%","@"," ","C","o","i","n","s","/","m","i","n"]

/*: "get json error" :*/
fileprivate let k_itemStr:[Character] = ["g","e","t"," ","j","s","o","n"," ","e","r","r"]
fileprivate let k_kitPageFormat:String = "olab"

/*: "matchId" :*/
fileprivate let k_textInputReturnName:[Character] = ["m","a","t","c","h","I"]
fileprivate let kItemKey:String = "data"

/*: "errno" :*/
fileprivate let kModeUserPath:[UInt8] = [0xde,0xeb,0xeb,0xe7,0xe8]

fileprivate func frameMe(player num: UInt8) -> UInt8 {
    let value = Int(num) + 135
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "VIPFee" :*/
fileprivate let kItemMessage:[Character] = ["V","I","P","F","e"]
fileprivate let k_collectUpgradeKey:String = "array"

/*: "Matching now" :*/
fileprivate let k_reportMsg:String = "to end make toMatchi"
fileprivate let kEventMessage:String = "class model heightng now"

/*: "userTouch" :*/
fileprivate let k_colorMessage:[Character] = ["u","s","e","r","T","o","u","c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let k_indexStr:[Character] = ["m","f","/","f","r"]
fileprivate let kDomainName:String = "E"
fileprivate let kSectionMorePath:String = "eCalname text self"
fileprivate let k_yourPathId:String = "Videotransform wrap"

/*: "fromType" :*/
fileprivate let k_toText:[UInt8] = [0xc1,0xcd,0xca,0xc8,0xaf,0xd4,0xcb,0xc0]

fileprivate func dataFrom(label num: UInt8) -> UInt8 {
    let value = Int(num) - 91
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "You've been barred from receiving calls" :*/
fileprivate let kViewId:[UInt8] = [0xaf,0x99,0x83,0xd1,0x80,0x93,0xd6,0x94,0x93,0x93,0x98,0xd6,0x94,0x97,0x84,0x84,0x93,0x92,0xd6,0x90,0x84,0x99,0x9b,0xd6,0x84,0x93,0x95,0x93,0x9f,0x80,0x9f,0x98,0x91,0xd6,0x95,0x97,0x9a,0x9a,0x85]

/*: "source" :*/
fileprivate let k_makePath:[UInt8] = [0x67,0x63,0x69,0x66,0x57,0x59]

fileprivate func seasonablenessSocial(max num: UInt8) -> UInt8 {
    let value = Int(num) - 244
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let k_videoPath:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "uid" :*/
fileprivate let kAddSharePath:[UInt8] = [0xb5,0xa9,0xa4]

fileprivate func domainFill(time num: UInt8) -> UInt8 {
    let value = Int(num) + 192
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "fromFreeCall" :*/
fileprivate let k_ofStr:[Character] = ["f","r","o","m","F","r","e","e"]
fileprivate let k_withId:String = "name directionCall"

/*: "requestCall" :*/
fileprivate let k_makeChangeUrl:[Character] = ["r","e","q","u","e","s","t"]
fileprivate let kAddEqualName:String = "false fromCall"

/*: "onVideoMatch" :*/
fileprivate let kHideUserValue:[Character] = ["o","n","V","i","d","e","o","M","a"]
fileprivate let kFrameName:[Character] = ["t","c","h"]

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let kBlockTitle:[UInt8] = [0x7a,0x60,0x54,0x55,0x4e,0x4c,0x40,0x55,0x48,0x42,0x1,0x57,0x48,0x45,0x44,0x4e,0x7c,0x1,0x73,0x44,0x42,0x44,0x48,0x57,0x44,0x1,0x57,0x48,0x45,0x44,0x4e,0x1,0x42,0x40,0x4d,0x4d,0x1,0x48,0x4f,0x42,0x4e,0x4c,0x48,0x4f,0x46]

private func mTheory(voice num: UInt8) -> UInt8 {
    return num ^ 33
}

/*: "refuseCall" :*/
fileprivate let kFatalStr:String = "rtapfustap"

/*: "onCancelCall" :*/
fileprivate let kManagerContent:[Character] = ["o","n","C","a","n","c","e","l","C","a","l","l"]

/*: "retryAfter" :*/
fileprivate let k_keepArcFormat:String = "RETR"
fileprivate let kEqualMsg:String = "bar"

/*: "onRequestCall" :*/
fileprivate let k_appFormat:[Character] = ["o","n","R","e","q","u","e","s","t","C","a","l","l"]

/*: "failType" :*/
fileprivate let kBeanMsg:String = "failTyptext mode m guard"
fileprivate let kTimeValue:String = "wrap"

/*: "callWaitingTime" :*/
fileprivate let kRegularFormat:String = "caequal"
fileprivate let kArrayMessage:[Character] = ["g","T","i","m","e"]

/*: "toUid" :*/
fileprivate let kBottomFormat:String = "self to astoUid"

/*: "Free 1 min" :*/
fileprivate let k_statusEventTitle:[Character] = ["F","r","e","e"," "]
fileprivate let k_modelTitle:String = "1 minvideo for kit make model"

/*: "%@/min" :*/
fileprivate let k_frameConfirmKey:String = "%@/minself to data"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EqualModelObjectProtocol.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class EqualModelObjectProtocol: StartThen {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.refreshOrAssessmentPrice()
            }
        }
        //: self.resetCamera()
        self.controlCamera()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        lastSubviews()
        //: setupSubViewsConstraint()
        constraintTag()
        //: bindInteraction()
        toCell()
        //: videoManager.startPreview(isMatch: true)
        videoManager.preview(isMatch: true)
        //: setPlayer(isRandon: false)
        sample(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            areaImage()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            ListSocketManager.shared.pathTab(info: [String(bytes: k_decisionId.map{vanguardFront(cover: $0)}, encoding: .utf8)!: (String(k_mainStr) + String(kModelLabName.prefix(4))), String(bytes: k_lineMessDismissValue.reversed(), encoding: .utf8)!: [(String(kFirstData.prefix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        timerMore()
        //: if TalkingSocketManager.shared.isSpace == false {
        if ListSocketManager.shared.isSpace == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            ListSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            ListSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            ListSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            ListSocketManager.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ContentChatManager = {
        //: let m = TalkingVideoChatManager()
        let m = ContentChatManager()
        //: let info = TalkingVideoChatModel()
        let info = ViewChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .colorStatuteNameApp()
        v.backgroundColor = .colorStatuteNameApp()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(areaImage), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: constFormalContent + 1, height: user_recordMarginValue + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(kActualStr) + String(kStrengthFormatMessage)))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(kInfoBassFormat.prefix(6)) + " sta" + String(kMakeMsg.prefix(4)) + kRangeContent.replacingOccurrences(of: "color", with: "i") + " chat" + kRankMessage.replacingOccurrences(of: "center", with: "g")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.submitGift(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(kGiftStr.suffix(6)) + "lose_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(verbalizeClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: ResumeControl = {
        //: let btn = TalkingButton()
        let btn = ResumeControl()
        //: btn.setImage(UIImage.submitGift(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.submitGift(name: (String(kBeanPath.prefix(6)) + String(kCellStr))), for: .normal)
        //: btn.setImage(UIImage.submitGift(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.submitGift(name: (String(kBeanPath.prefix(6)) + String(kCellStr))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.fontChange(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.fontChange(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(k_smallMessage) + kViewValue.replacingOccurrences(of: "style", with: "g")).edgeDelay(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.submitGift(name: "icon_lounge")
        img.image = UIImage.submitGift(name: (String(kTitlePath.suffix(6)) + k_giftStr.replacingOccurrences(of: "photo", with: "ng")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.submitGift(name: "icon_rank_coin")
        img.image = UIImage.submitGift(name: (String(k_shareMsg)))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.fontChange(type: .Regular, fontSize: 15)
        lab.font = UIFont.fontChange(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.submitGift(name: "vip_btn_top")
        imgV.image = UIImage.submitGift(name: (k_cookieTitleName.lowercased() + String(kMainPath.prefix(8))))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: ResumeControl = {
        //: let btn = TalkingButton()
        let btn = ResumeControl()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(adjoinClick), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.submitGift(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.submitGift(name: (String(kPlusTitle) + String(k_keyBindContentUrl.suffix(8)))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.submitGift(name: "icon_rank_coin")
        imgV.image = UIImage.submitGift(name: (String(k_shareMsg)))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .usufructuaryHeadSkinColour()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(k_micMessage)).edgeDelay(vipFee)
        //: lab.font = UIFont.fontChange(type: .Medium, fontSize: 15)
        lab.font = UIFont.fontChange(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: CancelReactiveCompatible = //: return CancelReactiveCompatible(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension EqualModelObjectProtocol {
    //: private func setPlayer(isRandon: Bool) {
    private func sample(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = VideoEffectTool.default.conType(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(k_itemStr) + k_kitPageFormat.replacingOccurrences(of: "lab", with: "r")))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func memory(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(k_textInputReturnName) + kItemKey.replacingOccurrences(of: "data", with: "d")): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: k_decisionId.map{vanguardFront(cover: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: kModeUserPath.map{frameMe(player: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        ValuePublishVideoManager.listObject(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.everyVideo()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(k_textInputReturnName) + kItemKey.replacingOccurrences(of: "data", with: "d"))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func diakinesis() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        ValuePublishVideoManager.thirdSub { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(kItemMessage) + k_collectUpgradeKey.replacingOccurrences(of: "array", with: "e"))].intValue
            //: self.refreshVideoCallPrice()
            self.refreshOrAssessmentPrice()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension EqualModelObjectProtocol {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func adjoinClick() {
        //: AddReactiveCompatible.share.func__pushToSubscribeAlert()
        AddReactiveCompatible.share.digitiserParams()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func verbalizeClick() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        switcheHalf()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        kSoundName.messUp(eventID: appChangeValue)

        //: if DistanceAppManager.share.appConfigMode.displayMatchV4PopUp, DistanceAppManager.share.loginUserMode.freeCallTimes > 0,
        if DistanceAppManager.share.appConfigMode.displayMatchV4PopUp, DistanceAppManager.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.infoShow()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        selectStart()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func areaImage() {
        //: guard TalkingSocketManager.shared.isSpace == false else {
        guard ListSocketManager.shared.isSpace == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.arcInfo(showMsg: kTopReplaceHeightData)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        teamOpen()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(k_reportMsg.suffix(6)) + String(kEventMessage.suffix(6))).localized
        //: setPlayer(isRandon: true)
        sample(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if DistanceAppManager.share.loginUserMode.randomVideoType == 2 {
        if DistanceAppManager.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            kSoundName.messUp(eventID: appExploreName)
            //: req_getVideoInfo()
            withValue()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            kSoundName.messUp(eventID: showBottomValue)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            ListSocketManager.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            ListSocketManager.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            ListSocketManager.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            ListSocketManager.shared.errorDelegate = self
            //: restart_randomVideo()
            bull()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func bull(cmd: String = (String(k_colorMessage)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        timerMore()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        memory(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func everyVideo() {
        // 停止定时器
        //: destroyWaitingTimer()
        timerMore()
        //: showCloseBtn()
        switcheHalf()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(kInfoBassFormat.prefix(6)) + " sta" + String(kMakeMsg.prefix(4)) + kRangeContent.replacingOccurrences(of: "color", with: "i") + " chat" + kRankMessage.replacingOccurrences(of: "center", with: "g")).localized
        //: setPlayer(isRandon: false)
        sample(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isSpace == false {
        if ListSocketManager.shared.isSpace == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            ListSocketManager.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            ListSocketManager.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            ListSocketManager.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            ListSocketManager.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        refreshOrAssessmentPrice()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func timerMore() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func withValue() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(k_indexStr) + kDomainName.lowercased() + String(kSectionMorePath.prefix(4)) + "l/get" + String(k_yourPathId.prefix(5)) + "Info")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: k_toText.map{dataFrom(label: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.everyVideo()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: DistanceAppManager.share.loginUserMode.randomVideoType = 1
                    DistanceAppManager.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.areaImage()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.arcInfo(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = FirstModelType.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = DecisionMakerThen()
                //: player.setMute(bEnable: true)
                player.setFor(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.dataView(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                AddModeReactiveCompatible.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                ToolThen.drunkComponent { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.talkBack()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func talkBack() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        kSoundName.messUp(eventID: const_pointValue)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard AddModeReactiveCompatible.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.arcInfo(showMsg: String(bytes: kViewId.map{$0^246}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            AddModeReactiveCompatible.shared.operationSub(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.everyVideo()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = RenameViewController()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.selectStart()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.everyVideo()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func teamOpen() {
        //: if DistanceAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if DistanceAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(DistanceAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(DistanceAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func switcheHalf() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func controlCamera() {
        //: guard TalkingSocketManager.shared.isSpace == false else {
        guard ListSocketManager.shared.isSpace == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.preview()
        }
    }
}

// MARK: - JibeMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension EqualModelObjectProtocol: JibeMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func interval(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = ValueModelType.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(k_textInputReturnName) + kItemKey.replacingOccurrences(of: "data", with: "d")): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: k_makePath.map{seasonablenessSocial(max: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: k_videoPath.reversed(), encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: kAddSharePath.map{domainFill(time: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = NameBinaryCount.randow
        //: if DistanceAppManager.share.loginUserMode.freeCallTimes > 0 && DistanceAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if DistanceAppManager.share.loginUserMode.freeCallTimes > 0 && DistanceAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(k_ofStr) + String(k_withId.suffix(4))))
            //: TalkingSocketManager.shared.isFreeCall = true
            ListSocketManager.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: k_decisionId.map{vanguardFront(cover: $0)}, encoding: .utf8)!: (String(k_makeChangeUrl) + String(kAddEqualName.suffix(4))), String(bytes: k_lineMessDismissValue.reversed(), encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        ListSocketManager.shared.pathTab(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            ListSocketManager.shared.pathTab(info: [String(bytes: k_decisionId.map{vanguardFront(cover: $0)}, encoding: .utf8)!: (String(k_mainStr) + String(kModelLabName.prefix(4))), String(bytes: k_lineMessDismissValue.reversed(), encoding: .utf8)!: [(String(kFirstData.prefix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.bull(cmd: (String(kHideUserValue) + String(kFrameName)))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func ingot(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(kFirstData.prefix(5)))].intValue
    }
}

// MARK: - ItemCallerDelegate【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension EqualModelObjectProtocol: ItemCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func motivation(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func entitle(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func currencyHide(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func positionData(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            RegisterDelegateHelper.equalClick(body: String(bytes: kBlockTitle.map{mTheory(voice: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = ValueModelType.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            mostValuablePlayer(toUid: String(uid))
        }
        //: stop_randomVideo()
        everyVideo()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.applicationAdd()
        //: DistanceAppManager.share.start1v1TalkCall(info: data, situation: .Calling)
        DistanceAppManager.share.getStarted(info: data, situation: .Calling)
    }
}

// MARK: - VisualCommunicationObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension EqualModelObjectProtocol: VisualCommunicationObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func fosterling(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        bull(cmd: (kFatalStr.replacingOccurrences(of: "tap", with: "e") + "Call"))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func addUser(data _: [String: Any]) {}
}

// MARK: - PerformanceErrorDelegate

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension EqualModelObjectProtocol: PerformanceErrorDelegate {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func socket(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(kManagerContent)) { // 取消通话异常
            //: stop_randomVideo()
            everyVideo()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(kHideUserValue) + String(kFrameName)), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            everyVideo()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(k_textInputReturnName) + kItemKey.replacingOccurrences(of: "data", with: "d"))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.arcInfo(showMsg: msg)
            //: uploadFailTime(type: 2)
            actionWith(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(kHideUserValue) + String(kFrameName)), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(k_keepArcFormat.lowercased() + "yAfte" + kEqualMsg.replacingOccurrences(of: "bar", with: "r"))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(k_textInputReturnName) + kItemKey.replacingOccurrences(of: "data", with: "d"))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.bull(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(k_appFormat)), errorNo == AppListSubscriptType.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            everyVideo()
            //: guard DistanceAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard DistanceAppManager.share.appStatus == NameQuickLookable.normal.rawValue else { return }
            //: AddReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            AddReactiveCompatible.share.viewParams(clickEvent: kNearbyTitle, sufficient: false)
            //: uploadFailTime(type: 1)
            actionWith(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        bull(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension EqualModelObjectProtocol {
    //: func uploadFailTime(type: Int) {
    func actionWith(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(kBeanMsg.prefix(7)) + kTimeValue.replacingOccurrences(of: "wrap", with: "e"))] = type
        //: parm["callWaitingTime"] = time
        parm[(kRegularFormat.replacingOccurrences(of: "equal", with: "ll") + "Waitin" + String(kArrayMessage))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        kSoundName.releaseStr(eventID: appVoiceValue, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func mostValuablePlayer(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(kRegularFormat.replacingOccurrences(of: "equal", with: "ll") + "Waitin" + String(kArrayMessage))] = time
        //: parm["toUid"] = toUid
        parm[(String(kBottomFormat.suffix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        kSoundName.releaseStr(eventID: const_screenText, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension EqualModelObjectProtocol {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func refreshOrAssessmentPrice() {
        //: guard DistanceAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard DistanceAppManager.share.appStatus == NameQuickLookable.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !DistanceAppManager.share.loginUserMode.loungePlus
        vipIcon.isHidden = !DistanceAppManager.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = NameBinaryCount.randow
        //: if DistanceAppManager.share.loginUserMode.freeCallTimes > 0 && DistanceAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if DistanceAppManager.share.loginUserMode.freeCallTimes > 0, DistanceAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if DistanceAppManager.share.appUserConfigMode.callBtnStyle == 1 {
            if DistanceAppManager.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(k_statusEventTitle) + String(k_modelTitle.prefix(5))).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = DistanceAppManager.share.loginUserMode.loungePlus ? vipFee:fee
            let price = DistanceAppManager.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(k_frameConfirmKey.prefix(6))).edgeDelay(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(k_frameConfirmKey.prefix(6))).edgeDelay(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.tapShared(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = DistanceAppManager.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = DistanceAppManager.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = DistanceAppManager.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = DistanceAppManager.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !DistanceAppManager.share.loginUserMode.loungePlus
            priceStackView.isHidden = !DistanceAppManager.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func lastSubviews() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func constraintTag() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(main_netValue + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func toCell() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(refreshOrAssessmentPrice),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: dataShowContent,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(diakinesis),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: dataQueryName,
                                               //: object: nil)
                                               object: nil)
    }
}
