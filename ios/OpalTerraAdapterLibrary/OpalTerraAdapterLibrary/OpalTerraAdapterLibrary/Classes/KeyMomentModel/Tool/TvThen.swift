
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let kDataStr:[Character] = ["m","f","/","m","o"]
fileprivate let k_componentText:String = "ment/range gift app user"

/*: "mf/moment/moments" :*/
fileprivate let kPicSendStr:String = "start topmf/mom"
fileprivate let kImageTitle:String = "actual"
fileprivate let k_lengthPath:String = "nt/mofalse to model"

/*: "mf/moment/like" :*/
fileprivate let kScaleViewTitle:[Character] = ["m","f","/","m","o","m"]
fileprivate let kDataSizeTitleMsg:[Character] = ["e","n","t","/","l","i","k","e"]

/*: "mf/crush/send" :*/
fileprivate let k_infoBodyPath:[Character] = ["m","f","/","c","r","u","s"]
fileprivate let kPathValue:String = "control"
fileprivate let kImageText:String = "background color inner title/send"

/*: "mf/moment/view" :*/
fileprivate let kArrayBlockId:[Character] = ["m","f","/","m","o","m","e","n","t"]
fileprivate let kInfoName:[Character] = ["/","v","i","e","w"]

/*: "mf/momentReply/list" :*/
fileprivate let kSuspendTitle:[Character] = ["m","f","/","m","o"]
fileprivate let k_drownKey:[Character] = ["m","e","n","t","R","e"]
fileprivate let k_viewKey:String = "of title action background elseply/list"

/*: "mf/momentReply/save" :*/
fileprivate let kTextPath:String = "mf/molocal var text player false"
fileprivate let kToTitle:String = "send if content vareply/save"

/*: "mf/moment/videoSign" :*/
fileprivate let k_draftName:String = "data line to method modemf/mom"
fileprivate let k_runId:String = "ideoSreturn shared add app"
fileprivate let kAppTableMessage:String = "igedge"

/*: "dynamicPic.zip" :*/
fileprivate let k_managerLabStr:String = "dviewa"
fileprivate let k_wrapStr:String = "c.zipvideo touch new selection"

/*: "pic" :*/
fileprivate let k_showUrl:[UInt8] = [0x55,0x4c,0x46]

private func modeOf(table num: UInt8) -> UInt8 {
    return num ^ 37
}

/*: "content" :*/
fileprivate let k_redModelId:[Character] = ["c","o","n","t","e","n","t"]

/*: "mf/moment/save" :*/
fileprivate let kTextReContent:[Character] = ["m","f","/","m","o","m","e","n","t","/"]
fileprivate let k_activeThirdManagerStr:String = "sapic"

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let k_headName:[UInt8] = [0x15,0x30,0x27,0x29,0x2c,0x36,0x2d,0x65,0x16,0x30,0x26,0x26,0x20,0x36,0x36,0x23,0x30,0x29,0x69,0x65,0x2,0x2a,0x65,0x31,0x2a,0x65,0x8,0x3c,0x65,0x1,0x3c,0x2b,0x24,0x28,0x2c,0x26,0x36,0x65,0x31,0x2a,0x65,0x33,0x2c,0x20,0x32]

/*: "mf/moment/saveVideo" :*/
fileprivate let k_equalFormat:String = "cell add table truemf/mo"
fileprivate let kUserName:[Character] = ["m","e"]
fileprivate let kSourceContent:String = "nt/savlet user"

/*: .png" :*/
fileprivate let k_viewValue:String = ".pnglet target"

/*: "dynamicPic" :*/
fileprivate let kRecordContent:String = "clickyna"
fileprivate let kTempContent:String = "stop"

/*: "freeImage" :*/
fileprivate let kMediumUrl:String = "freeImatype to at possibility height"
fileprivate let kSignDataPath:String = "toe"

/*: "mf/momentReply/replylist" :*/
fileprivate let k_sharedIconMsg:String = "mf/mimage sure method context"
fileprivate let kVideoName:String = "actual viewply/"

/*: "mf/moment/delete" :*/
fileprivate let kShareMessage:[Character] = ["m","f","/","m","o","m","e","n","t","/","d"]
fileprivate let kArrayValue:[Character] = ["e","l","e","t","e"]

/*: "mf/momentReply/delete" :*/
fileprivate let k_sizeMakeFormat:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/","d","e","l","e","t"]
fileprivate let kViewKey:[Character] = ["e"]

/*: "mf/moment/pin" :*/
fileprivate let kSkinName:String = "mf/momfalse the with player"

/*: "mf/moment/remindNum" :*/
fileprivate let k_headValueMessName:[Character] = ["m","f","/","m","o","m","e","n","t","/","r","e","m","i","n","d","N"]
fileprivate let kRowName:[Character] = ["u","m"]

/*: "mf/moment/remindList" :*/
fileprivate let kBlockTextValue:String = "mf/mmiss info"
fileprivate let k_retData:String = "oment/aspect key"
fileprivate let kMainPostPath:String = "any var right ok viewList"

/*: "page" :*/
fileprivate let kGreetMessage:[UInt8] = [0x48,0x39,0x3f,0x3d]

fileprivate func viewBubble(hidden num: UInt8) -> UInt8 {
    let value = Int(num) - 216
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cos/getCredential" :*/
fileprivate let k_rawTitle:String = "cos/gname document view equal icon"
fileprivate let kElementKey:[Character] = ["e","n","t","i","a","l"]

/*: "type" :*/
fileprivate let k_dataPlayerStr:String = "tnamee"

/*: "user/reportUserInfo" :*/
fileprivate let kGiftMsg:[Character] = ["u","s","e","r","/","r","e","p","o","r","t","U","s","e","r","I","n"]
fileprivate let k_styleUrl:String = "FO"

/*: "uid" :*/
fileprivate let k_positionMessage:[Character] = ["u","i","d"]

/*: "deviceId" :*/
fileprivate let kDataValue:String = "medium"
fileprivate let kSharedGiftKey:String = "eviceIdmodel nor as new view"

/*: "deviceName" :*/
fileprivate let k_amViewValue:String = "deviceNimage share warning transaction"
fileprivate let kCountDoingValue:[Character] = ["a","m","e"]

/*: "iphone" :*/
fileprivate let k_withStr:String = "mainphone"

/*: "fileUrl" :*/
fileprivate let kNameTitle:[Character] = ["f","i","l","e","U","r"]
fileprivate let kAddEmptyTextMsg:String = "pic"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TvThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class TvThen: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func fail(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(kDataStr) + String(k_componentText.prefix(5)) + "list")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func doEffect(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(kPicSendStr.suffix(6)) + kImageTitle.replacingOccurrences(of: "actual", with: "e") + String(k_lengthPath.prefix(5)) + "ments")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func dowrySelect(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(kScaleViewTitle) + String(kDataSizeTitleMsg))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func addValueCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(k_infoBodyPath) + kPathValue.replacingOccurrences(of: "control", with: "h") + String(kImageText.suffix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func completion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(kArrayBlockId) + String(kInfoName))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func alongCell(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(kSuspendTitle) + String(k_drownKey) + String(k_viewKey.suffix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func number(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(kTextPath.prefix(5)) + "mentR" + String(kToTitle.suffix(9)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func themeCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(k_draftName.suffix(6)) + "ent/v" + String(k_runId.prefix(5)) + kAppTableMessage.replacingOccurrences(of: "edge", with: "n"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func titleCompletion(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        clickTip()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            sunna(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((k_managerLabStr.replacingOccurrences(of: "view", with: "yn") + "micPi" + String(k_wrapStr.prefix(5))))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: k_showUrl.map{modeOf(table: $0)}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(String(k_redModelId))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = SweepRequestModel()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(kTextReContent) + k_activeThirdManagerStr.replacingOccurrences(of: "pic", with: "ve"))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.rear(showMsg: String(bytes: k_headName.map{$0^69}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.clickTip()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func messageRequest(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(k_equalFormat.suffix(5)) + String(kUserName) + String(kSourceContent.prefix(6)) + "eVideo")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func sunna(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.compressedImageData()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(k_viewValue.prefix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((kRecordContent.replacingOccurrences(of: "click", with: "d") + "micPi" + kTempContent.replacingOccurrences(of: "stop", with: "c")))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func clickTip() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(kMediumUrl.prefix(7)) + kSignDataPath.replacingOccurrences(of: "to", with: "g")))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func split(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(k_sharedIconMsg.prefix(4)) + "omentRe" + String(kVideoName.suffix(4)) + "replylist")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func extragalacticNebula(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(kShareMessage) + String(kArrayValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func laterDoing(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(k_sizeMakeFormat) + String(kViewKey))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func videoCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(kSkinName.prefix(6)) + "ent/pin")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func extra(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(k_headValueMessName) + String(kRowName))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func selectCompletion(page: Int, completion: @escaping (_ data: [SizeValueListModel]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(kBlockTextValue.prefix(4)) + String(k_retData.prefix(6)) + "remind" + String(kMainPostPath.suffix(4)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: kGreetMessage.map{viewBubble(hidden: $0)}, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [SizeValueListModel] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<SizeValueListModel>.deserialize(from: arr as? Array) as? [SizeValueListModel] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func tipCompletion(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(k_rawTitle.prefix(5)) + "etCred" + String(kElementKey))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(k_dataPlayerStr.replacingOccurrences(of: "name", with: "yp")): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func cardLike(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = SweepRequestModel()
        //: reqModel.requestServer = DistanceAppManager.share.appConfigMode.reportDomain
        reqModel.requestServer = DistanceAppManager.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(kGiftMsg) + k_styleUrl.lowercased())
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = DistanceAppManager.share.loginUserMode.userID
        params[(String(k_positionMessage))] = DistanceAppManager.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(kDataValue.replacingOccurrences(of: "medium", with: "d") + String(kSharedGiftKey.prefix(7)))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(String(k_amViewValue.prefix(7)) + String(kCountDoingValue))] = (k_withStr.replacingOccurrences(of: "main", with: "i"))
        //: params["type"] = 1
        params[(k_dataPlayerStr.replacingOccurrences(of: "name", with: "yp"))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(kNameTitle) + kAddEmptyTextMsg.replacingOccurrences(of: "pic", with: "l"))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        showEndName.buttonCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
